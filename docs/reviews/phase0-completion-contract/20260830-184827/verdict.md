# 레인 B 계획 «재심» — 53차 판 (재심 #15)

```yaml
adjudicator: codex
verdict: needs-attention
reviewed_at_head: 23a01775
reviewed_plan_paths:
  - docs/plans/2026-08-12-tos-phase0-completion-contract-design.md
  - docs/plans/2026-08-11-tos-completion-development-plan.md
reviewed_scope_digest: 17f800e1598677ad90d2e45f581664aa01d88dc92b1c6d494b1a15f071efaaed
bound_set_digest: daaba47b1c4b2b31717c098c8d761d9fd2b0cd1eb7e0d55b48d46a4e059f1c3b
job_id: review-mtfmnvt1-lb60ix
job_class: review
base: 4c9f68c8
scope: branch
prior_verdict: docs/reviews/phase0-completion-contract/20260830-182054/verdict.md
```

**게이트 판정: 통과 아님.**  findings **1 (medium)** — 신규 material.
**S-26 ② 카운터 0 유지** (세 회차 연속 needs-attention).

**#14 의 지적은 해소됐다** — 심판이 귀속 분리를 확인했고, 다른 재심 인용(`:6462` #9 ·
`:6465` #10)에서도 불일치를 **발견하지 못했다(없음을 증명한 것은 아님)**.
새 material 은 **다른 자리**에서 나왔다.

---

## Codex 판정 (verbatim — 손대지 않음)

```json
{
  "verdict": "needs-attention",
  "summary": "출하 보류. #14 지적은 회피가 아니라 해소됐다. #13의 유일 finding과 저작자 후속 발견이 정확히 분리됐고, #14 귀속도 사실과 일치한다. 다른 명시적 재심 귀속—including :6462의 #9 paired mutant와 :6465의 #10 worktree 반례—에서 불일치는 발견하지 못했다(없음을 증명한 것은 아님). 계수도 67/32/17, mutation 145종으로 일치했고, 불변 기본값·단일 allow_mutable=True 호출·세 축의 두-다리 blob 대조가 유지됐다. 그러나 처분 산문에서 별도 능력 과대서술 1건을 재현했다. 기본 검사는 rc 0이지만 self-test는 /tmp와 /private/tmp 모두 임시 디렉터리 생성 실패로 rc 2여서 145종 실행 결과는 독립 검증하지 못했다.",
  "findings": [
    {
      "severity": "medium",
      "title": "CAP-2 docstring이 코드펜스 위양성을 닫았다고 거짓 서술한다",
      "body": "`_cap_guard_sites`는 코드펜스 상태를 판별하지 않고 모든 비이력 행을 검사한다. 그런데 docstring은 코드펜스 안을 훑지 않는다고 주장한다. fenced `total_count > 1000 → PREVENTION_UNVERIFIABLE` 예시를 주입한 독립 호출에서 CAP 사이트가 4→5로 증가했으며, 계약도 코드펜스 예시의 위양성을 명시 잔여로 기록한다. 이는 그 (b) 잔여 자체의 단순 재확인이 아니라, 열린 능력을 닫았다고 기록한 별도 (a) 산문 결함이다. 이후 감사자가 과잉 차단이 해소됐다고 오판할 수 있다.",
      "file": "tools/tos_contract_check.py",
      "line_start": 2438,
      "line_end": 2439,
      "confidence": 0.99
    }
  ],
  "next_steps": [
    "2438-2439행의 능력 서술을 실제 구현 및 계약의 명시 잔여와 일치시킨다.",
    "쓰기 가능한 환경에서 `.venv/bin/python tools/tos_contract_check.py --self-test`를 실행해 145종 전건 판별·죽은 검사 0·과잉 차단 0을 독립 확인한다.",
    "(b) 계약 :3899 stale, S-26 ⑧, 핀 이전 세탁, 기존 셀 내부 숨기기와 코드펜스 위양성 자체는 기존 공개 잔여로 분리 추적한다."
  ]
}
```

원문 전체는 `.omc/review/20260830-184827/codex-raw.json`.

---

## 수용검사 (오케스트레이터 = Claude)

**채택 1 · 기각 0 · 팬텀 0.**

### 결속 대조

`plan_scope_digest` 포착 == 재계산 `17f800e1…` · `bound_set_digest` == `daaba47b…`
(결속 문서 무변경 → **O-6 재결속 불요**) · 계약 blob `ecbd478e…` 불변 ·
S-26 ①ⓑ 이력 술어 **공집합** — ⑥ 미발화.

### finding 실측 — 구조로 확정한다

| 검사 | 결과 |
|---|---|
| `file:line` (`:2438-2439`) | **실재** — `_cap_guard_sites` docstring |
| 문언 | 「코드펜스 «안»과 HTML 주석은 … **훑지 않는다**」 |
| 구현 | `_cap_guard_sites` 는 `doc.is_history_row` 만 건너뛰고 **전 행을 훑는다**.  호출하는 것은 `_normalize_scan_text`·`_scan_chunks` 뿐이고 **펜스 인지 함수(`_fence_spans`·`_enclosing_fence`)를 부르지 않는다** |
| 같은 파일의 반대 진술 | `:2363` 「**[44차 — 펜스 배제를 «철회»한다]** … 펜스 안 예시에 대한 위양성은 이 판이 닫지 «못한다» — **등재**한다」 |
| 의도적으로 silenced 인가 | **아니다** — 같은 파일이 정반대를 적는다 |
| 비협상 규칙 배치 | 없음 (배치 0 — 29판 연속) |

**기제의 «부재»로 확정했다.**  행동 탐침보다 강한 증거이며, 이 아크의 anti-phantom 규율
(존재·부재 양방향 확인)에 부합한다.

### 내 재현은 실패했다 — 그 사실을 그대로 적는다

심판은 「fenced 예시 주입에서 CAP 사이트가 **4→5**」라고 적었다.  오케스트레이터가
`_cap_guard_sites` 를 직접 호출해 두 형상(서술형·화살표형)을 펜스 «안»과 «밖»에 각각
주입했으나 **전부 4 → 4** 였다 — `GUARD_ARROW_RE`·`GUARD_CMP_RE`·`GUARD_CAPNUM_RE` 를 한
창에서 동시에 만족시키는 뮤턴트를 만들지 못했다.

**그러므로 이 판정은 심판의 «4→5» 수치를 승인하지 않는다.**  finding 은 **구조 근거로만**
채택한다(펜스 인지 기제 부재 + 같은 파일의 철회 선언).  재현 실패를 심판 오류로 읽지 않되,
재현하지 못한 수치를 재현한 것처럼 적지도 않는다.

### 귀속 — 53차가 만든 것이 아니다

`git log -S` 실측: 이 docstring 줄과 `:2363` 의 철회 선언이 **같은 커밋 `df72ae5b`(44차)**
에서 왔다.  44차가 배제를 철회하면서 **그 사실을 적은 자리와 옛 주장을 적은 자리를 둘 다
남겼다** — 한 커밋 안의 자기모순이고, 열한 판 동안 서 있었다.

53차가 만진 것은 `:312-318` 한 자리뿐이므로 이 finding 은 **그 diff 밖**이다.  그럼에도
**«범위 밖 기존 부채»로 분리하지 «않고» 차단으로 채택한다** — 이번 focus 가 «검사기 안에서
능력을 주장하는 자리가 실제 능력과 일치하는가»를 2순위 축으로 **명시 요청**했기 때문이다.
범위를 넓혀 놓고 그 결과를 범위 밖이라 부르는 것은 **피심판자가 심사 범위를 스스로 정하는
것**이며, 이 게이트가 막으려는 자기 승인과 같은 형상이다.

### S-26 축별 상태

| 축 | 상태 |
|---|---|
| ① 동결 (이력 술어) | 충족 |
| ② 2회 연속 material 0 | **0** — 세 회차 연속 material 1 |
| ③ validator rc 0 | 충족 |
| ④ CUR/CIT/VER/CARD/RULE 0 | 충족 |
| ⑤ 배터리 독립 검증 | **미검증 — 여섯 회차 연속**.  이번엔 `/tmp` 와 `/private/tmp` 둘 다 시도 후 rc 2 |
| ⑥ 리셋 | 미발화 — 카운터 0 은 ②의 실패 |
| ⑧ 도달 가능성 | 열림 (등재) |

### S-26 ② 카운터

**0.**  궤적: … → 0 → 0 → 1 → 1 → **1**.

---

## 오케스트레이터 관측 (판정 아님)

1. **세 회차 연속 material 이 «산문»에서 났고, 이번 것은 열한 판 묵은 자리다.**
   #13 부분 스윕(51차) · #14 거짓 귀속(52차) · #15 능력 과대서술(**44차**).
   앞 둘은 직전 판이 만든 것이었으나 이번 것은 **아크 깊숙이 서 있던 것**이 focus 확대로
   드러난 것이다.  즉 산문 층의 결함 재고는 «최근 판»에 한정되지 않는다.
2. **같은 형상 전수 확인**: 「훑지 않는다 / 배제한다 / 제외한다」로 능력을 주장하는 자리
   여섯을 전수 대조했고, 나머지는 전부 **실재하는 배제**였다(정규식 `EXTERNAL_SUFFIX_RE` ·
   함수 인자 · `--skip-sweep` 플래그).  거짓 주장은 이 하나였다 — 다만 **모집단을 세
   어휘로 잡았으므로 다른 어휘로 표현된 자리는 미확인**이다.
3. **⑤ 는 이제 여섯 회차 연속**이고 심판이 경로 둘을 시도해 막혔다.  종결의 상한 둘
   (⑤·⑧) 중 ⑤ 는 «심판 환경»이 아니라 **배터리의 디스크 의존** 쪽에서 닫는 것이 유일한
   실효 경로로 보인다.  다만 그 변경은 50·51차가 잠근 표면에 닿고 오케스트레이터가 그
   제약을 로컬에서 재현하지 못한 상태다.

---

## 운영 기록 (정직)

잡 1건 · 완주 2분 45초 · 중복 0 · 매달림 0.  포워더 우회 5회차 연속 무사고.
