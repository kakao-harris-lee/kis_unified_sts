# 레인 B 계획 «재심» — 55차 판 (재심 #17)

```yaml
adjudicator: codex
verdict: needs-attention
reviewed_at_head: 73ecd8c4
reviewed_plan_paths:
  - docs/plans/2026-08-12-tos-phase0-completion-contract-design.md
  - docs/plans/2026-08-11-tos-completion-development-plan.md
reviewed_scope_digest: aeeaa8814d29bd4e8241f13865f3f9ff6081792044076701656e742bdfb3a883
bound_set_digest: daaba47b1c4b2b31717c098c8d761d9fd2b0cd1eb7e0d55b48d46a4e059f1c3b
job_id: review-mtfrqhbl-mx7kna
job_class: review
base: d1baf9e0
scope: branch
prior_verdict: docs/reviews/phase0-completion-contract/20260830-205952/verdict.md
```

**게이트 판정: 통과 아님.**  findings **2 (medium 2)** — 둘 다 신규 material · 둘 다 저작 산문.
**S-26 ② 카운터 0 유지** (다섯 회차 연속 needs-attention).

**이 판의 성과: 이 아크에서 처음으로 심판의 «수치 주장»을 오케스트레이터가 재현했다.**

---

## Codex 판정 (verbatim — 손대지 않음)

```json
{
  "verdict": "needs-attention",
  "summary": "출하 보류. 신규 material (a) 2건이다. CAP-2의 새 배제 설명은 직전 행 재사용 경로를 누락했고, C1 전칭은 파일 상단에 그대로 남았다. 기본 검사와 --report는 rc 0(미앵커 67·닫힌 표 32·픽스처 셀 17)이지만 --self-test는 쓰기 가능한 임시 디렉터리가 없어 rc 2였으므로 145종은 독립 검증하지 못했다. 백틱 식별자·호출관계와 50·51차 회귀 세 축에서는 추가 결함을 발견하지 못했다(없음을 증명한 것은 아님).",
  "findings": [
    {
      "severity": "medium",
      "title": "CAP-2의 '이력·픽스처 행 통째 배제' 주장은 직전 행 경로에서는 거짓이다",
      "body": "새 문언은 이력 행을 통째로 건너뛰고 픽스처 행도 통째로 면제한다고 적지만, `_cap_guard_sites`는 현재 행을 배제하기 전에 직전 행을 무조건 `_normalize_scan_text`로 읽어 `prev`에 넣는다. 독립 탐침에서 정확히 `['| v2.22 | >= 1000 |', '→ PREVENTION_UNVERIFIABLE']`를 주입하면 2행이 CAP-2 자리로 검출됐고, `['| **T-84** | >= 1000 |', '→ PREVENTION_UNVERIFIABLE']`도 같은 결과였다. 같은 내용을 각 배제 행 한 줄에 넣었을 때만 검출되지 않았다. 따라서 ①·②는 '현재 후보 행 배제'일 뿐 내용 전체의 배제가 아니며, 전칭적인 셋 목록은 실제 데이터 흐름을 숨긴다.",
      "file": "tools/tos_contract_check.py",
      "line_start": 2445,
      "line_end": 2448,
      "confidence": 1.0
    },
    {
      "severity": "medium",
      "title": "C1 전칭을 지역 docstring에서만 고쳐 파일의 검사 계약은 여전히 과대서술한다",
      "body": "이번 변경은 `check_c1` docstring을 정직하게 좁혔지만, 모듈의 검사 축 요약은 여전히 C1이 '기수를 말하는 모든 문장'을 대조한다고 선언한다. 실제 구현은 `CARDINALITY_TOKEN_RE`의 아라비아 숫자와 `개|원소`만 본다. 계약 6799행의 유일한 `아래 9개`를 메모리에서 정확히 한 번 `아래 여덟 항목`으로 바꿔 `check_c1`을 실행한 결과 C1 위반은 0건이었다. 즉 같은 파일 안에서 공개된 능력 계약과 새 지역 설명이 서로 모순된다.",
      "file": "tools/tos_contract_check.py",
      "line_start": 8,
      "line_end": 10,
      "confidence": 1.0
    }
  ],
  "next_steps": [
    "CAP-2의 직전 행 데이터 흐름을 문언 또는 구현으로 일치시키고 이력·픽스처 직전 행 변이를 회귀 대조군에 추가한다.",
    "파일 상단 C1 전칭 두 자리를 지역 docstring과 동일한 실제 모집단으로 정정한다.",
    "쓰기 가능한 환경에서 --self-test를 다시 실행해 145종 전건 판별·죽은 검사 0·과잉 차단 0을 확인한다."
  ]
}
```

원문 전체는 `.omc/review/20260830-211025/codex-raw.json`.

---

## 수용검사 (오케스트레이터 = Claude)

**채택 2 · 기각 0 · 팬텀 0.**

### 결속 대조

`plan_scope_digest` 포착 == 재계산 `aeeaa881…` · `bound_set_digest` == `daaba47b…`
(결속 문서 무변경 → **O-6 재결속 불요**) · 계약 blob `ecbd478e…` 불변 ·
S-26 ①ⓑ 이력 술어 **공집합** — ⑥ 미발화.

### finding 1 — 심판 탐침을 «적힌 그대로» 재현했다

이번 focus 는 「수치 주장은 재현 가능한 형태로 적어라 — 정확히 무엇을 어디에 주입했는지」를
요구했고, 심판이 그것을 지켰다.  오케스트레이터가 그 주입을 그대로 돌린 결과:

| 주입 | CAP 사이트 |
|---|---|
| (무편집 기준) | **4** |
| `['\| v2.22 \| >= 1000 \|', '→ PREVENTION_UNVERIFIABLE']` (이력 행 + 다음 행) | **5** |
| `['\| **T-84** \| >= 1000 \|', '→ PREVENTION_UNVERIFIABLE']` (픽스처 행 + 다음 행) | **5** |
| 같은 내용을 배제 행 **한 줄**에 몰기 (대조) | **4** |

**직전 두 회차(#15·#16)에서 심판 수치를 재현하지 못해 승인하지 않았는데, 이번엔 재현됐다.**
차이는 심판의 능력이 아니라 **focus 가 재현 가능성을 요구했다는 것**이다.

구조 근거: `:2474` `prev = _normalize_scan_text(doc.lines[lineno - 2]) if lineno >= 2 else ""`
— **무조건**이며 이력·픽스처 판별이 없다.  그러므로 55차의 「이력 행을 통째로 건너뛴다 ·
픽스처 행도 통째로 면제」는 **후보 행 배제**일 뿐이고, 배제된 행의 «내용»은 다음 행을 훑을 때
`prev` 로 다시 들어온다.

### finding 2 — 부분 스윕, 네 번째

55차는 `check_c1` docstring 만 좁히고 **모듈 상단 축 요약(`:10`)은 그대로 두었다.**
같은 파일이 같은 축에 대해 두 가지를 말한다.

**오케스트레이터 전수 조사에서 심판이 짚지 «않은» 세 번째 자리를 찾았다**: `:53-54`
「(4) 를 참조하며 수량을 말하는 **모든 문장**을 정규식으로 수집」 — 같은 전칭이다.
심판은 `:8-10` 만 냈다.  **셋을 한 판에 고친다** — 둘만 고치면 이번 판이 다시 부분 스윕이 된다.

### 기각 사유 대조 (두 건 공통)

`file:line` 실재 **확인** · 의도적 silenced **아니다** · 비협상 규칙 배치 **없음**(31판 연속) ·
범위 밖 부채 **아니다**(둘 다 55차가 이 판에서 쓴 문장, 또는 그 문장이 고쳤어야 할 짝).

### S-26 축별 상태

| 축 | 상태 |
|---|---|
| ① 동결 (이력 술어) | 충족 |
| ② 2회 연속 material 0 | **0** — 다섯 회차 연속 material |
| ③ validator rc 0 | 충족 (`--report` 도 rc 0) |
| ④ CUR/CIT/VER/CARD/RULE 0 | 충족 |
| ⑤ 배터리 독립 검증 | **미검증 — 여덟 회차 연속** |
| ⑥ 리셋 | 미발화 |
| ⑧ 도달 가능성 | 열림 (등재) |

### S-26 ② 카운터

**0.**  궤적: … → 1 → 1 → 2 → **2**.

---

## 오케스트레이터 관측 (판정 아님)

1. **처분 방향은 «문언을 낮춘다»로 고정한다.**  심판은 finding 1 에 「문언 또는 구현으로
   일치」를 제시했다.  구현 쪽(선행 창까지 배제)을 택하면 46차가 세운 「셀 경계는 «면제의
   단위»이지 «술어의 단위»가 아니다」가 깨지고 모집단이 줄어든다 — 44차의 펜스 배제가
   4 → 1 로 줄여 «죽은 검사 2건»을 만든 것과 **같은 극성**이다.  그래서 56차는 문언만
   낮추고 구현을 건드리지 않는다(실측으로 확인: 처분 후에도 탐침이 4 → 5 그대로).
2. **다섯 회차 연속 material 이 저작 산문이고, 네 판 연속 «수리»가 새 material 을 낳았다.**
   #13(51차) · #14(52차) · #16(54차) · #17(55차).  #15 만 아크 깊숙이 묵은 자리였다.
   **수리가 재고를 늘리는 구조**가 이 아크의 현재 지배 패턴이다.
3. **근거 층 검사는 이번엔 통과했다.**  #16 이 팬텀 식별자를 냈으므로 이번 focus 가 백틱
   식별자·호출 관계를 1순위로 세웠고, 심판이 그 축에서 「추가 결함을 발견하지 못했다
   (없음을 증명한 것은 아님)」로 적었다.  55차가 커밋 전에 인용 식별자 여덟을 전수
   재점검한 것이 실효를 낸 것으로 보이나 **대조 실험이 없으므로 인과가 아니라 관측**이다.

---

## 운영 기록 (정직)

잡 1건 · 완주 2분 50초 · 중복 0 · 매달림 0.  포워더 우회 7회차 연속 무사고.

**진행 순서에 대한 판단 기록**: 이 회차 도중 「재심 #18 진행」 지시가 있었으나 #17 이 아직
running 이었다(로그 나이 3초 · elapsed 2분 27초).  #17 판정을 모르는 채 #18 을 겹치면
② 의 «연속» 계수에 쓸 수 없는 표본이 되고(수리 전 내용을 심사한 것이 된다) 결속이 흐려지므로
**#18 을 띄우지 않고 #17 완주를 기다렸다.**  그 사유를 그때 보고했다.
