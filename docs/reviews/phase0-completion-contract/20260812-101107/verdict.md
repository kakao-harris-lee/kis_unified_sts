# verdict — 레인 B (계획 심판) · v1.3.13 · **첫 완주**

## 심판 메타

```yaml
adjudicator: codex
verdict: needs-attention
gate_status: NOT_PASSED            # 3회 연속 — P-0 및 D0 착수 계속 차단
reviewed_at_head: 2b7b2a209aefb9bd7186949f405f6418fd4902cd
reviewed_plan_paths:
  - docs/plans/2026-08-12-tos-phase0-completion-contract-design.md
  - docs/plans/2026-08-11-tos-completion-development-plan.md
reviewed_scope_digest: 95a2ac552b18dc281fd36f3d0ac985f584212aa470ecf74ba8fb553f6cf303db
reviewed_version: v1.3.13 (1675행)
findings: 6                        # high 5 / medium 1
```

실행: 모드 B (`task --background --fresh --effort high`, `write: false` 실측),
job `task-mspebjgn-ws79qi`, 8m 24s, desync 0, **verdict 방출**.

## 동결 검증 — 이번 실행이 이전 4회와 다른 유일한 조건

| 시점 | `plan_scope_digest` | |
|---|---|---|
| 디스패치 직전 | `95a2ac55…6f303db` | 기준 |
| result 회수 직후 | `95a2ac55…6f303db` | **일치** |

추가 독립 확증: Codex가 **자체 보고한** 주 문서 SHA-256
`42f065912ac9e3d6243d48434238c5c2bd00f4e14e50860cb84b18e9e2cf8e72`가 재계산값과
일치. 심판이 실제로 v1.3.13을 읽었다는 증거다.

**v1.3 대상 심판 4회 연속 실패의 원인이 in-flight 편집이었음이 대조군으로 확증됐다** —
같은 도구·같은 모드에서 문서만 정지시키자 완주했다.

## 수용검사 — 6건 전건 채택, 기각 0

기각 사유 3가지 중 해당 없음. Codex가 P5 기준 6(`CLAUDE.md` 비협상 규칙)을
**해당 없음**으로 명시 판정했고, **실선물 주문 영구 차단 보존**을 확인했다.

### 직접 실측으로 확증

**finding #5 — 문서가 자기가 금지한 등치를 사용한다**

```
:1007  §6.3.3  "`non-null == approved`로 취급하면 그것은 자기신고를
                구조 파생으로 위장하는 것이다"
:1148  §7.4    "BOUND — 의존 키 K를 특정했고, K ∈ non-null(147) → 문언을 현행화"
```

같은 문서가 :1007에서 금지하고 :1148에서 사용한다. `non-null(147)`이라는 표기 자체가
non-null에 **승인 카운트 라벨**을 붙여 등치를 명시화한다. **가장 명백한 자기모순.**

**finding #3 — 자기선언 `required_kinds`가 쌍 게이트를 무력화한다**

K-2가 파생을 포기하고 선언으로 바꾼 결과(v1.2→v1.3의 F-4 대응), 모든 evidence가
`{PACKAGE}` 하나만 선언하면 K-7·K-8을 충족하고 **생략된 kind의 쌍은 P에 생성되지
않는다.** FWD-a/FWD-b는 이미 만들어진 쌍만 검사하므로 이 누락을 볼 수 없다.
`binding_basis`는 §의 실재만 검사하지 선언의 의미 적합성을 증명하지 않는다.

→ **F-4를 고치면서 만든 새 구멍.** 파생 불가를 선언으로 대체한 트레이드오프의 대가.

## P1 — v1.2 재심 8건의 처리 판정

| # | 판정 |
|---|---|
| F-7 (T-3c 공집합) | **해소** |
| F-8 (순서·P-0 범위) | **해소** |
| F-9 (CI·rollback) | **해소** |
| 신규high (G1~G3 11조건) | 부분해소 — 11행 열거는 됐으나 일부 CHECKABLE 분류가 상위 조건을 증명하지 않음 |
| F-4 (K-2/K-3) | 부분해소 — 1:1 테이블은 실행 가능하나 의미적 타당성이 자기선언 |
| F-2 (30/30) | **회피** — OD-1로 조건을 삭제·완화. 운영자 결정 표기는 있으나 원래 조건 충족은 아님 |
| F-5 (approved) | **문구만 바뀌고 실질은 그대로** — 술어 없이 UNCHK-001로 이동해 종료조건에서 제외 |
| F-6 (UNDECIDED) | **문구만 바뀌고 실질은 그대로** — 7곳 전부 UNDECIDED여도 완료 가능, U-7 판정 함수 부재 |

## P2 — §13은 방어 기제인가 정당화 장치인가

**판정: 정당화 장치에 가깝다.** U-3~U-5는 가시성 방어를 제공하지만 **핵심 완전성 규칙
U-2가 실행 불가능**하다.

```
U-2  입력 우주 = 정의된 전수 목록 없음
     판정 주체 = 없음
     판정 시점 = 없음
     → 미등재 축을 기계가 발견할 방법이 없다 = 자기신고
반면 U-1 은 등재된 축을 완료조건에서 제외한다
     → 레지스터가 누락 방어보다 제외 정당화에 쓰일 수 있다
```

**상위 Phase 0 네 종료조건의 잔존**: 30/30 소실 / phantom 0은 81행 자기선언 쌍에만
차단, 409행은 비차단 지표 / 완료·권한 분리는 **온전히 보존** / current-status 정합은
163·16과 진입점만 남고 147 approved 의미는 제외.

## P4 — 저작자 자기신고 5건 전부 **일치**

Codex가 직접 재실측(파서·AST census·파일시스템·`--check` 실행): 코퍼스 `29/30` 3곳 /
Part-1 reviewer 372/372 고유값 1 / 별칭(대소문자·`..`·`./`) 통과·symlink 0 /
docstring 7곳(AST 기반) / `tos_evidence_run.py:1574` 인용. **전건 일치.**

## 심판 자기신고 한계

> "제안된 생성기·CSV·T-1~T-45는 아직 존재하지 않아 실행할 수 없었고, 전체 8,742
> pytest는 실행하지 않았다."

T-계열 판정은 **정적 대조 근거**다. 단 P4 5건은 직접 재실측했다 — 이전 회차보다
증거 기반이 강하다.

## 운영자 결정과의 관계 (오케스트레이터 판단 — 심판 소관 아님)

**finding #2는 OD-1·OD-2를 재쟁점화한다.** "상위 종료조건이 실제로 약화됐다"는
판정은 정확하지만, **그 약화는 운영자가 명시적으로 결정한 것**이다(§0).

- OD-1: 30/30을 거버넌스 트랙으로 이관 — 약화가 아니라 **소관 이전**이 의도
- OD-2: 기계 검사 가능 축으로 축소 + 불가 축을 §13에 노출 — **축소가 의도**

`codex-gate` 규정상 Codex verdict가 비협상 규칙을 뒤집을 수 없듯, **운영자 스코프
결정도 심판이 뒤집지 않는다.** 다만 finding #2가 지적한 **"약화의 실제 크기"**는
기각 사유가 아니라 **기록 대상**이다 — 운영자가 결정 시 그 크기를 알고 있었는지는
별개 문제이고, 이 판정이 그것을 처음으로 정량화했다.

→ 처분: finding #2는 **채택하되 "수정"이 아니라 "OD-1·OD-2의 알려진 대가"로 §0에
명시 기록**한다. 운영자에게 재확인을 올릴 가치가 있다.

## 게이트 판정

```
통과 = adjudicator: codex AND verdict: approve AND digest 일치
현재 = codex           AND needs-attention  AND 일치
                           ^^^^^^^^^^^^^^^ 3회 연속 불성립
```

**P-0 및 D0 구현 착수 계속 차단.**

## next_steps (Codex 원문)

1. 현재 verdict로 P-0 및 D0 구현 착수를 계속 차단한다.
2. 위 차단 finding이 해소·회피·잔존 중 무엇인지 **동일한 digest 결속 재심**에서
   다시 판정하기 전에는 게이트를 통과시키지 않는다.

## 이 세션의 구조적 성과

판정 자체는 `needs-attention`이지만, **심판 레인이 작동한다는 것이 처음으로 증명됐다.**
동결 → 완주 → verdict 방출 → digest 일치 확인의 전 경로가 한 번에 성립했고,
그 조건이 "문서를 정지시킨다" 하나였음이 4회 실패와의 대조로 드러났다.
