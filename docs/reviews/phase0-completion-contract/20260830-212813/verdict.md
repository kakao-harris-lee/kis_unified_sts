# 레인 B 계획 «재심» — 56차 판 (재심 #18)

```yaml
adjudicator: codex
verdict: needs-attention
reviewed_at_head: 128ce02d
reviewed_plan_paths:
  - docs/plans/2026-08-12-tos-phase0-completion-contract-design.md
  - docs/plans/2026-08-11-tos-completion-development-plan.md
reviewed_scope_digest: 8ff00699fcf488076f3f1520e76cfb0397e4139c1b46b3c5e94e73ec30e121bf
bound_set_digest: daaba47b1c4b2b31717c098c8d761d9fd2b0cd1eb7e0d55b48d46a4e059f1c3b
job_id: review-mtfsdeok-9q3mwg
job_class: review
base: 0d5d9e31
scope: branch
prior_verdict: docs/reviews/phase0-completion-contract/20260830-211025/verdict.md
```

**게이트 판정: 통과 아님.**  findings **3 (medium 3)** — 전부 신규 material · 전부 저작 산문.
**S-26 ② 카운터 0 유지** (여섯 회차 연속 needs-attention).

**궤적이 두 회차 연속 늘었다: 1 → 1 → 2 → 2 → **3**.**

---

## Codex 판정 (verbatim — 손대지 않음)

```json
{
  "verdict": "needs-attention",
  "summary": "출하 보류. 신규 material (a) 3건이다. C1 모집단의 네 번째 과대서술이 남았고, CAP-2의 새 선행-창 설명도 창 길이·정규화 한계를 누락했으며, 같은 docstring의 픽스처 정체 설명은 실제 완전일치 술어와 반대다. 기본 검사 rc 0·경고 0, ruff clean, CAP 모집단 4→5 및 펜스 제외 4→1, 미앵커 67·닫힌 표 32·픽스처 셀 17은 재현했다. --self-test는 /private/tmp까지 쓰기 불가라 rc 2였으므로 정적으로 확인한 145종의 전건 판별·죽은 검사 0·과잉 차단 0은 독립 검증하지 못했다. 백틱 식별자와 50·51차 회귀 축에서는 추가 결함을 발견하지 못했다(없음을 증명한 것은 아님).",
  "findings": [
    {
      "severity": "medium",
      "title": "C1의 네 번째 모집단 설명이 여전히 실제 토큰 술어보다 넓다",
      "body": "`iter_cardinality_statements`는 아직 \"(4)를 참조하며 수량을 말하는 자리\"를 훑는다고 선언하지만, 실제로는 160자 창에서 `CARDINALITY_TOKEN_RE`의 아라비아 숫자와 `개|원소`만 찾는다. 계약 6799행의 유일한 `아래 9개`를 정확히 한 번 `아래 여덟 항목`으로 바꿔 `check_c1`을 실행한 결과 위반 목록은 `[]`였다. 따라서 56차가 맞췄다는 세 자리 밖에 같은 C1 능력 과대서술이 하나 더 남아 있다.",
      "file": "tools/tos_contract_check.py",
      "line_start": 986,
      "line_end": 997,
      "confidence": 1.0
    },
    {
      "severity": "medium",
      "title": "\"바로 다음 행이면 검출된다\"는 새 CAP-2 설명이 선행 창의 절단·정규화 경계를 누락한다",
      "body": "`prev`를 읽어도 실제 판정에는 `(prev + cur-prefix)[-GUARD_WINDOW:]`만 남고 HTML 주석도 먼저 제거된다. 계약 10108행 뒤 10109행에 `| v2.22 | >= 1000 ` 뒤로 `x` 300개와 ` |`를 붙이고, 10110행에 `→ PREVENTION_UNVERIFIABLE`를 주입하자 모집단은 4로 불변이었다. `| **T-84** |`로 바꾼 동일 탐침도 4였다. 반면 300자 채움이 없는 두 짧은 탐침은 각각 4→5였다. 비교식이 배제 행에 있고 결과 토큰이 바로 다음 행에 있다는 새 문언의 조건만으로는 검출을 보장하지 않는다.",
      "file": "tools/tos_contract_check.py",
      "line_start": 2455,
      "line_end": 2459,
      "confidence": 1.0
    },
    {
      "severity": "medium",
      "title": "같은 CAP-2 docstring이 픽스처 면제를 접두 일치로 잘못 설명한다",
      "body": "문언은 manifest 앵커로 \"시작하는 행\"을 뺀다고 하지만 `_fixture_row_id`는 정규화한 첫 셀의 완전일치를 요구한다. 계약 10108행 뒤 10109행에 `| **T-84** | >= 1000 → PREVENTION_UNVERIFIABLE |`를 주입하면 모집단은 4였지만, 정확히 `| **T-84**-NORM | >= 1000 → PREVENTION_UNVERIFIABLE |`로 바꾸면 5가 됐다. 이는 45차가 폐기한 접두 면제를 현재 설명이 다시 주장하는 형태다.",
      "file": "tools/tos_contract_check.py",
      "line_start": 2447,
      "line_end": 2449,
      "confidence": 1.0
    }
  ],
  "next_steps": [
    "C1 네 번째 docstring과 CAP-2의 창·픽스처 정체 설명을 실제 술어에 맞춰 함께 정정한다.",
    "쓰기 가능한 환경에서 `--self-test`를 재실행해 145종 전건 판별·죽은 검사 0·과잉 차단 0을 확인한다.",
    "선행 창의 짧은 행/창 밖/HTML 주석 변형과 픽스처 완전일치/접두 변형을 회귀 대조군으로 고정한다."
  ]
}
```

원문 전체는 `.omc/review/20260830-212813/codex-raw.json`.

---

## 수용검사 (오케스트레이터 = Claude)

**채택 3 · 기각 0 · 팬텀 0.**

### 결속 대조

`plan_scope_digest` 포착 == 재계산 `8ff00699…` · `bound_set_digest` == `daaba47b…`
(결속 문서 무변경 → **O-6 재결속 불요**) · 계약 blob `ecbd478e…` 불변 ·
S-26 ①ⓑ 이력 술어 **공집합** — ⑥ 미발화.

### 세 탐침 전부 «적힌 그대로» 재현했다

두 회차 연속으로 focus 가 「무엇을 어디에 주입했는지 적어라」를 요구했고 심판이 지켰다.

| 주입 | CAP 사이트 | 판정 |
|---|---|---|
| (무편집 기준) | **4** | — |
| 짧은 두 줄 (배제 행 + 다음 행) | **5** | #17 재확인 |
| 비교식 뒤 `x`×300 채움 + 다음 행 | **4 불변** | **finding 2 확정** |
| 픽스처 앵커 **완전일치** 한 줄 | **4** (면제됨) | — |
| 픽스처 앵커 **접두만**(`-NORM`) 한 줄 | **5** (면제 안 됨) | **finding 3 확정** |

### finding 1 — 내 「세 자리 일치」가 거짓이었다

`iter_cardinality_statements`(`:986`) docstring 이 「(4) 를 참조하며 **수량을 말하는 자리**」로
남아 있다.  56차가 고친 셋(`:10` 축 요약 · `:53-54` 모집단 서술 · `check_c1` docstring) 밖의
**네 번째 자리**다.  이번 focus 가 「네 번째 자리를 찾아라」를 1순위로 명시했고 심판이 찾았다.

**계수 전칭의 계보**: 54차 「면제는 픽스처 행 **하나뿐**」 → 55차 「배제는 **셋**」 →
56차 「C1 **세 자리**가 이제 일치」.  **세 판 연속 계수 전칭을 썼고 세 번 다 틀렸다.**

### finding 3 — 호출자 산문이 피호출자 docstring 과 정면 모순

`_cap_guard_sites` 는 「앵커로 **시작하는** 행만 뺀다」고 적는데, 그것이 부르는
`_fixture_row_id` 의 docstring 은 「**접두 일치가 아니라 첫 셀 «완전일치»**」이며
45차 재심 #5 F1 이 정확히 그 접두 형상을 폐기한 기록까지 담고 있다.

### 귀속 — 둘은 묵은 자리, 하나는 내 것

| finding | 도입 | 판단 |
|---|---|---|
| 1 (C1 네 번째) | `60c76134` | **묵은 자리** — 다만 56차가 「세 자리 일치」라 «주장»한 것이 거짓이므로 이 판의 material |
| 2 (선행 창 경계) | **56차(`128ce02d`)** | 내가 이 판에서 쓴 문장 |
| 3 (접두 서술) | `df72ae5b`(44차) | **묵은 자리** — 45차가 구현을 고치면서 호출자 산문을 남겼고, 내가 54·56차에 그 docstring 을 두 번 다시 쓰면서도 남겼다 |

셋 다 **범위 밖 부채로 분리하지 않는다** — 이번 focus 가 「전칭을 깨라 · 능력 서술을 의미로
훑어라」를 명시 요청했고, 범위를 넓혀 놓고 결과를 범위 밖이라 부르는 것은 피심판자가 심사
범위를 스스로 정하는 것이다.

### 기각 사유 대조 (세 건 공통)

`file:line` 실재 **확인** · 의도적 silenced **아니다**(같은 파일·같은 호출 관계가 정반대를
구현·서술한다) · 비협상 규칙 배치 **없음**(32판 연속).

### S-26 축별 상태

| 축 | 상태 |
|---|---|
| ① 동결 (이력 술어) | 충족 |
| ② 2회 연속 material 0 | **0** — 여섯 회차 연속 material |
| ③ validator rc 0 | 충족 (경고 0 · ruff clean 도 심판이 재현) |
| ④ CUR/CIT/VER/CARD/RULE 0 | 충족 |
| ⑤ 배터리 독립 검증 | **미검증 — 아홉 회차 연속** (`/private/tmp` 까지 쓰기 불가) |
| ⑥ 리셋 | 미발화 |
| ⑧ 도달 가능성 | 열림 (등재) |

---

## 오케스트레이터 관측 (판정 아님)

1. **산문 층은 수렴하지 않고 있다.**  궤적 1 → 1 → 2 → 2 → **3**.  다섯 판 연속(52·54·55·56차)
   수리가 새 material 을 낳았고, 이번엔 **한 docstring 안에서 두 건**이 났다.
   같은 자리를 세 번(54·55·56차) 다시 썼는데 매번 새 오류가 나왔다.
2. **원인 가설(검증 안 됨)**: 이 자리들은 «구현의 요약»인데, 요약은 필연적으로 전칭을
   부른다(모든·셋·하나뿐·통째).  전칭은 반례 하나로 깨지고, 나는 매번 **읽은 범위 안에서만**
   전칭을 세운다.  **구조적 처방은 요약을 «금지»하거나 요약이 인용하는 술어를 기계로
   대조하는 것**이지, 요약을 더 정확히 쓰는 것이 아닐 수 있다.  다음 판의 후보로 등재한다.
3. **⑤ 는 아홉 회차 연속.**  심판이 이번엔 `/private/tmp` 까지 시도해 막혔다.

---

## 운영 기록 (정직)

잡 1건 · 완주 4분 14초 · 중복 0 · 매달림 0.  포워더 우회 8회차 연속 무사고.
