# verdict — 레인 B (계획 심판) · v2.3 · **11회 연속 완주**

```yaml
adjudicator: codex
verdict: needs-attention
gate_status: NOT_PASSED            # 13회 연속
reviewed_at_head: 2b7b2a209aefb9bd7186949f405f6418fd4902cd
reviewed_plan_paths:
  - docs/plans/2026-08-12-tos-phase0-completion-contract-design.md
  - docs/plans/2026-08-11-tos-completion-development-plan.md
reviewed_scope_digest: e1691b7767c26f5d9b6d9c02dc1db14d3fc15cfdce8799cd4b0e51785874815b
primary_doc_sha256: 1d4c244b690bfc36e1e2c95a990eb7447f2825b2ce38a8f8bd9d61c1fb59a8d0
reviewed_version: v2.3 (3559행)
findings: 5                        # critical 0 / high 5  (+ 심판 레인 독립 2건)
prior_verdict: .omc/review/20260812-223028/verdict.md
mode: A (adversarial-review, --scope working-tree), job review-msq6berw-fmrf19, 12m27s, write=false
```

동결 4종 지표 시작=종료 일치(11회 연속). 심사 중 개정 0.
뮤테이션은 전부 scratchpad 사본에서 수행 — 작업트리 tracked diff 는 `uv.lock` 뿐.

> **스트리밍 트랩 2회차 재현**: `progressPreview` 에 `{"verdict":"approve"}` 가 **2회**
> 나타난 뒤 최종은 `needs-attention`. **중간 preview 를 판정으로 읽으면 게이트가 무너진다.**

## 처분

**v2.2 직전 7건**: **해소 2**(F2·F6) / 부분해소 4(F1·F3·F4·F5) / **회피(범위축소) 1**(F7).

## 수용검사 — 7건 채택(Codex 5 + 심판 레인 독립 2), 기각 0

### F1 (high) — SELF-1 이 `limit()` 로 우회된다. **end-to-end 실증**

```
DEFECT_WORDS = 7개 닫힌 목록 (결함·위반·반증·충돌·우회·미검출·통과시킨)
:126-129  loose_limits() 는 `ref not in known` 만 본다 — **Case 의 red 여부를 안 본다**
:117-121  defect() 경로는 green_bound_defects() 로 red 를 검사한다  ← **비대칭**
```

우회 3종 전부 성립: 어휘 밖 서술(`잡지 못한다`·`조용히 잘못된 값을 산출한다`) ·
**green Case 참조**(`결함이 생존한다 [Case:T-75]`) · 동의어 치환.
**실제 러너에 미수정 결함 2건을 `limit()` 로 주입 → exit 0 · 28/28 · SELF-1 GREEN.**

**⇒ 문서 `:204` 의 "노트로 빠져나가는 통로 자체를 기계로 막았다"는 반증됐다.**
진전은 실재한다(승격 11건·exit 1 실증) — 그러나 **폐쇄가 아니라 축소다.**

### F2 (해소) — T-76 앵커는 정본

`EVIDENCE-REGISTER-002.csv` + `-DEV.csv` 독립 재측정: **26종·합계 490 전 항목 일치.**
레지스트리 등록 실재(`enforcement.py:169`, 키 12).

> **다만 실효 앵커는 3행 픽스처다** — OD-3-A 가 실 CSV 열람을 금지하므로 프로토타입은
> 구조적으로 이 표를 읽을 수 없다. §5.2.8.0 의 "**이 표가 T-76 의 앵커다**"는
> **현재 시제로 참이 아니며**, 한계 문단이 그 사실을 고지하지 않는다.

### F3 (high) — OD-3-A 의 AST 경계가 **런타임 문자열 조립으로 우회**된다

```
prefix='tos-'; p=prefix+'spec/'      → 리터럴·AST 스캔 둘 다 [] (통과)
p=''.join(('tos-','spec/'))          → 통과
p='tos-'+chr(115)+'pec/'             → 통과
```

상수 `+` 폴딩과 상수 f-string 만 접는다. **프로토타입이 실 코퍼스에 접근하면서
T-77 이 OD-3-A 를 clean 으로 보고할 수 있다.** D·E 는 이미 미검사이므로
**A·B·C 가 기계 경계의 전부인데 그 중 A 가 뚫린다.**

부수: 사본을 **repo 밖에 두고 실행해도 28/28 GREEN** — 경계 검사가 자기상대 경로
기준이라 위치 이동을 감지하지 못한다.

### F4 (high) — `config/tos_completion.yaml` 이 **실제 유일 소스가 아니다**

문서가 T-71 분포와 T-76 26종을 **그대로 보유**(`:748`, `:1219-1237`)하는데 미래 YAML 과의
**파생·정합 검사가 없다.** rollback 도 모순: `:2850` 은 설정만 되돌리면 안전하다 하고
`:2858` D0-A 표면 목록에는 **config 가 빠져 있다.**

### F5 (high) — 6e `DEFERRED` 분기가 전역 미해결을 한 행으로 축소하고, **5번째 지표에 소비처가 없다**

OQ-11 은 **전역 레벨→kind 매핑**을 지배하는데 `DEFERRED_WITH_SCOPE` 는 `STATE-EV-004`
한 행만 제외한다 — **나머지 행의 floor 는 여전히 미확정.** 그리고 U-10 은 `:2727` 에서
**정확히 4지표**를 정의하는데 5번째에 이름·스키마·검사기·레지스트리·대조군·소비처가 전부 없다.

**심판 레인 독립 발견**: `무응답` 분기에 **진입 조건이 없다.** 문서 전체에 기한·마감·
타임아웃 규정 **0건**(오케스트레이터 재검산: naive grep 6건은 전부 `폐기**한**` 의
부분문자열 오탐). `REFUSED` 는 명시 행위라 진입 가능하나 **무응답은 "아직 대기 중"과
기계적으로 구별 불가** — F5 가 겨냥한 영구 대기가 침묵 쪽에서 그대로 열려 있다.

### F6 (해소) — 반증된 "상수" 주장 전수 갱신 확인

`:720`·`:2326`·`:2556`·`:2739`·`:3366` 전부 "입력 뮤테이션 불변 / 규칙 뮤테이션 가변"으로 갱신.

### F7 (회피 — 범위축소) — U-9/U-9a 철회가 **보호하던 속성까지 없앴다**

`closable=YES`→`NO` + owner 비움 + 무관한 실재 절 인용 + 앵커 동시 갱신 →
**U-1a·U-9·U-9a·집계 findings 전부 0**(심판 독립 재현). U-1a 는 `YES` 인 행에만
적용되고(`:2747`), U-8/U-8a 는 규범 참조가 남아야 발화한다.
**의무가 자기 소유자와 게이트 기여를 스스로 제거하는 경로가 열려 있다.**

### ⚠ 심판 레인 독립 발견 — F7 철회가 **§11 내부에서 미전파** (high, 오케스트레이터 확증)

```
:2743  ~~U-9·U-9a~~  **v2.3 철회** — "종료조건에서 빠진다"
:2726  **`closable=NO` 인 모든 행이 `reason` 을 보유** (U-9, T-63)   ← 취소선 없음. 활성
```

**같은 §11 안에서 강한 형식은 철회하고 약한 형식은 남겼다.** 더 나쁜 것은 남은 쪽이
**v1.9 형식**이고, 문서 자신이 `:2387` 에서 **"v1.9까지 `reason` 이 비었는지만 봐서
아무 산문이나 통과했다"고 결함으로 규정한 바로 그 형식**이라는 점이다.

**⇒ 기지(旣知) 결함 조건이 종료조건으로 살아남았다.** 심판 인용(F7)만 보고 고쳤으면
놓쳤을 위치이며, **선언층/평가층 간극 12판 연속**이다.

## 비협상 규칙 — 위반 0 (재발 없음)

8조항 `file:line` 개별 대조. v2.1 하드코딩 위반은 v2.2 해소 후 **v2.3 재발 없음**
(임계값 전부 `proto/config.yaml`).

> **NIT(비물질)**: `:2783` 이 config-driven 규칙을 `CLAUDE.md:20-22` 로 인용하나
> 실제는 `:21-23`(20행은 공백). 1행 off — 인용 정확성을 스스로 규율하는 문서로서 흠결.

## 관통 패턴 — 12번째

| 층 | 형태 |
|---|---|
| v1.3.8~v2.1 | 문서 안 선언↔평가 간극 (10회) |
| v2.2 | **증거 도구**로 전이 (러너가 발견을 exit 에서 분리) |
| **v2.3** | **증거 도구의 교정에도 같은 간극** — `note` 를 막자 `limit` 이 같은 기능을 한다. 그리고 F7 철회가 §11 안에서 미전파 |

**막은 옆에 인접 우회로가 남는다**(§8.2 가 5회 반복으로 정식화한 그 패턴)가
**증거 층에서 재현됐다.**

## 게이트

```
통과 = codex AND approve AND digest 일치
현재 = codex AND needs-attention AND 일치     → 불성립 (13회 연속)
```

**P-0 및 모든 D0 구현 착수 차단 유지.**

## next_steps (Codex 원문)

1. Lane B 와 Phase 0 를 NOT_PASSED 로 유지한다 — green 28-case baseline 은 독립 뮤테이션을 견디지 못한다.
2. SELF-1 과 OD-3-A 가 제시된 우회 문자열을 red 로 만든 뒤에만 재심한다.
3. 모든 6e 처분과 5번째 U-10 지표를 실행 가능한 검사기·종료 소비처에 결속한다.
4. config 정본/rollback 모순과 U-9 전이 속성을, **범위 철회를 증명으로 세지 않고** 해소한다.
