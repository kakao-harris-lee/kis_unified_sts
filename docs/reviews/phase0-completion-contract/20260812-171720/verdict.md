# verdict — 레인 B (계획 심판) · v2.0 · **8회 연속 완주**

```yaml
adjudicator: codex
verdict: needs-attention
gate_status: NOT_PASSED            # 10회 연속
reviewed_at_head: 2b7b2a209aefb9bd7186949f405f6418fd4902cd
reviewed_plan_paths:
  - docs/plans/2026-08-12-tos-phase0-completion-contract-design.md
  - docs/plans/2026-08-11-tos-completion-development-plan.md
reviewed_scope_digest: f4bc7c8ca6f0c2e2217d5bb32829e163a7bfdc39393da90e05f3fa921214a6d7
primary_doc_sha256: e658313e813e4823f6d4976f5323fc42c58e061cc668063cf9b2ac76f0638b62
reviewed_version: v2.0 (3067행)
findings: 4                        # critical 1 / high 3
prior_verdict: .omc/review/20260812-153121/verdict.md
mode: A (adversarial-review, --scope working-tree), job review-msptjbya-vfk9am, 27m07s, write=false
```

동결 4종 지표 시작=종료 일치(8회 연속). 심사 중 개정 0.
Codex 실측 범위: `0 staged, 1 unstaged, 3 untracked`. `parseError: null`, `status: 0`.

**절차 관측(2회 연속 재발)**: job status 의 `updatedAt` 이 최초 갱신 후 고정된 채
`elapsed` 만 증가한다. **이 필드는 생존 판정에 쓸 수 없다** — PID `etime` + `progressPreview`
병행 확인이 필요하다. 이번 job 은 정상 완료(PID 소멸 + `status=completed`).

## 처분

**v1.9 직전 6건**: 해소 2 (F3·F4) / **부분해소 4** (F1·F2·F5·F6) / 미해소 0 · 회피 0.
findings 추이: v1.8 **11** → v1.9 **6** → v2.0 **4**. 다만 **critical 재출현**.

## 수용검사 — 4건 전건 채택, 기각 0

인용 실측: `file:line` 전건 존재·내용 일치. **팬텀 0건.**
비협상 8조항 `file:line` 개별 대조 → **위반 0 (3회 연속)**. Codex 가 상위 계획·CLAUDE.md
양쪽 행번호를 교차 인용했다(예: (b) `CLAUDE.md:36,96` + 상위 계획 `:39-40,233-237` + 대상 `:2317`).

기각 사유 3종(팬텀 / 의도적 silenced / 비협상 배치) 어느 것에도 해당 없음.

### ⚠ CRITICAL — `U-11` 이 모든 게이트의 완료를 **금지**한다 (저작자 용어 오류)

```
:637-638  `contributions` 는 술어 id → {MET, NOT_MET} 의 **전사(全射)** 사상이며,
          11개 술어 전부에 항이 있어야 한다
:640      verdict 는 contributions 의 **논리곱**으로 파생된다
```

**전사(surjective) = 공역의 모든 원소가 상에 포함된다** ⇒ 유효 벡터마다 `NOT_MET` 이
**최소 하나** 있어야 한다 ⇒ 논리곱은 **영구 `NOT_MET`** ⇒ **정상적인 all-MET 완료
상태가 계약 위반이 된다.**

저작자가 의도한 것은 **전역(total) 사상** — "11개 술어 전부에 항이 있다"(같은 줄 뒷부분).
**한 단어가 반대 뜻이었고, 그 결과 F1을 고치려 만든 계약이 상수성을 해결하는 대신
영구화했다.**

부수 2건(같은 finding):
- **술어 id 가 정의되지 않았다** — `contributions` 의 키 우주에 유일·전수 소스가 없다
- **T-2 는 여전히 공허하다** — 정상값이 이미 `NOT_MET` 인 10개 술어에서는 "입력 삭제 →
  `NOT_MET`" 과 "입력 무시 → `NOT_MET`" 이 구별되지 않는다. **S-18 이 요구하는
  "두 값을 갖는 입력"이 그 10개에는 없다.** 구별 가능한 전이는 `MET` 예상인 G2
  vocabulary 술어 하나뿐이다

### F-B (high) — 범위형이 미배정 차단을 **무료 placeholder** 로 만든다

```
:3021  범위  `Phase <n>-<m>`  (0 ≤ n < m ≤ 7)
       ⇒ `Phase 0-7` 이 유효.  "0~7 어딘가"는 배정이 아니다
```

책임 phase 를 정하지 않고도 U-1a 차단을 제거할 수 있다. 비차단 계수는 우회를 막지 않는다.
문법의 출처도 지적됐다 — **독립 소유권 규범이 아니라 기존 문법 밖 데이터를 수용하려고
추가했다**고 문서 자신이 명시한다(§13.2.1 ③).

또 **T-72 가 §11 에 결속되지 않았다** — §8 테스트표(:2055)와 §13.6.4(:3028)에만 있고
종료조건은 T-62 만 인용한다. **선언층/평가층 간극 9번째.**

### F-C (high) — F5 의 우회가 **floor 조작으로 이동**했고, 현재 경로는 **도달 불가**

```
:1499-1502  "floor 를 정하는 것은 규범이지 저작자의 선언이 아니다"
:1029-1038  (같은 문서) 레벨→kind 매핑은 **이 문서의 미승인 해석**이다 — OQ-11
```

**두 진술이 충돌한다.** 실측 확증: `VER-002-001:136-175` 는 evidence level 의 시험
의미만 정의하고 **surface_kind 매핑은 없다**. `tools/tos_spec_status.py:379-388` 은
`minimum_evidence_level` 이 비었는지만 검사한다. ⇒ **floor 는 규범 파생이 아니다.**

더 무거운 쪽은 반대 방향이다:

```
매핑을 유지하면  →  STATE-EV-004 는 검증 가능 floor 0 → FWD-a 필연 불충족
§11             →  FWD-a 충족이 **종료 조건**
§9              →  OQ-11 은 **비차단**            ← 정면 충돌
```

**OQ-11(외부 스펙 소유자 판정)이 실질적으로 Phase 0 를 차단한다.** 이 문서가 IC-5
계열로 다섯 번 겪은 영구 차단 덫이며, 이번엔 **v2.0 의 F5 구조적 폐쇄가 만들었다.**

### F-D (high) — U-9 의 절 인용은 U-7(의미 판정)을 제거하지 못한다

인용의 **해석 가능성**만 검사하고 **그 절이 원리적 불가능성을 실제로 지지하는지**는
검사하지 않는다. 저작자 자신이 UNCHK-021(:2592)에 그렇게 적었다. T-63 은 가짜 절과
자유 산문만 거부하고, T-74 는 동일 커밋 앵커 갱신을 못 잡는다. **⇒ F6 은 해소가 아니라
부분해소.** 저작자가 "기계화했다"고 주장한 범위가 실제 범위보다 넓었다.

## 관통 패턴

| 판 | 형태 |
|---|---|
| v1.3.8 ~ v1.7 | 선언 추가 → 소비처 미연결 (5회) |
| v1.8 | 양방향 동시 |
| v1.9 | 3건 동시 (문법→대조군 · 지표 3개→1개 결속 · 병합→미전파) |
| **v2.0** | **T-72 미결속(9번째)** + **교정이 새 결함을 만듦**(U-11 전사 · F5→OQ-11 차단) |

**v2.0 의 새로운 형태**: 앞선 판들은 "고치고 전파를 빠뜨렸다"였는데, 이번엔
**고치는 행위 자체가 두 개의 새 결함을 생성**했다 — 관측량을 추가하려다 완료를
금지했고, 우회를 닫으려다 영구 차단을 만들었다.

## 게이트

```
통과 = codex AND approve AND digest 일치
현재 = codex AND needs-attention AND 일치     → 불성립 (10회 연속)
```

**P-0 및 모든 D0 구현 착수 차단 유지.**

## 저작자 개정으로 닫을 수 없는 항목 (운영자 판단 필요)

**F-C 후단**은 개정 대상이 아니다. OQ-11 은 "레벨→kind 매핑을 누가 승인하는가"이고
문서가 **스펙 소유자 사항**으로 등재했다. 그 판정 없이는

- 매핑을 유지 → `STATE-EV-004` 가 FWD-a 를 통과 못 함 → **Phase 0 종료조건 미충족**
- 매핑을 완화 → **F5 가 되돌아감**(floor 조작 우회)

**두 선택지 모두 저작자 권한 밖이다.** 문서 개정으로 할 수 있는 것은
①"OQ-11 비차단" 표기를 **차단으로 정정**하거나 ② FWD-a 종료조건에서 해당 행을
명시 제외하고 그 제외를 계수하는 것뿐이며, **어느 쪽이든 Phase 0 완료 정의가 바뀐다.**
