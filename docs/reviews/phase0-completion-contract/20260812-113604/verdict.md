# verdict — 레인 B (계획 심판) · v1.6 · **4회 연속 완주**

```yaml
adjudicator: codex
verdict: needs-attention
gate_status: NOT_PASSED            # 6회 연속
reviewed_at_head: 2b7b2a209aefb9bd7186949f405f6418fd4902cd
reviewed_scope_digest: 1bd6e489affb69dab11fa73dc66d8aac0f5d336f363f590a299dd45b95337058
primary_doc_sha256: dd261a50e27a3cb8512d5c9f7dc967726bedefc6a1a1dcc12001f83bbce31d90
reviewed_version: v1.6 (2173행)
findings: 10                       # high 6 / medium 4
prior_verdict: .omc/review/20260812-111003/verdict.md
```

동결 5종 지표 시작=종료 일치. 4회 연속 완주.

## 처분 분포

| | v1.5 심판 | v1.6 심판 |
|---|---|---|
| 해소 | 3 | **2** (#5, DEF-3) |
| 부분해소 | 5 | 3 (#1, #3, #6) |
| 회피 | 1 | 1 (#2) |
| 문구만 | 1 | **2** (#4/DEF-2, #7) |
| 신규 | — | **3** (#1-DEF-1-regression, U-1a, non-negotiable-lookahead) |

## 수용검사 — 10건 전건 채택, 기각 0

### 확증 1 — 저작자의 의심이 **실제 우회로**로 실증됐다

v1.6 §5.2.8은 "합집합 규칙을 쓰면 138행의 floor 가 `{RUNTIME}` 뿐이라 **실질적으로
이상하다**"고 쓰고 OQ-11 미판정으로 등재했다. 심판이 그것을 **통과 경로**로 구체화했다.

```
합집합 규칙  →  단독 EV-L3 의 floor = {RUNTIME}
§5.2.4       →  RUNTIME 은 충족 판정에서 제외 (검증 불가 kind)
                ⇒ 검증 가능 쌍 0개
```

실측:

```
STATE-EV-004   status=READY   level='EV-L3'   → 검증 가능 쌍 0 → FWD-a 공허 통과
```

**IC-5 계열의 네 번째 재발이며, IC-5의 직전 재발(DEF-1)을 고친 바로 그 개정에서
생겼다.** v1.5는 "충족 요구를 검증 가능 kind로 한정"해 도달 불가를 풀었고,
v1.6은 합집합 규칙으로 floor 를 좁히면서 **그 한정이 만든 공허 통과 경로를
확인하지 않았다**. 두 수정이 각각은 옳고 조합이 결함이다.

### 확증 2 — U-1a는 **무력하면서 동시에 과잉 차단**이다

심판 지적: `owner_track` 은 `미배정`만 아니면 통과한다 — 허용 값 membership,
트랙 실재, 담당 주체 결속 검사가 없다.

추가 실측 (오케스트레이터):

```
:2072  UNCHK-004  새 29/30 전사 지점의 자동 발견   owner_track = 미배정
:2084  UNCHK-014  문서가 선언하지 않은 축          owner_track = 미배정
```

**U-1a 는 현재 즉시 Phase 0 를 차단한다.** 그런데 UNCHK-014 는 §13.5.2가
**원리적으로 닫을 수 없다**고 선언한 항목이다 — 소유 트랙을 배정해도 닫히지 않는다.

→ **원리적 한계와 미착수 작업을 같은 레지스터에 넣고 같은 규칙을 걸었다.**
전자는 소유자가 있을 수 없고, 후자는 있어야 한다. 다음 개정은 이 둘을 **분리**해야
하며, 그러지 않으면 U-1a 는 영구 차단이 된다(또 다른 IC-5).

### 확증 3 — **`CLAUDE.md` 비협상 규칙 갭 (첫 발생)**

지금까지 6회 심사에서 비협상 충돌은 계속 "해당 없음"이었다. **이번에 처음 나왔다.**

```
CLAUDE.md:104,107   백테스트는 look-ahead bias 를 피해야 한다.
                    LookaheadGuard 를 사용하고 지표 입력을 현재 컨텍스트
                    타임스탬프로 제한한다
상위 계획 §6 Phase 3:343-357
                    backtest fill model 을 synthetic/paper 관측치로 calibration
                    → 종료조건에 LookaheadGuard 또는 timestamp 경계 검증 **없음**
```

**대상은 이 설계 문서가 아니라 상위 계획이다.** P-0(상위 계획 개정) 범위에 이 항목을
추가해야 한다 — 현재 §12.1.1의 P-0 범위에 없다.

### 확증 4 — 저작자의 자체 스윕 주장이 불완전했다

| 항목 | 판정 |
|---|---|
| 옛 수치(374/81/13/13/6) | 역사 기록만 남음 — **통과** |
| K-7·K-8 앵커 K-2 이전 | **통과** |
| `D0-2→D0-1` | 문자열은 역사 서술뿐이나 **단계 명칭 전파 불완전**(§1 결속표가 여전히 D0-2) |
| **"범위 경계"** | **T-58에 능동 잔존** |
| **T-22** | 철회 선언했으나 **여전히 능동 테스트** |

**T-58은 자기모순이다**: "`+Security`/`+Broker`를 UNCHK 등재로 전환하면 실패"를
요구하는데, **v1.6의 처분이 정확히 그 전환**이다(UNCHK-016/017).

## 관통하는 패턴 — "등재는 차단이 아니다"가 3건에서 동시 적중

#2(회피) · #7(문구만) · #1의 OQ-11이 전부 같은 형태다. **UNCHK/OQ 등재는 미해결을
기록할 뿐 아무것도 막지 않는다.** 심판의 next_steps 첫 줄이 이를 못박는다:

> OQ/UNCHK 등재만으로 해소 판정을 부여하지 않는다. 실제 Phase 0 차단 전이와
> mutation 대조군이 확인되기 전에는 approve 불가다.

OD-2가 세운 "검사 불가를 등재해 노출한다"는 기제는 **노출에는 성공했고 차단에는
실패했다.** v1.6이 U-1a로 값을 매기려 했으나 위 확증 2가 보이듯 설계가 미완이다.

## DEF-4·DEF-5(이연)와의 관계

동결 중 Stop 게이트가 낸 DEF-4(§11이 `UNDECIDED`를 여전히 허용)·DEF-5(새 차단 계약에
대조군 없음)를 **심판이 독립 발견했다** — #4/DEF-2 finding 의 evidence 에 `:1883`과
T-25가 그대로 인용됐다. 두 채널이 같은 결함을 잡았다.

## 게이트

```
통과 = codex AND approve AND digest 일치
현재 = codex AND needs-attention AND 일치     → 불성립 (6회 연속)
```

## next_steps (Codex 원문)

1. 현재 verdict로 P-0 및 모든 D0 구현 착수를 계속 차단한다.
2. **OQ/UNCHK 등재만으로 해소 판정을 부여하지 않는다.** 실제 Phase 0 차단 전이와
   mutation 대조군이 확인되기 전에는 approve 불가다.
3. 재심에서는 floor의 모든 소비처, 전 계약군 호출 범위, `UNDECIDED`/U-1a 완료 전이를
   다시 실행 검증해야 한다.
