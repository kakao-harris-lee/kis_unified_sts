# verdict — 레인 B (계획 심판) · v2.2 · **10회 연속 완주**

```yaml
adjudicator: codex
verdict: needs-attention
gate_status: NOT_PASSED            # 12회 연속
reviewed_at_head: 2b7b2a209aefb9bd7186949f405f6418fd4902cd
reviewed_plan_paths:
  - docs/plans/2026-08-12-tos-phase0-completion-contract-design.md
  - docs/plans/2026-08-11-tos-completion-development-plan.md
reviewed_scope_digest: 6d4c3aab2ac2f98950763d0cd1e9cf64f2d940e8ba07d9ea94d49b651a5ac7c2
primary_doc_sha256: b3d830add709dc4045e06cf1e37f9da6ff9432b8c6845fedb3bf3a3d1a8eaa0b
reviewed_version: v2.2 (3404행)
findings: 7                        # critical 0 / high 7
prior_verdict: .omc/review/20260812-194442/verdict.md
mode: A (adversarial-review, --scope working-tree), job review-msq4r1ep-mmbn3y, 9m55s, write=false
codex_session: 019ff62d-b26f-7710-92e3-bc4800b1e0ad
```

동결 4종 지표 시작=종료 일치(10회 연속). 심사 중 개정 0.

**이번 회차의 방법론적 차이**: Codex 가 프로토타입을 실행하는 데 그치지 않고
**독립 뮤테이션을 직접 수행**했다 — `Path('tos-' + 'spec')` 문자열 합성 ·
L3 floor 에 `PACKAGE` 주입 · `closable` 전이 + 앵커 동시 갱신.
"exit 0 을 무결성 증명으로 취급하지 말라"는 지시가 실행으로 이행됐다.

> **절차 관측**: 스트리밍 `progressPreview` 에 `"verdict":"approve"` 가 잠시 나타났으나
> **미완성 중간 산출**이며 최종 구조화 verdict 는 `needs-attention` 이다.
> **중간 preview 를 판정으로 읽으면 게이트가 무너진다** — 기록해 둔다.

## 처분

**v2.1 직전 5건**: 부분해소 4 (OQ-11 · 설정 임계값 · INV-C3 · U-9) /
**미해소 1** (T-76·S-19·S-9 — 재분류 회피 판정).

**비협상**: 문서에서 리터럴 임계값은 **제거됨**(위반 0). 다만 정식 설정 표면이
D0-A 에 결속되지 않아 **부분해소**.

## 수용검사 — 7건 전건 채택, 기각 0

`file:line` 실측. 팬텀 0. 기각 사유 3종 어느 것에도 해당 없음.

### F1 (high) — **증거 도구가 자기가 발견한 결함을 실패에서 제외했다**

```python
# tools/spikes/phase0_contract/test_contracts.py:650-664
def u8b_literal_exclusion(cfg, rep: Report) -> None:
    ...
    rep.note(                       # ← Case 가 아니라 note
        f"... U-8b 위반 검출={bool(u8b_problems)}"   # False
    )
```

러너는 **G6(두 번째 AUTHORITY 게이트)가 허용 집합에 자동 포함되고 U-8b 가 위반을
검출하지 못함을 직접 관측**하고도, 그 결과를 실패 집계에서 뺐다. 따라서
**"19/19 양방향 성립 · exit 0" 이 유지된 채로 완료→권한 분리 결함이 생존한다.**

> **오케스트레이터 자체 판정**: 이것은 심판 지적이자 **저작자의 보고 오류**다.
> 프로토타입 출력의 `발견` 절에 U-8b 결함이 인쇄돼 있었고 그것을 반증 ④ 로 문서에
> 반영했으면서, **동시에 "19/19 성립"을 검증 결과로 보고했다.** 두 진술은 같은
> 실행에서 나왔고 서로 모순된다 — **green 이 보이는 것과 다른 뜻이라는, 이 문서가
> 12판째 다루는 바로 그 결함을 증거 도구가 저질렀고 저작자가 검산 없이 옮겼다.**

### F2 (high) — T-76 의 26종 앵커가 **여전히 실재하지 않는다**

`:2303` 이 "`minimum_evidence_level` 원시값 26종 (값, 행수) 표"를 앵커로 선언하지만
§5.2.8 에는 **고유값 수 `26` 과 floor 집합 분포 10행만** 있고 그 표가 없다.
프로토타입 강제 레지스트리에도 T-76 이 없다.
**심판 독립 뮤테이션**: L3 floor 에 `PACKAGE` 추가 → padded `EV-L3` 가 FWD-a-0 통과,
검출기 부재. v2.1→v2.2 처분은 **§11 참조만 추가**했고 앵커는 물질화되지 않았다.

### F3 (high) — OD-3 경계 준수 주장이 **측정과 강제 범위를 과장**한다

| 주장 | 실측 |
|---|---|
| `git status` 로 `proto/` 밖 신규 0건 | **러너 자신이 `proto/` 밖 신규 파일**이다 |
| 경계가 기계로 닫혔다 | T-77 은 **A~C 만** 검사. **D(CI 미편입)·E(D0 승격 금지)는 강제 없음** |
| A 리터럴 스캔 | `Path('tos-' + 'spec')` 뮤턴트를 **놓쳤다**(심판이 직접 심음) |
| C 쓰기 차단 | **열거된 일부 API 만** 감싼다 |

**⇒ "D0-A 와 실질 동일한 평가기를 스파이크라 부르는 경계가 다섯 조항 모두에서
기계적으로 닫혔다"는 주장은 성립하지 않는다.**

### F4 (high) — 비협상 교정이 **최종 D0-A 설정 표면에 결속되지 않았다**

리터럴은 문서에서 제거됐으나, 실재하는 설정 파일은 **OD-3-E 가 승격 금지한
프로토타입의 `config.yaml` 뿐**이다. §12.1 D0-A 산출물·§12.2 rollback·CI 입력 어디에도
정식 설정 경로·스키마·소유권·누락/오형 검사가 없다.
**프로토타입의 3→7 거동 변화는 폐기 대상 코드만 증명한다.**

### F5 (high) — 6e 는 **실행 가능한 생산 절차가 아니다**

단계명은 "판정 취득"이나 실행 행위는 **이름 없는 "스펙 소유자"에게 제기**하는 것뿐이다.
저장소 전수 검색에서 소유자 식별·요청 채널·판정 어휘/스키마·추적 경로·revision binding·
거부/무응답 처리·검증 호출이 **전부 부재**. 7단계는 **기록의 존재만** 요구하므로
내용 미결속 기록으로 우회되거나 영구 대기한다.

### F6 (high) — 반증된 "verdict 상수" 주장이 **활성 소비처에 남아 있다**

```
§4.2.2   v2.2 가 정정   — 상수성은 입력 변화에만 참
:3220    활성 잔존      — "verdict 가 상수이므로 사유 집합이 유일한 관측 표면이다"
§11      같은 주장 반복
UNCHK-022 구 1/11 설명 유지
```

**S-9(소비처 전수 갱신)를 "기계 강제 없는 규율"로 재분류한 바로 그 판에서 S-9 가
다시 위반됐다.** v2.2 §8.2 가 "강제 없는 규율은 반복 위반된다"고 자인했고,
**그 예측이 같은 문서 안에서 실현됐다.**

### F7 (high) — U-9 우회가 **그대로 Phase 0 을 통과한다**

**심판 독립 뮤테이션**: `closable=YES`→`NO` + owner 비움 + 무관한 `§4.2.2` 인용 +
앵커 동시 갱신 → **U-9·U-9a 포함 전체 enforcement findings 0.**
소유자 의무와 게이트 기여가 동시에 제거된다. UNCHK-021 등재는 소비·차단하지 않는다.

## 관통 패턴 — 11번째, 그리고 **증거 층으로 전이**

| 판 | 형태 |
|---|---|
| v1.3.8~v2.1 | 선언 ↔ 평가 간극 (10회) |
| **v2.2** | **간극이 증거 도구로 옮겨갔다** — 러너가 결함을 관측하고 exit 에 반영하지 않았다(F1). 그리고 S-9 재분류 직후 S-9 재위반(F6) |

**문서를 실행 가능하게 만들자 결함도 실행 층으로 따라 올라왔다.**
전환이 무의미했다는 뜻은 아니다 — F2·F3·F7 은 **심판이 직접 뮤테이션을 돌려** 찾았고
그것은 종이 심사에서 불가능했다. 다만 **"실행 증거"가 자동으로 더 신뢰할 만한 것은
아니며, 증거 도구 자체가 같은 규율을 받아야 한다**는 것이 이번 판의 교훈이다.

## 게이트

```
통과 = codex AND approve AND digest 일치
현재 = codex AND needs-attention AND 일치     → 불성립 (12회 연속)
```

**P-0 및 모든 D0 구현 착수 차단 유지.**

## next_steps (Codex 원문)

1. P-0 및 모든 D0 구현 착수 차단을 유지한다.
2. 재심에서는 **note 로만 남는 발견 없이 실패가 exit status 에 결속**되고,
   T-76·정식 설정·OQ-11 결정 아티팩트가 **실제 소비되는** 실행 증거를 다시 확인한다.
