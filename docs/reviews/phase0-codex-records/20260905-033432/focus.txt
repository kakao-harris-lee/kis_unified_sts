레인 B 계획 «재심» — Phase 0 완료 계약 v2.22, S-26 ② «독립 재심 2회 연속» 트랙 2회차. 운영자 명시 지시(2026-09-04 「S-26 재심 재개」)로 진행. 직전 판(.omc/review/20260904-233516/verdict.md · needs-attention · medium 1)의 finding 1 을 **계약 편집 없이** 검사기 수리로 처분했다 — 이 판의 1순위 질문은 **그 finding 이 «해소 / 부분 해소 / 미해소 / 회피» 중 무엇인가**이고, 2순위는 직전 판과 같은 currency 질문(계약 밖 변경이 동결 문언을 stale·거짓으로 만들었는가)이다. 계약 결속 경로 두 문서는 재심 #5 approve(48243cd2) 이래 byte 불변이다(blob 899689fc / ec3464c0). 계약의 다른 절을 새로 훑어 산문 결함을 찾는 것은 범위가 아니다(계약 K-10 · 2026-08-30 정지 지시 사유).

먼저 읽을 것 (순서대로):
1. CLAUDE.md — 비협상 규칙 및 «Read the index before the contract»(`python tools/tos_contract_index.py --locate S-26`, `--locate "§7.4"`, `--locate "§11"` 로 구간을 잡아 그 범위만 읽어라. 전체 스캔·요약 금지).
2. .omc/review/20260904-233516/verdict.md — 직전 판. finding 1: `_d1_u6prime_row_state` 가 U-6′ (ㄹ)(2) 스캔 범위(`scope_desc`)를 소비하지 않고 (4) 기록 경로를 디렉터리 접두사만 대조했다(fail-open).
3. 처분 커밋 fbb1a364 (`git show fbb1a364 -- tools/tos_completion_status.py tests/tools/test_tos_completion_status.py`; 사본 .omc/review/20260905-033432/evidence/scope-diff.patch 후반부): `_derive_d1_disposition` 이 `_d1_locate_no_dependency_record_stamp` 를 **한 번** 호출해 같은 `stamp_dir` 을 행-문법 검사(`record_path = <stamp_dir>/verdict.md` 완전 일치 요구 · None → (4) 미충족)와 기록 검증(`_d1_no_dependency_record_state(stamp_dir=…)` · 재탐색 없음)에 넘긴다 · (2)는 `scope_desc` 문자열과 `file_count` 를 각각 요구 · 대조군 ⑩-a~⑩-e(범위 삭제 · 범위 치환 · 같은-prefix 미선택 경로 · 두 스탬프 중 이전 스탬프 인용 → UNDECIDED · 정확 문자열 green). 계약 U-6′ (ㄹ) 문언(`--locate "§7.4"` 범위 안 U-6′ 블록)과 대조해 (2)(4)가 이제 문언과 같은 강도인지, 새 fail-open 이나 역방향 과잉 차단(정당한 행이 검사기 렌더링과 달라 red 가 되는 경로)이 생기지 않았는지 판단하라.
4. 같은 HEAD 에 함께 올라온 별건 커밋 93f4f6bd(UNCHK-024 · VERIFICATION-PROFILE-002 신설 키 `MAX_time_conservative_freshness_age_ms` null 등록 · resolver/marketfeed `VER-002-KEYS` 선언에서 `max_age_bound` → 신설 키 · census 164/17 · D0-5[resolver]/[marketfeed] `VALUED+UNBOUND` → `VALUED+BLOCKED`). **판정 대상은 이 커밋의 내용 자체가 아니라** 그것이 동결 계약 문언과 모순을 만들지 않는가다 — 특히 계약 :2974-2979(「UNCHK-024 … 실재하는 UNBOUND 키 … 무영향」)가 당시-참 기록으로 남는 것이 S-12 와 정합하는지, §11 D0-5 행의 «결정된 처분» 요건에 `VALUED+BLOCKED` 가 통과값인지(§7.4 D-1 처분 어휘). 계약은 편집되지 않았다.
5. S-26 ①~⑧ 항별 독립 측정을 직전 판과 같은 방식으로 다시 하고 summary 에 명시하라(② 는 이 판이 material 0 이면 «1/2», 아니면 «0/2» 로 적어라 — 직전 판이 material 1 이었으므로 카운터는 이 판에서 다시 시작한다). ⑧ 은 기존 교착 그대로이며 이 판의 종료 요구가 아니다.

결속 필드(디스패치 직전 포착 · .omc/review/20260905-033432/revision.txt): reviewed_at_head fbb1a36461161683887451fbacbc8dc518a918dd · reviewed_plan_paths = docs/plans/2026-08-12-tos-phase0-completion-contract-design.md, docs/plans/2026-08-11-tos-completion-development-plan.md · reviewed_scope_digest(plan_scope_digest) 41d967596b4c4757ada5ddb73945eff6199184659586043a22c0406f230313e7 · bound_set_digest(O-6, tos-spec/src/part-1-foundation/decisions/OQ-11-DISPOSITION.md:15) 4e6c975f794696066a25abe4ee827594afa18f8fac8bfb5e7bf31d43508b3c2f · decided_at_head 8923aab2. 재계산해 대조하라 — `{ git rev-parse HEAD; printf '%s\0' <두 경로> | LC_ALL=C sort -z -u | xargs -0 -r shasum -a 256; } | shasum -a 256`.

독립 측정 (직접 실행 · rc 를 읽어라):
 (a) `python tools/tos_contract_check.py` (③·④) 및 `--self-test` (⑤ · 145종 전건 판별 · 죽은 검사 0 · 역방향 과잉 차단 0).
 (b) `python tools/tos_completion_status.py --check` — RESULT GREEN · `d0a_entry_state=ENTRY_OK` · D0-5 행. 샌드박스에 쓰기 가능한 임시 디렉터리가 없어 rc 2 가 나면 그 사실을 적고 HEAD 의 생성물 `tos-spec/src/TOS-COMPLETION-STATUS.md` 로 대체 확인하라(직전 판과 같음).
 (c) `git rev-list --full-history 48243cd2..HEAD -- <두 결속 경로>` 공집합(⑥·①).
 (d) `python tools/tos_spec_status.py --check` — PASS 여부와 non-blocking WARNING(`2026-08-11-tos-completion-development-plan.md` §1 기준선 표가 163/16 인데 프로파일은 164/17)의 성격: 그 문서는 결속 경로라 편집이 S-26 ⑥ 리셋을 일으킨다 — 경고를 non-blocking 으로 두는 것이 계약(U-12 · S-26)과 정합한지 판단하라. 편집을 권고하려면 리셋·O-6 재결속·재심 비용을 함께 적어라.

이미 확정된 것(재판정 금지): §11 완료 판단(운영자 countersign 222dc4de) · 레인 A 코드 심판(Codex 코드 심사 제외 · 이번 두 커밋은 Claude 측 리뷰어가 별 레인으로 봤다) · 신설 키의 값 승인은 Bounds-Approver(운영자) 몫 · D0/P-0 착수 금지·restricted_live·production 불변.

범위 밖: 계약 산문 품질 · 코드 스타일 · 위 3·4·(d) 와 무관한 파일.

출력: verdict(approve | needs-attention) · summary(직전 finding 1 의 «해소/부분 해소/미해소/회피» 명시 · S-26 ②~⑥ 각 항 성립/불성립 · ② 카운터 «1/2» 또는 «0/2» · ⑧ 한 줄) · findings[] (severity · title · body · file · line_start/line_end 는 HEAD fbb1a364 에 실재 · confidence · recommendation) · next_steps[]. 계약 본문 «편집» 을 권고할 때는 S-26 ⑥ 리셋·O-6 재결속·재심 비용을 함께 적고, 편집 없이 닫을 수 있는 처분이 있으면 그것을 우선 적어라.
