레인 A 적대적 코드 심판 — Phase 0 완료 계약 §12.3 절차표 9행 «codex-reviewer 적대적 코드 리뷰» 이행. 이 판정이 Phase 0 완료 판정(§11)에 앞서는 기록 심판(reviewer of record)이다.

먼저 읽을 것 (순서대로):
1. CLAUDE.md — 비협상 규칙(선물 long/short 대칭 · 실계좌 선물 영구 차단 · 주식 EOD 일괄청산 금지 · ClickHouse 신규 사용 금지 · RL/TFT 부활 금지 · 임계값/심볼/포트/Redis DB/스케줄 하드코딩 금지 · Redis DB 1 + TTL · KST 세션 판정). 이 규칙에 배치되는 권고는 내지 마라.
2. .omc/review/20260903-165133/evidence/ 의 렌즈 증거 4건 (architecture.md · security.md · performance.md · style.md). 이것은 Claude 렌즈가 낸 «증거»이지 판정이 아니다. 각 발견을 독립 재검증하고, 렌즈가 놓친 것을 찾는 것이 당신의 일이다. 렌즈의 «점검했고 건전함» 목록도 그대로 믿지 말고 표본을 직접 재실측하라.

심사 범위: `git diff 28475ca1^ HEAD -- . ':!docs/plans' ':!docs/reviews'` — HEAD = faea9720. Phase 0 완료 계약의 D0 구현 블록 34파일(코드성 24파일 +11,428/−92): 검사기 tools/tos_completion_status.py · 코퍼스 검사기 tools/tos_spec_status.py · 색인 tools/tos_contract_index.py · tools/tos_evidence_run.py · tools/tos_profile_census.py · tools/wfcanon-v222.py · 진입 하니스 tools/tos_entry_harness.sh(계약 §12.3.4-R 정본 블록과 byte-동일 · sha 059e13f2… lockstep 핀) · live 실행기 tools/u17-verify.sh · CI .github/workflows/{tos-gate,tos-firewall,test}.yml · config/tos_completion.yaml · 테스트 tests/tools/*.py · 레지스터 CSV 3종 · 생성물 tos-spec/src/TOS-COMPLETION-STATUS.md · decisions/*.md 4건 · tos/src/tos/** docstring 전용 7파일.

범위 밖(심사하지 말 것): docs/plans/ 두 계획 문서의 산문(레인 B 소관 · 동결 · blob 결속). 단, 검사기가 «계약이 요구하는 것을 실제로 구현했는가»를 확인하기 위해 계약을 참조하는 것은 필요하다 — 10k행 문서이므로 `python tools/tos_contract_index.py --locate <§번호|식별자>` (예: §11 · §12.1 · §12.3.4 · U-12 · U-13 · U-14 · U-15 · U-16 · U-17 · K-3 · K-4 · D0-5)로 범위를 잡아 그 구간만 읽어라. 계약 본문 편집을 권고하지 마라(어떤 byte 변경도 S-26 리셋·재결속을 부른다 — 결함이 계약 쪽에 있으면 «계약 에라타 대상»으로 분류해 적되 편집안을 쓰지 마라).

우선순위 질문(높은 것부터 · 각각 file:line 실측으로 답하라):
① fail-open 경로 — 입력 부재·형식 오류·git 실패·gh 비-200·예외에서 «통과값»(MET · ENTRY_OK · NOT_REQUIRED · NO_ROWS_CLEAR · PREVENTION_ACTIVE 등)이 나오는 경로가 하나라도 있는가. `.get(k, default)` · `except: continue/pass` · 빈 집합에 대한 전칭(vacuous truth) · 기본값 대체를 전수 점검하라.
② 자기신고 vs 구조 파생 — 검사기·하니스가 소비하는 값 중 저작자가 문서/YAML/markdown 에 직접 적어 넣을 수 있고 다른 구조(git 이력 · blob id · 서버 응답)로 교차 검증되지 않는 것이 있는가. 특히 하니스 R-3(스탬프 선택자) · R-4(adjudicator/verdict) · R-7(`--full-history` 이력 술어) · U-15 currency · U-16 provenance · U-17 (b)②.
③ 죽은 검사 — 실패할 수 없는 술어, 대조군이 없어 판별력이 증명되지 않은 테스트. 범위 내 테스트 중 «수정을 되돌려도 여전히 통과하는» 것이 있는지 뮤테이션 관점으로 표본 검증하라(파일을 편집하지 말고 논리로 · 또는 임시 사본에서).
④ 게이트 우회 — 피심판자가 새 스탬프 디렉터리/verdict.md 를 만들거나 워크플로 파일을 PR 에서 고쳐 tos-gate 를 green 으로 만드는 경로. sha 검증 스텝 · 룰셋 required check 의 «이름만» 한계 · fork PR.
⑤ lockstep 정합 — sha 핀 자리(tos-gate.yml:17 · u17-verify.sh · wfcanon-v222.py · 계약 · 상위 계획)와 실제 파일 sha 가 전부 059e13f2… 로 일치하는가 실측.
⑥ D0-5 — tos/src/tos 변경 7파일이 정말 docstring 전용인가(코드 토큰 diff 0 · import-firewall 무영향).
⑦ CLAUDE.md 정합 — 하드코딩/DRY/설정 구동 위반.
⑧ 오늘 추가된 두 커밋(cdd3c561 mypy 1건 · faea9720 tos_spec_status 스캐너 git 우주)의 정확성 — 특히 `_reverse_scan_git_universe` 의 폴백이 fail-open 인지(git 실패 시 os.walk 로 조용히 떨어져 gitignore 파일이 census 에 섞이는 것이 허용 가능한가 — 계약 §2 표는 open-trading-api/ 미추적을 기준선으로 둔다).

등재 축 대조(보안 렌즈 E-1/E-3/C-1/A-1 관련): 계약 §13 레지스터 `tos-spec/src/verification/PHASE0-UNCHECKABLE-REGISTER.csv` 는 UNCHK-012(독립 리뷰 수행 여부 · blocked_by=«리뷰 산출물의 provenance 결속» · Phase 6) · UNCHK-023(git 이력 파생의 신뢰 기반 · Phase 1) · UNCHK-008(저장소 밖 강제 표면 · Phase 0) 을 «Phase 0 에서 기계 검사할 수 없는 축»으로 이미 등재하고 생성물에 노출한다. 렌즈가 실측한 «위조 verdict.md → ENTRY_OK»·«PR 안의 워크플로 정의» 경로가 이 등재 문언에 **그대로 덮이는지**, 아니면 등재보다 넓어서 새 finding 인지를 당신이 판정하라 — 덮인다면 pre-existing·등재됨으로 분류하되 그 판단 근거(레지스터 행 문언 대조)를 body 에 적고, 덮이지 않으면 severity 를 그대로 두라. 등재 여부와 무관하게 D0 블록이 그 경로를 «새로 열었거나 넓혔는지»(in-range diff 가 기여했는지)는 별도로 답하라.

D-1 처분 축(아키텍처 렌즈 high · `tools/tos_completion_status.py::_derive_d1_disposition` 약 :3670-3685): 계약 §7.4(색인 `--locate "§7.4"` → :2682-2823)는 처분 넷을 «공급된 키 K 와 프로파일 우주의 관계»로 정의한다(VALUED=K 존재·non-null · BLOCKED=K 존재·null · UNBOUND=K 가 VER-002 키가 아님 · UNDECIDED=K 미공급 잔여)이며 :2698 은 「저작자가 처분을 고르면 그 자체가 자기신고」라고 못박는다. 검사기는 «UNBOUND 선언 문언» 정규식을 키 대조보다 먼저 평가해 단락하고, D0-5 7사이트 전부가 그 분기로 UNBOUND 다(렌즈 뮤테이션: 키를 위조해도 불변 · 키 0개 + 문장만으로 UNBOUND · 문장 제거 + 실키면 VALUED). 이것이 §7.4 정의의 구현인지 자기신고의 재도입인지 — 계약 원문(:2698·:2705·:2771·:2785)과 검사기 코드·`tests/tools/test_tos_completion_status.py` 의 D-1 대조군을 직접 대조해 판정하라. 이 축은 §11 종료조건(D0-5 7사이트 전부 검사기 파생 배정 · UNDECIDED 차단)의 직접 입력이다.

출력: verdict(approve | needs-attention) · summary · findings[] (severity critical/high/medium/low · title · body · file · line_start/line_end · confidence · recommendation) · next_steps[]. finding 의 file:line 은 HEAD faea9720 에 실재해야 한다. 렌즈가 이미 적은 발견을 채택할 때도 당신의 독립 재검증 결과를 body 에 적어라. 범위 밖 기존 부채는 pre-existing 으로 표기하라(비차단 분류를 위해).
