레인 B 계획 «재심» — Phase 0 완료 계약 v2.22, S-26 ② «독립 재심 2회 연속» 트랙 3회차. 운영자 명시 지시(2026-09-04 「S-26 재심 재개」)로 진행. 직전 판(.omc/review/20260905-033432/verdict.md · needs-attention · medium 1 · 직전 finding 「부분 해소」)의 finding 1 을 **계약 편집 없이** 검사기 수리로 처분했다(커밋 0140b866). 1순위 질문: **그 finding 이 «해소 / 부분 해소 / 미해소 / 회피» 중 무엇인가.** 2순위: 계약 밖 변경이 동결 문언을 stale·거짓으로 만들었는가(currency). 계약 결속 경로 두 문서는 재심 #5 approve(48243cd2) 이래 byte 불변이다(blob 899689fc / ec3464c0). 계약의 다른 절을 새로 훑어 산문 결함을 찾는 것은 범위가 아니다(계약 K-10 · 2026-08-30 정지 지시 사유).

먼저 읽을 것 (순서대로):
1. CLAUDE.md — 비협상 규칙 및 «Read the index before the contract»(`python tools/tos_contract_index.py --locate S-26`, `--locate "§7.4"` 로 구간을 잡아 그 범위만 읽어라. 전체 스캔·요약 금지).
2. .omc/review/20260905-033432/verdict.md — 직전 판. finding 1: `_d1_u6prime_row_state` 가 (2) `scope_desc` 와 (4) `record_path` 를 reason 어디엔가의 부분문자열로만 봐서, 기대 scope 를 무관한 문맥에 두고 실제 공시는 다른 범위를 적어도, 선택 경로 뒤에 `.not-the-selected-record` 를 붙여도 `True`.
3. 처분 커밋 0140b866 (`git show 0140b866`; 사본 .omc/review/20260905-041033/evidence/fix-diff.patch): reason 을 ` · ` / `;` / 개행으로 구획 분할(`_d1_u6prime_segments`)하고, (2)는 구획 하나가 `스캔 결과: 후보 우주 {N}개, 스캔 범위 {scope_desc}, 파일 {F}개` 와 **정확 동일**, (4)는 구획 하나가 `독립 리뷰 기록: {record_path}` 와 **정확 동일**(리스트 멤버십 · 접미·접두·병기 불가). (1)(3)은 고정 문자열 부분문자열 검사 유지. 대조군 ⑪-a~⑪-e(무관 구획의 미끼 scope + 실제 구획 다른 scope · 경로 접미 · 두 번째 경로 병기 · scope 는 맞고 N 오류 · 세 값 구획 분산) + 정확 문자열 green. 계약 U-6′ (ㄹ) 문언(`--locate "§7.4"` 범위 안 U-6′ 블록 · 계약은 reason 문법을 못박지 않고 넷을 «적는다»고만 한다)과 대조해 (2)(4)가 이제 문언과 같은 강도인지, 검사기가 정한 구획 문법이 계약과 모순되지 않는지(계약이 정하지 않은 문법을 검사기가 정하는 것이 허용되는 형상인지 — D-5 의 `VER-002-KEYS:` 는 계약이 정했다), 새 fail-open 이나 역방향 과잉 차단이 생기지 않았는지 판단하라. 알려진 잠재 가정: `scope_desc` 는 `str(rel.parent | rel)`(tools/tos_completion_status.py 의 호출자) 로 repo-relative 경로라 구획 구분자를 포함하지 않는다 — 포함하면 red(fail-closed) 방향.
4. 커밋 목록 `git log --format='%h %s' 48243cd2..HEAD`(사본 evidence/commits.txt). 직전 판이 이미 판정한 것(93f4f6bd 가 계약을 stale 로 만들지 않음 · 163/16 경고 non-blocking 정합)은 재판정하지 말고 0140b866 이 그것을 깨지 않았는지만 본다.
5. S-26 ①~⑧ 항별 독립 측정을 다시 하고 summary 에 명시하라(② 는 이 판이 material 0 이면 «1/2», 아니면 «0/2»). ⑧ 은 기존 교착 그대로이며 이 판의 종료 요구가 아니다.

결속 필드(디스패치 직전 포착 · .omc/review/20260905-041033/revision.txt): reviewed_at_head 0140b86658896e24292fd46f6164e306e6afff0c · reviewed_plan_paths = docs/plans/2026-08-12-tos-phase0-completion-contract-design.md, docs/plans/2026-08-11-tos-completion-development-plan.md · reviewed_scope_digest(plan_scope_digest) e15a6cc3ddb1850103713660c18d2bd77fb9b0f4189ca87bb8fd5d86ea06058c · bound_set_digest(O-6, tos-spec/src/part-1-foundation/decisions/OQ-11-DISPOSITION.md:15) 4e6c975f794696066a25abe4ee827594afa18f8fac8bfb5e7bf31d43508b3c2f · decided_at_head 8923aab2. 재계산해 대조하라 — `{ git rev-parse HEAD; printf '%s\0' <두 경로> | LC_ALL=C sort -z -u | xargs -0 -r shasum -a 256; } | shasum -a 256`.

독립 측정 (직접 실행 · rc 를 읽어라):
 (a) `python tools/tos_contract_check.py` (③·④) 및 `--self-test` (⑤ · 145종 전건 판별 · 죽은 검사 0 · 역방향 과잉 차단 0).
 (b) `python tools/tos_completion_status.py --check` — RESULT GREEN · `d0a_entry_state=ENTRY_OK`. 샌드박스에 임시 디렉터리가 없어 rc 2 면 그 사실을 적고 HEAD 생성물 `tos-spec/src/TOS-COMPLETION-STATUS.md` 로 대체 확인(직전 두 판과 같음).
 (c) `git rev-list --full-history 48243cd2..HEAD -- <두 결속 경로>` 공집합(⑥·①).
 (d) 직접 대조군: `_d1_u6prime_row_state` 를 직전 판의 세 공격 문자열(미끼 scope · 경로 접미 · 두 번째 경로)로 호출해 각각 False 인지, 정확 구획으로 True 인지 — 그리고 당신이 고안한 **새** 우회 형상 하나 이상(구분자 변형 · 공백 · 유니코드 · 구획 순서 등)을 시험하라.

이미 확정된 것(재판정 금지): §11 완료 판단(운영자 countersign 222dc4de) · 레인 A 코드 심판(Codex 코드 심사 제외 · 0140b866 은 Claude 측 리뷰어가 별 레인으로 봤다 · 차단 없음) · 신설 키 값 승인은 운영자 몫 · D0/P-0 착수 금지·restricted_live·production 불변.

범위 밖: 계약 산문 품질 · 코드 스타일 · 위 3·(d) 와 무관한 파일.

출력: verdict(approve | needs-attention) · summary(직전 finding 1 의 «해소/부분 해소/미해소/회피» 명시 · S-26 ②~⑥ 각 항 성립/불성립 · ② 카운터 «1/2» 또는 «0/2» · ⑧ 한 줄) · findings[] (severity · title · body · file · line_start/line_end 는 HEAD 0140b866 에 실재 · confidence · recommendation) · next_steps[]. 계약 본문 «편집» 을 권고할 때는 S-26 ⑥ 리셋·O-6 재결속·재심 비용을 함께 적고, 편집 없이 닫을 수 있는 처분이 있으면 그것을 우선 적어라.
