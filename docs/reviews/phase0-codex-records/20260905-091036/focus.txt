레인 B 계획 «재심» — Phase 0 완료 계약 v2.22, S-26 ② «독립 재심 2회 연속» 트랙 4회차. 운영자 명시 지시(2026-09-04 「S-26 재심 재개」)로 진행. 직전 판(.omc/review/20260905-041033/verdict.md · needs-attention · medium 1 · 직전 finding 「부분 해소」)의 finding 1 을 **계약 편집 없이** 검사기 수리로 처분했다(커밋 9f80f6a1). 1순위 질문: **그 finding 이 «해소 / 부분 해소 / 미해소 / 회피» 중 무엇인가.** 2순위: 계약 밖 변경(9f80f6a1 · 8e14069a)이 동결 문언을 stale·거짓으로 만들었는가. 계약 결속 경로 두 문서는 재심 #5 approve(48243cd2) 이래 byte 불변이다(blob 899689fc / ec3464c0). 계약의 다른 절을 새로 훑어 산문 결함을 찾는 것은 범위가 아니다(계약 K-10 · 2026-08-30 정지 지시 사유).

먼저 읽을 것 (순서대로):
1. CLAUDE.md — 비협상 규칙 및 «Read the index before the contract»(`python tools/tos_contract_index.py --locate S-26`, `--locate "§7.4"` 로 구간을 잡아 그 범위만 읽어라. 전체 스캔·요약 금지).
2. .omc/review/20260905-041033/verdict.md — 직전 판. finding 1: `_d1_u6prime_row_state` 가 정본 `스캔 결과:` / `독립 리뷰 기록:` 구획의 **존재**만 검사해, 정확 구획과 모순 구획을 별개 구획(` · ` / `;` / 개행)으로 병기하면 True.
3. 처분 커밋 9f80f6a1 (`git show 9f80f6a1`; 사본 .omc/review/20260905-091036/evidence/fix-diff.patch): 구획을 라벨 접두(`startswith("스캔 결과:")` / `startswith("독립 리뷰 기록:")`)로 분류 → 종류별 **정확히 1개**(0 → 미충족 · ≥2 → 중복 위반 · 1 → 정본 문자열과 동일해야 함). 대조군 ⑫-a~⑫-e(모순 구획 병기 × 세 구분자 · 두 번째 기록 경로 · 동일 구획 2개 중복) + 기존 ⑩/⑪/green 유지. 계약 U-6′ (ㄹ)(`--locate "§7.4"` 범위 안 U-6′ 블록)과 대조해 (2)(4)가 이제 문언과 같은 강도인지, 새 fail-open(라벨 변형 · 공백 · 유니코드 · 구획 순서 · 라벨이 다른 구획 안에 등장 등 — 직접 시험하라)이나 역방향 과잉 차단이 남지 않았는지 판단하라. Claude 측 리뷰어가 적은 비차단 관찰: 개행 분할 뒤 선행 공백이 있는 정본 구획은 `startswith` 에 걸리지 않아 미충족(fail-closed 방향 · 이 커밋 이전부터의 성질).
4. 별건 커밋 8e14069a(UNCHK-024 종결 · `MAX_time_conservative_freshness_age_ms` = 1000 ms 운영자(Bounds-Approver) 승인 · census 148/16 · D0-5[resolver]/[marketfeed] `VALUED` 7/7 · 게이트 status §3.28 · 사본 evidence/approval-commit-stat.txt). 판정 대상은 값 승인 자체(운영자 결정)가 아니라 동결 계약과의 모순 여부만 — 특히 계약 :2974-2979 「UNCHK-024 … 실재하는 UNBOUND 키 … 무영향」이 당시-참 기록으로 남는 것(직전 두 판이 이미 S-12 정합으로 판정 · 재판정 금지)과, §11 D0-5 행이 `VALUED` 균일로 바뀐 것이 계약 §7.4 처분 어휘·§11 행 상태와 정합한지.
5. S-26 ①~⑧ 항별 독립 측정을 다시 하고 summary 에 명시하라(② 는 이 판이 material 0 이면 «1/2», 아니면 «0/2»). ⑧ 은 기존 교착 그대로이며 이 판의 종료 요구가 아니다.

결속 필드(디스패치 직전 포착 · .omc/review/20260905-091036/revision.txt): reviewed_at_head 9f80f6a131174df099da4b7137de79047305c50b · reviewed_plan_paths = docs/plans/2026-08-12-tos-phase0-completion-contract-design.md, docs/plans/2026-08-11-tos-completion-development-plan.md · reviewed_scope_digest(plan_scope_digest) ea4ea16c17f2ebf572251071bdb6395b9f238e83329379df7ef6a93c5f0ed43f · bound_set_digest(O-6, tos-spec/src/part-1-foundation/decisions/OQ-11-DISPOSITION.md:15) 4e6c975f794696066a25abe4ee827594afa18f8fac8bfb5e7bf31d43508b3c2f · decided_at_head 8923aab2. 재계산해 대조하라 — `{ git rev-parse HEAD; printf '%s\0' <두 경로> | LC_ALL=C sort -z -u | xargs -0 -r shasum -a 256; } | shasum -a 256`.

독립 측정 (직접 실행 · rc 를 읽어라):
 (a) `python tools/tos_contract_check.py` (③·④) 및 `--self-test` (⑤ · 145종 전건 판별 · 죽은 검사 0 · 역방향 과잉 차단 0).
 (b) `python tools/tos_completion_status.py --check` — RESULT GREEN · `d0a_entry_state=ENTRY_OK`. 샌드박스 임시 디렉터리 부재로 rc 2 면 그 사실을 적고 HEAD 생성물 `tos-spec/src/TOS-COMPLETION-STATUS.md` 로 대체 확인.
 (c) `git rev-list --full-history 48243cd2..HEAD -- <두 결속 경로>` 공집합(⑥·①).
 (d) `python tools/tos_spec_status.py --check` — PASS · profile 164/16 · non-blocking 경고 성격(직전 판 판정 유지).
 (e) 직접 대조군: `_d1_u6prime_row_state` 를 직전 판의 병기 공격(정확 구획 + 모순 구획 · 세 구분자 각각)으로 호출해 False 인지, 정확 구획 하나로 True 인지 — 그리고 당신이 고안한 **새** 우회 형상 하나 이상을 시험하라.

이미 확정된 것(재판정 금지): §11 완료 판단(운영자 countersign 222dc4de) · 신설 키 값 승인(운영자 Bounds-Approver 결정 8e14069a) · 레인 A 코드 심판(Codex 코드 심사 제외 · 9f80f6a1·8e14069a 는 Claude 측 리뷰어가 별 레인으로 봤다 · 차단 없음) · D0/P-0 착수 금지·restricted_live·production 불변.

범위 밖: 계약 산문 품질 · 코드 스타일 · 위 3·4·(e) 와 무관한 파일.

출력: verdict(approve | needs-attention) · summary(직전 finding 1 의 «해소/부분 해소/미해소/회피» 명시 · S-26 ②~⑥ 각 항 성립/불성립 · ② 카운터 «1/2» 또는 «0/2» · ⑧ 한 줄) · findings[] (severity · title · body · file · line_start/line_end 는 HEAD 9f80f6a1 에 실재 · confidence · recommendation) · next_steps[]. 계약 본문 «편집» 을 권고할 때는 S-26 ⑥ 리셋·O-6 재결속·재심 비용을 함께 적고, 편집 없이 닫을 수 있는 처분이 있으면 그것을 우선 적어라.
