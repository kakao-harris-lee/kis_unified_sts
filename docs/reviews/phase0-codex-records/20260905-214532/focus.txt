레인 B 계획 «재심 #10» — Phase 0 완료 계약 v2.22(에라타 57·58차 · 재심 #9 approve `review-mtnyojac-nhulgc` @ 38bfb1fd) · **결속 head 갱신 회차**. 계약 blob(`6f94dfbb…`)·개발계획 blob(`ec3464c0…`)·bound_set_digest(`045f3ae7…`) 는 재심 #9 와 **byte 동일**이고 이 판에서 계약 문언은 심사 대상이 아니다. 심사 대상은 하나다: 38bfb1fd 이후의 커밋 세 개(C3 7d6fc076 verdict 착지 · C4 f24deb84 생성물 재생성 · **머지 커밋 97596460** = `origin/main` dc900970(PR #462 · 주식 regime 관측성 · 7파일) 을 브랜치에 머지)가 **결속 경로를 건드리지 않았고 approve 의 대상(계약 내용)을 바꾸지 않았는가.** 운영자 명시 지시(2026-09-05 「PR 개설 진행」 · PR #644 main ← 이 브랜치).

왜 이 판이 필요한가(사실 · 판정 대상 아님): 진입 하니스 U-15 R-7 은 `git log --full-history <reviewed_at_head>..HEAD -- <bound_paths>` 공집합을 요구한다. PR merge ref 에서 HEAD = merge(main, branch) 인데, main(dc900970)이 reviewed_at_head(38bfb1fd)의 조상이 아니면 그 머지 커밋은 두 부모 모두 interesting 이라 main-부모와 !TREESAME 인 자리(계약)에서 **머지 커밋 자체**가 나열된다 → `APPROVAL_STALE`(PR #644 tos-gate run 실측 · 로컬 재현 `.omc/review/20260905-214532/evidence/`). #642 가 통과한 것은 당시 main 이 reviewed_at_head 의 조상이었기 때문이다. 처방은 main 을 브랜치에 머지한 head 를 **새 reviewed_at_head** 로 세우는 것 — 그러면 PR merge ref 에서 main 부모는 uninteresting 이 되어 나열되지 않는다. 계약·검사기·하니스는 무접촉이다(하니스 R-7 의 술어 변경은 §12.3.4-R lockstep 이라 하지 않는다).

먼저 읽을 것 (순서대로):
1. CLAUDE.md — 비협상 규칙 및 «Read the index before the contract». 계약 본문은 이 판에서 읽을 필요가 없다(blob 동일). 굳이 확인하려면 `python tools/tos_contract_index.py --locate "§12.3.4"` 로 R-7 정본만.
2. .omc/review/20260905-144700/verdict.md — 재심 #9 approve(reviewed_at_head 38bfb1fd · plan_scope_digest d669993d… · bound_set_digest 045f3ae7…).
3. `git log --format='%h %p %s' 38bfb1fd..97596460309e25a6bce55b453d832eb6e566f4ab` — 세 커밋. `git diff --stat 38bfb1fd 97596460 -- docs/plans/2026-08-12-tos-phase0-completion-contract-design.md docs/plans/2026-08-11-tos-completion-development-plan.md` = **공집합**인가 · `git show --stat 97596460` 의 7파일이 전부 PR #462(`config/stock_regime.yaml` · `services/stock_strategy/daemon.py` · `services/trading/indicator_queries.py` · `shared/streaming/stock_regime.py` · 테스트 3)인가 · C3 가 `docs/reviews/phase0-completion-contract/20260905-144700/verdict.md`(R-3 정본) + 보존소 + README 만인가 · C4 가 `tos-spec/src/TOS-COMPLETION-STATUS.md` 재생성만인가.

결속 필드(디스패치 직전 포착 · .omc/review/20260905-214532/revision.txt): reviewed_at_head 97596460309e25a6bce55b453d832eb6e566f4ab · reviewed_plan_paths = 위 두 경로 · reviewed_scope_digest(plan_scope_digest) 5748fc5bb2e7e1a8ca292dd95daa8af1eae9caf113f3fbbf469728d28ae2a5fb · bound_set_digest(O-6, OQ-11-DISPOSITION.md:15) 045f3ae7565860df6e0c38d3c7ee49c76f3a4d3784d646279cd1be2727dbb429(불변 · 재결속 없음) · decided_at_head 1db8f9b8. 재계산해 대조하라 — `{ git rev-parse HEAD; printf '%s\0' <두 경로> | LC_ALL=C sort -z -u | xargs -0 -r shasum -a 256; } | shasum -a 256`. 계약 blob `git hash-object <계약>` = 6f94dfbbafd48fa3d1b4c73266fdfda19da9bce5 인가.

독립 측정: `python tools/tos_contract_check.py` + `--self-test` rc 0(직접 실행). `python tools/tos_completion_status.py --check` 는 이 verdict 착지(C3′) 전이라 `[U-15] APPROVAL_STALE(97596460)` + `[D0-1] 생성물 불일치` 2건이 예상값(샌드박스 rc 2 면 사실을 적어라). 그 둘 «외» 위반이 없는지만.

재판정 금지: 계약 문언(57·58차 · #9 approve 그대로) · 코드(PR #462 는 자체 심사·머지 완료 · Codex 코드 심사 제외) · S-26 상태(② 0 · ⑧ 교착) · D0/P-0 착수 금지·restricted_live·production 불변.

출력: verdict(approve | needs-attention) · summary(세 커밋의 결속 경로 무접촉 여부 · blob·digest 일치 명시) · findings[] (file:line 은 HEAD 97596460 에 실재) · next_steps[]. 계약 본문 «편집»을 권고할 이유는 이 판에 없다 — 권고한다면 S-26 ⑥ 리셋·O-6 재결속·재심 비용을 함께 적어라.
