# TOS 커널 라운드 #3 계획 — `EventKind.CORPORATE_ACTION`(C1) · item 12 venue-facts 합성 술어(C2) · W2-K `RELEASED` 투영 + ⓖ 해소 세대(C4) · attestation 재계수 문언(C7)

- **상위**: Phase 5 계획 §11 처분(2026-09-12 · 결정 1 «라운드 #3 다음 웨이브» · 11.3 범위 = C1·C2·C4·C7 · 제외 C3/C5/C6/C8) · 커널 라운드 #1 계획 §0 공통 규율 · 라운드 #2 계획 §2 결정 1(분리 모듈은 `gateway.py` 무import) · §2 결정 2(3진 극성 `True⇒SATISFIED · False⇒DENIED · None⇒UNKNOWN` — §11 결정 5 로 **확정**) · Phase 5 §10(W2-K 정의) · Phase 3 계획 §7.11 이월 ⓖ · W5 계획 §6 ⑥(nontrade 엔진 진입은 라운드 #3) · 설계 #31 §2.2(«확장은 열거 추가만») · ADR-002-010 §10 line 217(용량은 rcl 만).
- **선행**: W5 MERGED → main `837afad3`. 서베이 `scratchpad/kr3-survey.md`(2026-09-12 · `file:line` 실측).
- **저작**: 세션 모델 단독. **커널 편집은 레인 K 한 곳**(round #1 §0) · 런타임 편집은 커널 필드 변경이 강제하는 소비자 갱신에 한정(라운드 #2 K-3 관용구) · 권한 부여 0 · 실 브로커 송신 0 · EV 상태 변경 0 · bound 문서 무접촉.
- **브랜치**: `feat/tos-kernel-round-3`(워크트리 `../kis_unified_sts-kr3` · main `837afad3` 기점).

## 0. 서베이 실측 (요지)

| 항목 | 실측 | 함의 |
|---|---|---|
| C1 어휘 | `EventKind` 2멤버(`engine/vocabulary.py:97-98`) · `ADMISSIBLE_EVENT_KINDS` positive 집합(`:104-106`) · 도크스트링·설계 #31 §2.2 «corporate-action 은 열거 추가만 · 디스패처 인터페이스 무변경» | 열거 추가는 예견됨 |
| C1 «추가만» 이 아닌 곳 | `EventResult.outcome_digest` 가 kind 별 2분기(`core.py:319-320`) · `EngineEvent._payload_matches_kind` 2엔트리 딕셔너리(`records.py:277-293`) · 런타임 `replay.py:407/421` 이 kind 별 비교(제3 kind 는 **비교 없음**으로 빠짐) · `test_engine_event_vocabulary.py` 가 «정확히 2» 와 bogus 문자열 `"CORPORATE_ACTION"` 을 핀 | 세 곳 + 테스트를 red-first 로 함께 |
| C1 핸들러가 할 수 있는 일 | nontrade 는 sibling edge 0(자기 쪽 import 0) · engine→nontrade import 는 firewall/importlinter 가 막지 않음(orthostate «widened edge» 선례 `engine/orthostate_projection.py:19-24`) · 용량은 rcl 만(`nontrade/_base.py:31-33` · cause 토큰 `TransitionCause.RECOGNIZED_EXTERNAL_CHANGE` `rcl/vocabulary.py:102`) · orthostate 에 non-trade 필드 없음 | 핸들러 = `nontrade_disposition` 호출 + 결과·cause **제안** 을 `EventResult` 에 실음 · 용량·투영 변경 0 |
| C2 현 게이트 | `_check_venue_generations`(`gateway.py:744-782`) = bare bool 2 · `_positive` 단일 게이트라 DENIED 도달 불가 · 사유 문언 2곳 stale(«no owning runtime service») · `records.py:808-811` `⚠ provisional` | 합성은 커널로 · 3진 극성 적용 · `mesh.py` 분리 패턴(`gateway.py` 무import · 닫힌 필드표 · `_verdict/_positive` 공유) |
| C2 venue 술어 | `session_phase_admits`(`venue/state.py:77` · `OrderAdmissibilityResult`) · `account_constraint_conservative`(bool) · `tradability_conflict_unknown`(`OrderAdmissibilityResult`) · `egress_currentness_active`(bool) | 반환 타입 혼재 → 합성 술어는 `bool | None` 로 정규화(SATISFIED/DENIED/UNKNOWN 매핑은 게이트) |
| C4 | `PROJECTION_ORDER` 7멤버 · `RELEASED` 의도적 부재(`engine/state.py:69,74-81`) · `ProvisionalReservationLedger` 에 release 메서드 없음(`:233-617`) · dedup 서명 5튜플에 해소 세대 없음(`:258-267`) · 커널 테스트 3건이 부재를 핀(파일 재확인 필요) · `engine/state.py` 704행(여유) · 런타임 `finality_projection.py` 가 RCL 반환 후 엔진 in-memory 투영은 프로세스 수명 동안 점유 | 커널: `RELEASED` + finality-proof 토큰 게이트 release + 서명 6튜플 · 런타임: `finality_projection.py` 가 consume 성공 후 release 호출 |
| C7 | `egressgw/__init__.py:36-41` «three fields … no owning runtime producer» = W3/W5 로 **거짓** · `gateway.py:756-762,776-781` 동일 · `test_egressgw_package.py:68-81` 이 그 문장을 리터럴 핀 · «five provisional(3,6,12,14,15)» 정적 분할표는 참(유지) · Phase 5 계획 `:31` `RESUME_CONSERVATIVE`(커널 부재 · `ReadinessVerdict.READY`) | 문언+테스트 동시 정정(red-first) · 문서 1행 |
| 규율 | 레인 K 단일 · 판정은 커널 · red-first 5단계 · 예산 손재등재 · stash/checkout/reset 금지 | §4 |

## 1. 범위

| 포함 | 제외 (사유) |
|---|---|
| C1 `EventKind.CORPORATE_ACTION` + `CorporateActionPayload` + `_handle_corporate_action`(disposition 소비 · cause 제안 · 용량 0) + `EventResult` 제3 분기 + 런타임 replay 비교 규칙 + inbox 통과 확인 · C2 `egressgw/venuefacts.py::venue_session_account_facts_current(...)`(sub-fact 3 → `bool | None`) + `SendBoundaryContext` 필드 3 신설·legacy 필드 삭제 + `_check_venue_generations` 3진 극성 + 런타임 `context.py` 소비자 갱신 · C4 `RELEASED` 투영 + `release(attempt_id, proof: FinalityProofRef)` + 서명 해소 세대 + 런타임 `finality_projection.py` 호출 · C7 재계수 문언 3곳 + 테스트 + `records.py` provisional 표지 + Phase 5 계획 1행 | C3 만기 술어(§11 결정 10) · C5/C6/C8(§11) · 런타임 nontrade 소비자를 엔진 이벤트로 **전환**(런타임 `observe_nontrade` 문은 유지 · 엔진 경로 결선은 엔트리포인트 웨이브) · G-1/G-2 · `IncidentService` 후보 진입점 · spec/register 편집 |

## 2. 결정

1. **C1 payload = 커널 레코드 + 주입 좌표, 판정은 핸들러 안 커널 술어**: `engine/records.py` `CorporateActionPayload(FrozenModel)`: `event: tos.nontrade.NonTradeEventRecord` · `envelope: TransitionEnvelope | None` · `split_spec: SplitTransformationSpec | None` · `correction: CorrectionReversalRecord | None` · `required_legs: frozenset[CredibleTransitionLegKind]` · 주입 좌표(`venue_admissibility: str | None` · `time_freshness: str | None` · `protective_action_may_proceed: bool | None` · `are_*`/`rcl_*` 좌표는 `nontrade_disposition` 16 kwarg 중 런타임이 공급 가능한 것만 필드로, 나머지는 None 고정 필드 — 필드명은 kwarg 명과 동일). `EngineEvent.corporate_action: CorporateActionPayload | None` + `_payload_matches_kind` 3엔트리. `EventKind.CORPORATE_ACTION = "CORPORATE_ACTION"` · `ADMISSIBLE_EVENT_KINDS` 3.
2. **C1 핸들러는 판정·제안만**: `EngineCore._handle_corporate_action(event, admission) -> EventResult` — (a) 커널 `nontrade` 술어들을 payload 로 호출(`transition_envelope_complete`·`favorable_netting_absent`·split 3종·`correction_reversal_idempotent`·`instrument_lineage_preserved`·`effective_window_blocks_new_risk`·`material_change_trigger_nonempty`) → `nontrade_disposition(...)` (b) `NonTradeOutcome(FrozenModel)`: `disposition` · `predicate_results: Mapping[str, bool | str | None]` · `unevaluated: tuple[str, ...]` · `capacity_remap_proposal: TransitionCause | None`(= `RECOGNIZED_EXTERNAL_CHANGE` **iff** `disposition is NONTRADE_ADMISSIBLE` 이고 payload 가 remap 대상 좌표를 가질 때 · 그 외 None) · `restrictive: bool`(비-ADMISSIBLE 4 멤버 · 어휘 `vocabulary.py:412-415` 인용) (c) `EventResult.nontrade_outcome: NonTradeOutcome | None` + `outcome_digest` 제3 분기 = `nontrade_outcome.outcome_digest`(disposition·cause·predicate_results 정렬 canonical digest) (d) **용량·투영·래치 변경 0** — `ProvisionalReservationLedger`·orthostate·halt 상태를 건드리지 않는다(구조 핀: 핸들러 본문 AST 에 ledger/projection 메서드 호출 0). 래치는 런타임 소비자(driver 가 `restrictive` 를 보고 `record_new_risk_halt` — 엔트리포인트 웨이브 · 지금은 `observe_nontrade` 문 유지).
3. **C1 replay 규칙**: `tos_runtime/engine/replay.py` — `CORPORATE_ACTION` 은 `outcome_digest` 단독 비교(EGRESS_RESULT 와 동일 등급) + `EVENT_CONSUMED` 영수증에 `nontrade_disposition` 문자열 병기(불일치 = divergence). `replay.py:407` 의 «None/None 스킵은 EGRESS_RESULT 도달 불가» 핀을 «EGRESS_RESULT·CORPORATE_ACTION 도달 불가» 로 확장. 시퀀서/플로우 지문은 DECISION_TICK 전용 그대로.
4. **C2 합성은 커널, sub-fact 는 런타임**: `egressgw/venuefacts.py`(신규 · `gateway.py` 무import · `records.py` 의 `_verdict/_positive` 공유): `venue_session_account_facts_current(session_facts_current: bool | None, tradability_facts_current: bool | None, account_facts_current: bool | None) -> bool | None` = 셋 중 하나라도 `False` ⇒ `False` · 하나라도 `None`(False 없이) ⇒ `None` · 전부 `True` ⇒ `True`(None 전파 AND · 순서 무관). `SendBoundaryContext` 에 필드 3 신설(`session_facts_current` · `tradability_facts_current` · `account_facts_current`) · **`venue_session_account_facts_current` 필드 삭제**(합성값은 게이트가 계산 · 표 밖 필드 = 구조 오류). `_check_venue_generations` 를 `venuefacts.py::venue_generation_item_verdict(context)` 로 이동(mesh 패턴): 합성값 `False` ⇒ **DENIED**(§11 결정 5) · `None` ⇒ UNKNOWN · `True` 이고 `broker_constraint_generation_current is True` ⇒ SATISFIED · broker 반쪽 `False` ⇒ DENIED · `None` ⇒ UNKNOWN. 사유 문언은 어느 sub-fact 가 막았는지 명시(첫 비양성 우선). 런타임 `compose/context.py`: `SessionFactsOwner` 가 이미 내부에서 세 sub-fact 를 계산하므로 그 셋을 노출(`session_facts_current()`·`tradability_facts_current()`·`account_facts_current()` 리더)해 필드 3 에 공급 · 합성은 더 이상 런타임이 하지 않음(`_and_with_none` 삭제) · honesty e2e 의 사유 핀은 새 문언으로.
5. **C4 release 는 finality-proof 토큰으로만**: `engine/state.py` — `PROJECTION_ORDER` 에 `RELEASED` 를 터미널 뒤 마지막 위치로 추가(주석 «cannot release» 삭제 → «releases only via finality proof») · `FinalityProofRef(FrozenModel)`: `attempt_id` · `proof_digest` · `evidence_seq` · `resolution_generation: int` · `ProvisionalReservationLedger.release(ref: FinalityProofRef) -> bool`: attempt 가 터미널 상태(FULL_FILL/CANCEL_ACK/EXPIRED/REJECT 등 기존 어휘) 이고 `ref.attempt_id` 일치일 때만 `RELEASED` 로 전이 · 응답 kind/문자열로는 불가(타입 게이트) · 이미 RELEASED 면 False(멱등) · dedup 서명 6튜플 = 기존 5 + `resolution_generation`(TIMEOUT 해소 후 같은 5튜플이 다시 오면 세대가 달라 DUPLICATE 가 아니라 새 결과 — ⓖ). 커널 테스트 3건(부재 핀) 개정 + 신규 핀. 런타임 `engine/finality_projection.py`: `FinalityReleaseConsumer.consume` 이 RCL RELEASED 를 기록한 뒤에만 `core.ledger.release(FinalityProofRef(...evidence_seq=<CAPACITY_RELEASE_INTENT seq>, resolution_generation=<witness generation>))` 호출 · 실패/False 는 evidence 로(`ENGINE_PROJECTION_RELEASE_SKIPPED`).
6. **C7 재계수**: `egressgw/__init__.py:36-41` → «정적 분할표 5 provisional(3,6,12,14,15) 은 커널 처분표 기준으로 유지 · 참조 런타임은 이제 다섯 항목 전부를 구조적으로 파생하며 **남은 운영자 attestation 0**(item 12 = `SessionFactsOwner` W5 · item 16 = `tos_runtime.safety.latch` W3)» · `gateway.py` item 12 사유 2곳 → 새 `venuefacts.py` 문언으로 대체(이동으로 자연 소멸) · `records.py:808-811` `⚠ provisional` → 필드 3 설명으로 · `test_egressgw_package.py:68-81` 를 «zero remaining» 핀으로 개정(red-first) · `docs/plans/2026-09-10-tos-phase5-recovery-governance-plan.md:31` `RESUME_CONSERVATIVE` → `ReadinessVerdict.READY`(문서) · `staterestore/__init__` 은 stale 없음(서베이 확인 · 무편집).
7. **커밋 순서(레인 K · red-first)**: K-1 C7 문언+테스트(가장 작음 · 기준선 확립) → K-2 C2 `venuefacts.py` + 필드 + 극성 + 런타임 `context.py`/owner 리더/e2e 문언 → K-3 C4 `RELEASED`/release/서명 + 테스트 3 개정 + 런타임 `finality_projection.py` → K-4 C1 어휘/payload/핸들러/`EventResult`/replay 규칙/테스트 → K-5 예산 손재등재 · 전체 게이트. 각 커밋은 `tos/tests` + `tos/runtime/tests` green.
8. **예산**: `gateway.py` 는 줄어듦(재등재 감소) · `records.py` 증가분 재등재 · `engine/state.py`·`core.py`·`records.py`(engine) 실측 후 1000/100 초과 시 **분할**(예: 핸들러를 `engine/_corporate_action.py` 로) 등재 금지(신규) · 런타임 `replay.py`/`finality_projection.py`/`context.py` 실측.

## 3. 기각 대안

- C1 을 «열거 추가만» 으로 최소화 → `outcome_digest`·payload 검증·replay 가 제3 kind 를 비교 없이 통과(정합성 축소) · 세 곳 동시. · 핸들러가 용량 remap 을 직접 적용 → ADR-002-010 §10 위반 · 제안 토큰만. · C2 를 런타임 합성 유지 → 판정 저작(라운드 #1 §0 규율 위반) · 커널로. · item 12 `False` 를 UNKNOWN 유지 → §11 결정 5 와 불일치. · C4 release 를 `EgressResultKind` 문자열로 → 응답만으로 용량 반환(Phase 5 종료 조건 1 위반) · 토큰 타입. · C7 을 docstring 만 고침 → 리터럴 핀 테스트 red · 동시 개정. · 라운드에서 nontrade 엔진 경로까지 런타임 결선 → 레인 K 범위 초과 · 엔트리포인트 웨이브.

## 4. 레인

| 레인 | 파일 | 내용 |
|---|---|---|
| **K**(단일 · 순차 커밋 K-1~K-5) | 커널: `engine/{vocabulary,records,core,state}.py`(+분할 시 `engine/_corporate_action.py`) · `egressgw/{__init__,gateway,records,venuefacts}.py` · `tos/tests/{engine,egressgw}/*` · `config/tos_size_budget.yaml` · 런타임(커널 강제 소비자 갱신만): `compose/context.py` · `calendar/owner.py`(sub-fact 리더 3 노출 · 합성 삭제) · `engine/replay.py` · `engine/finality_projection.py` · `tos/runtime/tests/{compose,engine,calendar}/*` 해당 테스트 · 문서: Phase 5 계획 `:31` | 결정 1~8 |

- 규율(round #1 §0 · round #2 §4): 커널 diff 는 K 커밋에만 · 판정 로직은 커널 · red-first(핀 → 갱신) · `git commit -- <paths>`·stash/checkout/reset 금지 · 예산 손재등재 · 레인 시작 시 `tos.__file__`/`tos_runtime.__file__` 이 이 워크트리인지 확인.

## 5. 종료 조건

- 커널·런타임 green · ruff/black/mypy 0 · firewall PASS(engine→nontrade edge 는 firewall/importlinter 허용 확인 · `.importlinter` 계약 3 유지) · lint-imports · 예산 0(신규 등재 없이 분할) · `tos_completion_status --check` GREEN · spec/contract `--check` PASS · EV 상태 변경 0.
- 실증: (1) `EventKind` 정확히 3 · bogus 문자열 여전히 거부 · CA 이벤트가 핸들러에 도달해 `nontrade_outcome` 생산 · 4 픽스처(W5 f3 관측을 payload 로 변환) disposition 이 런타임 `NonTradeEventProcessor` 결과와 **동일**(같은 커널 술어) (2) replay: CA 이벤트 포함 이력이 identical · outcome 변조 ⇒ divergence (3) item 12: sub-fact `(True,True,True)`+broker True ⇒ SATISFIED · 어느 하나 `False` ⇒ DENIED · `None` ⇒ UNKNOWN · 합성 e2e SATISFIED 유지 · honesty e2e UNKNOWN + 새 사유 문언 (4) `RELEASED` 가 finality 토큰으로만 · 응답 kind 로 시도 = 타입 오류 · TIMEOUT 해소 후 재도착이 DUPLICATE 아님 (5) `egressgw/__init__` «zero remaining» 핀 green · 옛 «three fields» 문자열 0 · `RESUME_CONSERVATIVE` 문서 0.
- 뮤테이션(리뷰 실행 필수): **M1** 핸들러에 `ledger.commit_unbound`/orthostate 호출 삽입 → 구조 핀 red · **M2** `outcome_digest` 제3 분기 제거 → replay 비교 핀 red · **M3** 합성에서 `False` 를 `None` 으로 → DENIED 핀 red · **M4** legacy 필드 `venue_session_account_facts_current` 재도입 → 필드표 핀 red · **M5** `release` 가 토큰 없이(문자열 kind 로) 전이 → 타입/핀 red · **M6** 서명에서 `resolution_generation` 제거 → ⓖ 재도착 DUPLICATE 핀 red · **M7** `__init__` 에 «three fields» 복원 → 재계수 핀 red · **M8** replay 에서 CA 를 비교 없이 통과 → divergence 핀 red · **M9** `ADMISSIBLE_EVENT_KINDS` 에서 CA 제거(enum 만 남김) → 도달 핀 red.
- 독립 리뷰(sonnet · 저자와 다른 패스 · 뮤테이션 표 필수) approve · 판정 PR 코멘트.

## 6. 운영자 확인 지점

1. `PROJECTION_ORDER` 에서 `RELEASED` 의 위치(터미널 뒤 마지막) 와 «RELEASED 는 finality 토큰으로만» 문언 승인.
2. `CorporateActionPayload` 에 실을 주입 좌표 집합(§2 결정 1) — 런타임이 공급 못 하는 좌표는 None 고정 필드로 두는 방식 승인.
3. item 12 DENIED 도달 개시(§11 결정 5 적용) — 운영상 DENIED 는 재무장 필요 상태임을 재확인.
4. engine→nontrade 단방향 edge 개방(orthostate 선례) 승인.
5. 후속: 런타임 nontrade 엔진 경로 결선(엔트리포인트 웨이브) · spec `EVIDENCE-REQUIRED-KINDS` 에 CA 관련 식별자 신설 여부.
