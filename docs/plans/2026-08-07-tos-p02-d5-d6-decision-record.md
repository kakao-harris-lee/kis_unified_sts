# P0-2 결정 기록 — canonicalization(D5) + 명세 대조 수단(D6) (2026-08-07 형식화)
## — **D6 결정 + D5 처분**

> **문서 성격**: 프로젝트 측 **결정 기록**(`2026-07-29-tos-phase0-role-scheme-and-disposition.md`의 D1/D3
> 기록과 동급 — **문서 종류로서 동급이며, 증거 등급은 동급이 아니다: 아래 ⚠ 표 참조**). GOV-001의 세
> 거버넌스 행위(문서 비준·ADR acceptance·live authorization) 중 어느 것도 아니며, 어떤 EV 항목도
> 이동시키지 않고 어떤 승인 효력도 갖지 않는다.
>
> 부제가 "D5·D6 결정 기록"이 아니라 **"D6 결정 + D5 처분"**인 것은 아래 귀속 등급표의 직접 귀결이다 —
> 결정 주체가 확인된 항목만 "결정"이라 부른다(§1.1).
>
> **소급 형식화임을 명시한다.** 두 항목 모두 **2026-07-29 무렵부터 실질 집행됐고**(아래 각 절의 실측
> 좌표), 본 문서는 그에 대한 **기록 행위만** 수행하며 내용을 새로 만들거나 변경하지 않는다. 미결 표면 =
> P0-2 실행계획 `docs/plans/2026-07-29-tos-phase0-p02-execution-plan.md` **§1 T3**(`:44-50`).
>
> ---
>
> **⚠ 두 항목의 귀속 등급이 다르다 — 이 문서는 그 차이를 봉합하지 않는다.**
>
> | 항목 | 처분 상태 | **결정 주체 귀속** |
> |---|---|---|
> | **D6** | 옵션 (a)로 집행됨 | **운영자 행위 확인됨** — 2026-07-29 운영자의 MCP 재가동 완료 보고(대화 수준·리포 외). §2.2 |
> | **D5** | 옵션 (a)로 **집행됨** | **미확인** — 운영자 응답이 어떤 표면에도 부재. 귀속 근거가 저작 세션 산출물뿐(순환). §1.2-b |
>
> **선례 기준**: D1/D3 기록(`2026-07-29-tos-phase0-role-scheme-and-disposition.md:11`·`:42`)은 **운영자
> 문언을 축자 인용**하고(`"혼합 — AI 리뷰 + 운영자 서명"`·`"일괄 소급 승인 + AFG 2건만 특정"`) **응답
> 표면을 지목**한다(register `docs/plans/2026-07-29-tos-phase0-human-gate-register.md` §11 `:287-313`의
> 결정 패키지 D1~D4에 대한 답변). ⇒ **D6은 이 기준을 실질 충족**(식별된 운영자 행위 + 지목된 표면)하고,
> **D5는 충족하지 못한다**(축자 문언 없음 · 응답 표면 없음 · 결정 패키지에 D5 항목 자체가 부재 — 위
> register §11은 D1~D4만 열거한다).
>
> ---
>
> **기록이 필요했던 이유**: 두 항목 모두 **코드·산출물에는 반영됐으나 기록 문서가 없었다.** 근거·효력
> 범위·비효력(무엇을 승인하지 *않는가*)이 코드 주석과 작업 메모에 흩어져 있으면 "D5가 프로덕션
> canonicalization을 확정했다"·"D6이 N-17을 종결시켰다" 같은 **과대 해석**이 구조적으로 가능해진다.
> 더욱이 D5의 경우 **코드 헤더의 자기기술이 반복 인용되면서 운영자 귀속이 사실처럼 굳는 경로**가
> 이미 열려 있었다. 본 기록은 그 경계를 명문화하고, 굳어가던 귀속을 **미확인 상태로 되돌린다.**

---

## 1. D5 — canonicalization 산출 알고리즘

### 1.1 처분 상태

**처분 상태**: 실행계획 §1 T3의 선택지 중 (a) — 잠정 `ev-l1-provisional-0` canonicalizer 로 **집행됨**.
**결정 주체 귀속 = 미확인.**

즉, **"무엇이 일어났는가"는 확정적이고("옵션 (a)가 집행됐다"), "누가 그것을 결정했는가"는 미확정이다.**
이 문서는 전자를 기록하고 후자를 **열린 채로** 둔다. 근거는 §1.2-b.

> 본 절의 제목을 "결정"이 아니라 "처분 상태"로 둔 것은 문체가 아니라 **판정**이다. 결정 기록은 결정
> 주체가 확인될 때만 "결정"이라 부를 수 있다.

실행계획이 제시한 선택지(`:46-48`):

| 선택지 | 내용 | 채택 |
|---|---|---|
| **(a)** | 잠정 `ev-l1-provisional-0` canonicalizer 채택 — **비프로덕션 라벨 명시**. non-live INSTANCE에 정합하고, 프로덕션 canonicalization은 G2 후속으로 유지 | **✅ 채택** |
| (b) | digest 공란 유지 — 승인·번들 바인딩 불가가 지속 | 미채택 |

### 1.2-a 집행 좌표 (실측) — "무엇이 일어났는가"

| 무엇 | 좌표 | 내용 |
|---|---|---|
| 도구 | `tools/bcp_digest.py` | Broker Capability Profile digest 도구 |
| 귀속 **주장** (증거 아님 — §1.2-b) | `tools/bcp_digest.py:7-11` | "a **provisional** canonicalizer adopted under operator decision **D5 (2026-07-29)**" |
| 비프로덕션 라벨 | `tools/bcp_digest.py:4-6` | `NON-PRODUCTION.  ALGORITHM ID = ev-l1-provisional-0` (배너로 격리) |
| 알고리즘 식별자 | `tools/bcp_digest.py:311` | `ALGORITHM_ID = "ev-l1-provisional-0"` |
| 자기식별 digest | `tools/bcp_digest.py:566` | 모든 출력이 `ev-l1-provisional-0:{kind}:sha256:{hex}` 형태 — 미래의 프로덕션 digest와 **혼동·무언 치환이 구조적으로 불가** |
| 커밋 | **`30ad565c`** (2026-07-30) | `feat(tools): D5 provisional BCP canonicalizer — ev-l1-provisional-0 digest tool` |
| 검증 테스트 | `tests/tools/test_bcp_digest.py` | **frozen golden vectors**(`:19-27` docstring · `:173-261` 고정 픽스처와 하드코딩 hex). as-of 2026-08-07 실측 **120/120 passed** — 단 이 숫자는 as-of이고, **집행의 근거는 숫자가 아니라 드리프트 검출 기제 자체**다(§1.3-2) |

**개정 이력(도구 자체의 품질 레인)**: attempt-1(2026-07-29)의 적대적 리뷰가 **REVISE(blocking 3건)**를
반환했고, attempt-2(2026-07-30)가 전건을 반영하면서 **번복된 결정의 원문을 `AMENDED` 표기로 보존**했다
(`tools/bcp_digest.py:26-33`). 거버넌스 객체는 과거에 무엇을 주장했는지 보여야 한다는 규율의 적용이다.

### 1.2-b 귀속 근거 — **순환**이며, 운영자 확인 대기

**귀속을 뒷받침하는 표면은 3건뿐이고, 셋 다 같은 저작 세션의 산출물이다.**

| # | 표면 | 왜 독립 증거가 아닌가 |
|---|---|---|
| 1 | `tools/bcp_digest.py:7-8` 코드 헤더 — "adopted under operator / decision **D5 (2026-07-29)**"(행 경계로 끊김) — 같은 귀속 문구가 `:388`·`:996`에도 재출현 | **자기기술.** 저작 세션이 자기 산출물 안에 쓴 문장이며, 세션 밖 무엇도 확증하지 않는다 |
| 2 | 커밋 `30ad565c` 메시지 본문 — "per operator decision D5 (2026-07-29)" | **동일 세션의 동일 산출.** 커밋 author는 `harris.lee`지만 이 리포의 AI-오케스트레이션 커밋은 **전부** 그 author를 쓰며, 본문 말미가 `Co-Authored-By: Claude Fable 5`다. 이 커밋이 담은 파일은 **`tools/bcp_digest.py`와 `tests/tools/test_bcp_digest.py` 2건뿐** — 운영자 입력물은 0건 |
| 3 | `tests/tools/test_bcp_digest.py:4` — "adopted under operator decision D5 (2026-07-29)" | **위 커밋에 동봉된 같은 저작물.** `:1157`은 그 문자열이 CLI 출력에 있는지를 단정할 뿐, 결정 사실을 검증하지 않는다 |

**운영자 응답 표면의 부재 — 양방향 실측(2026-08-07, HEAD `670aef6a` + 워킹트리)**:

| 찾은 곳 | 방법 | 결과 |
|---|---|---|
| 캠페인 README **운영자 결정 절 3곳** — `:455`(2026-07-31 wave-4 직후) · `:471`(같은 날 후속 집행) · `:556`(2026-08-04 결정 4건) | `grep -n 'D5\|canonicaliz\|bcp_digest\|ev-l1-provisional'` 전문 | **0 hit.** 운영자 결정이 실제로 기록되는 절들에 D5는 **없다** |
| human-gate register `docs/plans/2026-07-29-tos-phase0-human-gate-register.md` | 동일 grep + §11 결정 패키지 정독 | **§11(`:287-313`)의 즉시 결정 항목은 D1·D2·D3·D4 4건뿐 — D5 항목 자체가 부재.** `:132`는 G2 행에서 `ev-l1-provisional-0`을 "비프로덕션"으로 **하류 참조**할 뿐 채택 결정을 귀속하지 않는다 |
| 대화 기록 | 리뷰어 확인 | 2026-07-29 대화에 **D5 응답 부재** (같은 확인에서 D6 운영자 행위는 **발견됨** — §2.2) |
| 리포 전역 | `grep -rn '\bD5\b'` (`.git` 제외) | tos 관련 hit는 `bcp_digest.py` 5건 · `test_bcp_digest.py` 2건 · 실행계획 `:46`(**미결 상태의 선택지 제시**) · 본 문서. **다른 D5 언급은 무관 문맥**(engine 설계 #31의 D5, ADR 결정지도의 D5, risk 골든마스터 테스트 fixture) |

⇒ **판정: 순환.** "운영자가 D5를 결정했다"의 유일한 근거는 그렇게 주장하는 저작물 자신이다.
**옵션 (a)가 집행된 것은 사실**이지만(§1.2-a는 전부 실측 가능), **그 집행이 운영자 결정의 이행이었는지
저작 세션의 자체 판단이었는지는 리포 안의 어떤 증거로도 가려지지 않는다.**

> **이 기록이 취하는 태도**: 세탁하지 않는다. 코드 헤더가 그렇게 적혀 있다는 이유로 운영자 귀속을
> 승계하지 않고, 반대로 저작 세션의 자체 판단이었다고 단정하지도 않는다. **미확인은 미확인으로
> 남기고 운영자 확인 게이트를 연다**(§3-1). 확인 전까지 `ev-l1-provisional-0`의 지위는 **세션-집행
> 잠정 도구**이며, 비프로덕션 라벨은 **어느 쪽으로 판명되든 유지**된다(라벨은 귀속이 아니라 알고리즘의
> 성격에서 나온다).

### 1.3 도구 규율 (집행에 부수하는 제약 — 기록의 일부)

1. **도구는 프로파일에 절대 쓰지 않는다.** `tools/bcp_digest.py:23-24` — "This tool NEVER writes to a
   profile. It computes and it verifies; **filling the digest fields is an approval-time authoring act
   performed by a human.**" ⇒ digest 기입은 도구의 부수효과가 아니라 **승인-시점 인간 저작 행위**다.
2. **digest 값이 바뀌면 `ALGORITHM_ID` 범프가 강제된다.** `tools/bcp_digest.py:31-33` +
   `tests/tools/test_bcp_digest.py:20-27`. golden vector가 이 강제의 유일한 **드리프트 검출기**다 —
   다른 digest 단정은 전부 상대 비교라 고정된 id 아래에서 알고리즘이 조용히 바뀌어도 green으로 남는다.
3. **`TBD`는 값이 아니라 채움 규약의 placeholder다.** `tools/bcp_digest.py:317` (`PLACEHOLDER = "TBD"`),
   `:905-906`에서 "a value is OWED"로 보고된다.

### 1.4 현재 상태 — draft의 digest 3필드는 여전히 `TBD` (의도된 상태)

`docs/broker-profiles/KIS-BROKER-CAPABILITY-PROFILE-draft.yaml`:

| 문서 | 좌표 | 필드 3종 |
|---|---|---|
| DOCUMENT 1 / MOCK_VTS | `profile_identity` 블록 | `canonicalization_version` · `canonical_semantic_digest` · `byte_digest` = 전부 `TBD` |
| DOCUMENT 2 / REAL_PROD | `profile_identity` 블록 | 동일 |

> **⚠ 행번호 드리프트 주의 — 이 파일은 행번호로 인용하지 말 것.** 본 기록 저작 중에도 병렬 세션이 이
> YAML을 편집하고 있어 앵커가 계속 움직인다. 같은 두 블록의 실측 이력:
>
> | 관측 시점 | DOCUMENT 1 | DOCUMENT 2 |
> |---|---|---|
> | HEAD `670aef6a` | `:266-269` | `:3119-3121` |
> | 워킹트리 — 2026-08-07 1차 관측 | `:320-323` | `:3365-3367` |
> | 워킹트리 — 같은 날 2차 | `:339-342` | `:3521-3523` |
> | 워킹트리 — 같은 날 3차(본 개정 시점) | `:339-342` | **`:3533-3535`** |
>
> **행번호가 이 기록 한 편을 쓰는 동안 세 번 움직였다**(DOCUMENT 2 기준 `3119 → 3365 → 3521 → 3533`).
> 네 시점 모두 **값(`TBD` 3개)과 구조는 불변**이고 행번호만 이동했다 — 즉 위 표는 드리프트의 *증거*이지
> 앵커가 아니다. **정본 앵커는 `profile_identity` 아래 3필드명뿐.** 이 파일을 행번호로 인용한 문장은
> 작성 시점에 이미 틀렸을 수 있다.

**이것은 미집행이 아니다.** 옵션 (a)가 확보한 것은 *산출 수단*뿐이고, 기입은 §1.3-1의 규율대로 **승인
시점의 인간 저작 행위**다. INSTANCE가 아직 `DRAFT`이므로(승인 배선 T4 미완) 채워지지 않은 것이 정직한
상태다.

### 1.5 효력 범위 — 그리고 명시적 비효력

**효력**:

- P0-2 실행계획 §1 T3의 D5 항목이 **집행 측면에서는 처분됐다**(미결 → 옵션 (a) 집행). 단 **거버넌스
  측면에서는 열려 있다** — 귀속 미확인이므로 §3-1의 운영자 확인 게이트가 선다.
- INSTANCE 승인 시점에 digest 2필드를 `TBD` 두 칸으로 남기지 않고 **재현 가능한 값**으로 채울 수단이 존재한다.

**비효력 — 이 집행이 하지 *않는* 것 (혼동 방지)**:

1. **프로덕션 canonicalization을 확정하지 않는다.** 버전화된 canonical form, 서명 바인딩, content
   addressing, cross-artifact digest 바인딩, canonicalization-version 협상 — 전부 **G2 미결 항목으로
   그대로 남는다**. 좌표: `docs/plans/2026-07-29-tos-broker-capability-profile-kis-draft.md:284`(U-1) ·
   실행계획 `:46`(D5) · `tools/bcp_digest.py:13-19`가 이를 코드 안에 못박아 뒀다.
2. **`tos-spec/src/part-1-foundation/ARCHITECTURE-GATE-STATUS.md` §7 step과 무관하다.** §7 step 1이
   요구하는 "canonicalization" 기제의
   **선정·보안 리뷰**를 충족하지 않으며(잠정·비프로덕션 라벨이 그 반대를 명시한다), step 8(첫 broker
   Capability Profile 완성)도 닫지 않는다. §8 Gate Verdict의 4행(`Ready for ADR acceptance: NO` 포함)은
   **불변**이다.
3. **어떤 승인 효력도 없다.** 문서 비준·ADR acceptance·live authorization 어느 것도 아니다. Bounds-Approver
   승인, INSTANCE `DRAFT` 해제, `EVIDENCE-REGISTER-002.csv`의 `pending-P0-2` **64행** 해소는 전부 별개
   게이트이며 이 집행으로 이동하지 않는다.
   **더욱이 귀속이 미확인이므로(§1.2-b), 이 도구를 "운영자 승인된 수단"으로 인용하는 것 자체가 금지된다.**
   현재 지위 = **세션-집행 잠정 도구**.
4. **live/production 아티팩트에 이 digest를 쓸 근거가 되지 않는다.** non-live INSTANCE 한정이며, 그 한정은
   digest 문자열의 접두사 자체(`ev-l1-provisional-0:`)로 강제된다.

---

## 2. D6 — 명세 대조 수단

### 2.1 처분 상태 (귀속 확인됨)

**처분 상태**: 실행계획 §1 T3의 조건문이 **운영자 행위로 해소**됐고, 옵션 (a)로 집행됐다.

실행계획이 제시한 것은 선택지라기보다 **조건문**이다(`:49-50`): "`kis-code-assistant-mcp` **재가동 가능
여부** — 가능하면 N-17 즉시 처리 / 불가면 공식 문서 수동 대조로 대체". ⇒ **조건의 참·거짓을 정하는
행위 = 운영자의 MCP 재가동**이며, 그것이 실제로 일어났다(§2.2). 수동 대조 대체 경로는 **발동되지 않았다.**

### 2.2 집행 좌표 (실측) — 운영자 행위와 하류 산출물의 구분

**⚠ 이 구분이 D5와의 결정적 차이다.** 아래 1번이 **운영자 행위**이고, 2번 이하는 그 행위의 **하류
산출물**이다. 하류 산출물을 운영자 행위의 증거로 재활용하지 않는다.

| # | 층위 | 좌표 | 내용 |
|---|---|---|---|
| **1** | **운영자 행위 (귀속 근거)** | **리포 외 — 2026-07-29 대화 수준** | 운영자가 **MCP 재가동(연결 완료)을 직접 보고**했다. 이것이 실행계획 §1 T3의 조건문("재가동 가능 여부")을 **해소하는 행위**다. **리포 안에 이 행위의 사본은 없다** — 대화 기록이 유일 원천이며 리포만으로는 재검증 불가(정직 표기) |
| 2 | 하류 산출물 — 수단 자기보고 | `docs/plans/2026-07-29-tos-p02-n17-spec-collation.md:5` | "**수단**: `kis-code-assistant-mcp` (조회 전용 — 주문 발생 없음)". **세션 자기보고**이므로 1번의 증거가 아니라 1번의 **결과**다 |
| 3 | 하류 산출물 — 대조 대상 | 같은 문서 `:4` → `docs/runbooks/kis-capability-probes.md` §7 (16항목) | 정본 체크리스트 |
| 4 | 하류 산출물 — 원전 | 같은 문서 `:6` | `github.com/koreainvestment/open-trading-api` `examples_llm/` (KIS 공식 1차 저장소) |
| 5 | 하류 산출물 — 접근 일자 | 같은 문서 `:7` | 2026-07-29 |
| 6 | 하류 산출물 — 커밋 | **`239b175a`** (2026-07-29) | `docs(tos-plans): N-17 spec collation record (16 items: 5 confirmed, 6 partial, 5 unknown)` |
| 7 | 하류 산출물 — 코드 표면 | `tools/broker_probes/registry.py` N-17 `skip_reason` | 처분 사실 반영으로 갱신(2026-08-07). 갱신 전에는 D6을 **미결 조건문으로 서술**해 팬텀을 재생성하고 있었다 |

**D5와의 대칭 비교 — 왜 하나는 확인이고 하나는 미확인인가**:

| | D6 | D5 |
|---|---|---|
| 식별된 운영자 행위 | **있음** (MCP 재가동 보고) | **없음** |
| 그 행위의 표면 | 대화(리포 외) — 지목 가능 | 지목 가능한 표면 자체가 없음 |
| 리포 내 증거 | 하류 산출물만 (자기보고) | 하류 산출물만 (자기보고) |
| **판정** | **귀속 확인** — 리포 내 증거가 자기보고뿐인 것은 D5와 같으나, **자기보고 밖에 실제 운영자 행위가 식별**된다 | **귀속 미확인** — 자기보고 밖에 아무것도 없다 |

> **정직 표기 3건**:
> 1. 1번은 **리포 외 증거**다. 이 문서는 그것을 리포 안으로 옮겨오지 않으며, 옮겨온 척도 하지 않는다.
>    리포만 읽는 독자에게 1번은 **검증 불가**이고, 그 사실을 여기 적어두는 것이 이 행의 목적이다.
> 2. D1/D3 선례의 **축자 인용**은 D6에도 없다(운영자 문언의 리포 내 사본 부재). D6이 선례 기준을
>    충족한다고 보는 근거는 축자 인용이 아니라 **식별된 운영자 행위 + 지목된 표면** 두 요소다.
> 3. 산출 문서 헤더 `:8`은 "**커밋**: 금지 (본 문서 미커밋)"라고 적혀 있으나 그 문서는 이후
>    `239b175a`로 실제 커밋됐다. 그 줄은 **저작 시점의 작업 규율**이며 현재 상태에 대해서는 stale이다.
>    본 기록은 이를 해소하지 않고 관측만 남긴다(산출 문서 본문 재작성은 별건).

### 2.3 산출 — 16항목 전수 판정

산출 문서 `:116-121` 집계표 직독:

| 판정 | 건수 | 항목 번호 |
|---|---|---|
| **확정** | **5** | 1, 3, 13, 14, 16 |
| **부분** | **6** | 2, 4, 5, 6, 8, 15 |
| **미확인** | **5** | 7, 9, 10, 11, 12 |
| 합계 | **16** | — |

**대조가 실제로 뒤집은 것 2건**(산출 문서 `:123-125`):

- **#14** — 로드맵 후보 TR id `HHDFC55020100`이 **반증**됐다(정답 `FHKST03030200`).
- **#13** — Q-MIC-1(모의 야간 세션 미지원)이 **추정 → 구조적 확정**으로 승격됐다. MOCK→REAL 외삽 금지의
  직접 근거가 된다.

**증거 규율(산출 문서 §0 `:16-37`)** — 결정의 일부로 기록해 둘 값:

- 증거 등급 **E1/E2/E3**을 도입해 "요청 body 전수 확보(E1)"에서만 **필드 *부재* 확정**을 허용했다.
- 부재 판정을 **"이 문서면에 없음"으로 한정**하고 "KIS 포털 명세 전체에 없음"으로 확대하지 않았다 —
  각 항목에 부재의 **범위**를 명시(anti-phantom 대칭).
- **2차 커뮤니티 원천(pykis·mojito·블로그)을 값 확정 근거로 사용한 건수 = 0**(`:153`).
- **추정값으로 채운 항목 = 0**(`:128`, `:152`). 확인 실패는 전부 "미확인"으로 남겼다.

### 2.4 효력 범위 — 그리고 명시적 비효력

**효력**:

- P0-2 실행계획 §1 T3의 D6 항목이 **처분됐다**(조건문 → 운영자 행위로 해소, 옵션 (a) 집행, 대체 경로
  미발동). **D5와 달리 귀속까지 확인된 처분**이다.
- N-17(측정이 아니라 문서 대조로만 해소 가능한 항목)이 **실행되어 산출물을 남겼다**.
- **D6 조건문을 살려두던 리포 내 표면 2곳이 해소됐다**(2026-08-07, 본 기록과 같은 작업):
  - 런북 `docs/runbooks/kis-capability-probes.md` §7 서두 — "재가동 불가 시 수동 대조 — 결정 D6에 종속".
  - 코드 `tools/broker_probes/registry.py` N-17 `skip_reason` — "The instrument … is operator decision D6".
    **코드 표면이 미결 조건문을 들고 있으면 CLI(`--coverage`·skip 사유 출력)가 팬텀을 계속 재생성한다**는
    것이 갱신 이유다.

**비효력**:

1. **미확인 5건은 열린 채로 남는다.** 후속 경로는 산출 문서 §4 `:132-140`에 명시돼 있다 — **#7 미지/중복
   필드 처리 → 운영자 문의 또는 모의 프로브**(주문 write-surface라 모의 한정), #9 `approval_key` 유효기간
   → **운영자 문의**, #10 WebSocket 구독 상한 → **P-14 실측**, #11 REST 유량 → **P-13 실측**,
   #12 자격증명 폐기 API → **운영자 문의**. D6은 이들을 닫지 않는다.
2. **산출 문서는 제안만 한다.** `config/kis/tr_ids.yaml` 편입(#16), `probes_real.py::ALLOWLIST` 갱신(#14)
   등은 전부 **별도 승인 레인**이며 D6이 승인하지 않는다(산출 문서 `:60` 등에 명시).
3. **INSTANCE 값 기입 권한이 아니다.** `hard_limits`는 **비운 채 유지**이고 통설 수치 기입은 여전히
   금지이며(`:55`), `sessions.subscription_limit`은 `null` 유지다(`:54`).
4. **어떤 게이트도 닫지 않는다.** GATE-STATUS §7 step 11(승인된 broker capability probe 실행)은 N-17이
   스크립트 프로브가 아니므로 무관하며, §8 Gate Verdict는 불변이다.

---

## 3. 이 기록이 여는 것 / 열지 않는 것

### 3-1. **운영자 게이트 (신설) — D5 귀속 확인**

> **질문**: 2026-07-29에 `ev-l1-provisional-0` 잠정 canonicalizer 채택은 **운영자 결정이었는가, 아니면
> 저작 세션의 사후 귀속인가?**

| 항목 | 내용 |
|---|---|
| **상태** | **PENDING — 운영자 응답 대기** |
| **왜 물어야 하는가** | 귀속 근거가 순환이다(§1.2-b). 리포 안 3개 표면이 전부 같은 저작 세션의 산출물이고, 운영자 응답은 어떤 표면에도 없다 |
| **가능한 처분 (a)** | **운영자 결정이었다** ⇒ 본 문서 §1.1을 D1/D3 형식(축자 문언 + 응답 표면 지목)으로 정정하고 귀속을 확정한다 |
| **가능한 처분 (b)** | **사후 귀속이었다** ⇒ 두 갈래. **(b-1) 사후 비준** — 운영자가 지금 채택을 승인하고 그 사실을 이 문서에 기록(비준 일자 = 오늘). **(b-2) 철회** — 채택을 무효화하고 D5를 미결로 되돌린다. 이 경우 `tools/bcp_digest.py`의 D5 귀속 문구 4곳(`:8`·`:388`·`:996` + 테스트 `:4`)과 `tests/tools/test_bcp_digest.py:1157`의 단정을 함께 정정해야 한다(도구 자체의 존치 여부는 별개 판단 — 코드는 유효하고 귀속 문언만 거짓이기 때문) |
| **비준 전 지위** | `ev-l1-provisional-0` = **세션-집행 잠정 도구**. "운영자 승인된 수단"으로 인용 금지. **비프로덕션 라벨은 (a)·(b) 어느 쪽으로 판명되든 유지된다** — 라벨은 귀속이 아니라 알고리즘의 성격(G2 미결)에서 나오기 때문 |
| **차단 범위** | 이 게이트는 **P0-2 T4를 차단하지 않는다.** digest 기입은 어차피 승인-시점 행위(§1.3-1)이고 현재 3필드는 `TBD`다. 차단하는 것은 **"운영자가 승인한 canonicalizer"라는 서술의 사용**뿐이다 |

**D6은 이 게이트의 대상이 아니다** — §2.2-1에서 운영자 행위가 식별됐다. 단 그 행위가 **리포 외 대화**에만
존재한다는 사실은 §2.2 정직 표기 1번에 남겨 둔다.

> **비차단 후속(선택)**: D6 귀속의 **리포-단독 재검증 불가** 상태는 운영자 countersign 한 줄이면 해소된다
> — 본 문서 §2.2-1에 "2026-07-29 MCP 재가동 보고는 내가 한 것이 맞다" 취지의 서명을 덧붙이면 리포 안에
> 1차 표면이 생긴다. **차단 사항이 아니다**(D6 처분 자체는 유효). 운영자가 D5 게이트에 응답하는 김에
> 함께 처리하면 편할 뿐이다.

### 3-2. 열지 않는 것 (P0-2 잔여)

T3의 두 항목이 처분됐어도 P0-2는 종결되지 않는다. 실행계획 §1 T4(승인 배선)가 남는다:

1. B-6 required-capability-set / minimum-live-gate 매핑 저작.
2. 프로브 결과 반영 → INSTANCE `DRAFT` 해제 패키지 → 독립 리뷰(D1 혼합 방식) → 운영자 승인(`approvers[]`).
3. `VERIFICATION-PROFILE-002.yaml` `scope.broker_capability_profiles` 링크 + bounds 값 기입
   (Bounds-Approver 소관).
4. `EVIDENCE-REGISTER-002.csv`의 `pending-P0-2` **64행** 해소 → P0-2 종결.

digest 3필드(§1.4)의 실제 기입은 **2번 단계의 인간 저작 행위**로 발생한다.

---

## 4. 참조 좌표 요약 (재검증용)

| 항목 | 미결 표면 | **귀속 근거** | 집행 좌표 | 커밋 |
|---|---|---|---|---|
| **D5** | 실행계획 `:44-48` | **없음 — 미확인(§1.2-b).** 리포 내 3표면(`bcp_digest.py:7-8`·커밋 `30ad565c` 본문·`test_bcp_digest.py:4`)은 전부 동일 저작 세션 산출물 | `tools/bcp_digest.py:4-11`·`:23-24`·`:311`·`:317`·`:566` / `tests/tools/test_bcp_digest.py:19-27`·`:173-261` / draft YAML 두 `profile_identity` 블록(digest 3필드 TBD 유지 — §1.4 드리프트 주의) | `30ad565c` (2026-07-30) |
| **D6** | 실행계획 `:44`·`:49-50` | **2026-07-29 운영자의 MCP 재가동 보고 — 대화 수준·리포 외**(§2.2-1). 리포 내 사본 없음 | `docs/plans/2026-07-29-tos-p02-n17-spec-collation.md:5`(수단)·`:116-121`(집계)·`:132-140`(후속) / `docs/runbooks/kis-capability-probes.md` §7(정본 체크리스트) / `tools/broker_probes/registry.py` N-17 `skip_reason`(2026-08-07 처분 반영) | `239b175a` (2026-07-29) |
| 미결 유지 | — | — | G2 프로덕션 canonicalization: 초안 메모 `:284`(U-1) + 실행계획 `:46` | — |
| **신설 게이트** | — | — | **D5 귀속 확인 — §3-1 (PENDING)** | — |

**선례 대조**: D1/D3 기록은 `role-scheme-and-disposition.md:11`·`:42`에서 운영자 문언을 **축자 인용**하고
register `human-gate-register.md` §11 `:287-313`을 **응답 표면으로 지목**한다. 그 §11의 즉시 결정 항목은
**D1~D4 4건**이며 **D5·D6은 등재돼 있지 않다** — 두 항목이 별도 경로로 흘렀다는 구조적 사실이고,
D5의 귀속 공백이 발생한 지점이기도 하다.
