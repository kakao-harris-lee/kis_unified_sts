"""tos_runtime test package marker (mirrors ``tos/tests/__init__.py``'s
rationale: makes ``tos/runtime/tests`` importable as a package so future test
modules can share fixtures/strategies via relative imports).
"""
