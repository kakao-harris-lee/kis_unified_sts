# Operator-attested "pending" Safety Currentness Vector dimensions
# (tos_runtime.compose._pending_dimensions).
#
# tos.cur.predicates.vector_complete floors its mandated dimension set to
# MANDATED_DIMENSION_FLOOR (every non-conditional tos.cur.DimensionKey
# member). This composition's own live services structurally establish
# exactly four of them (COMMIT_LOG, TRUSTWORTHY_TIME, SAFETY_AUTHORITY,
# ACTION_FLOW). The 17 below have NO runtime owner in Phase 2 — no lane
# P/Q/R/S service computes a real positively_established verdict for them.
#
# Every field below is a named-TBD `null` in this example — an operator
# MUST fill in a concrete value for every field of every dimension before
# compose_paper_runtime will start; a still-null field refuses composition
# at startup (fail-closed, never a silent default).
#
# Each dimension's owner_identity is stamped
# "operator-attestation-pending-phase-5" by compose itself (not configured
# here) so a reader of the assembled vector can see, structurally, that
# these coordinates are an interim operator sign-off standing in for a
# real Phase 5 runtime owner's verdict — never presented as if a runtime
# owner produced them.

# owner note: Phase 5 (a real Aggregate Risk currentness owner) replaces this attestation
AGGREGATE_RISK:
  bound_generation: null
  bound_digest: null
  restrictive_floor: null
  positively_established: null
# owner note: Phase 5 (a real Order Construction Policy constraint owner) replaces this attestation
CONSTRAINT:
  bound_generation: null
  bound_digest: null
  restrictive_floor: null
  positively_established: null
# owner note: Phase 5 (a real Order Construction Policy owner) replaces this attestation
CONSTRUCTION:
  bound_generation: null
  bound_digest: null
  restrictive_floor: null
  positively_established: null
# owner note: Phase 5 (a real decision-context owner) replaces this attestation
CONTEXT:
  bound_generation: null
  bound_digest: null
  restrictive_floor: null
  positively_established: null
# owner note: Phase 5 (a real critical-input snapshot owner) replaces this attestation
CRITICAL_INPUT:
  bound_generation: null
  bound_digest: null
  restrictive_floor: null
  positively_established: null
# CURRENTNESS_POLICY: Phase 5 W3-b landed a real dimension_readers entry
# (tos_runtime.compose._currentness_wiring._currentness_policy_dimension_reader_for,
# over tos.cur.predicates.policy_covers_mandated_dimensions) — this block is
# REMOVED, never left null; a config that still carries a CURRENTNESS_POLICY
# key here now refuses to load (tos_runtime.compose._pending_dimensions
# _READER_OWNED_DIMENSION_KEYS).
# owner note: Phase 5 (a real decision-proof-intent owner) replaces this attestation
DECISION_PROOF_INTENT:
  bound_generation: null
  bound_digest: null
  restrictive_floor: null
  positively_established: null
# DEVIATION: Phase 5 W3-a1 landed a real dimension_readers entry
# (tos_runtime.safety.deviation.DeviationService, via
# tos_runtime.compose._safety_wiring.build_safety_mesh) — this block is
# REMOVED, never left null; a config that still carries a DEVIATION key
# here now refuses to load (tos_runtime.compose._pending_dimensions
# _READER_OWNED_DIMENSION_KEYS).
# owner note: Phase 5 (a real egress-identity owner) replaces this attestation.
# W3.1 independent review MEDIUM-4 (2026-09-11): a reader briefly landed here
# using only credential_route_authority_disjoint (1 of 3 kernel predicates) and
# was reverted -- asserting positively_established on 1/3 coverage was an
# over-claim. The one real predicate is now an evidence-only observation
# (_wiring.py's _record_egress_identity_observation), never a dimension verdict.
EGRESS_IDENTITY:
  bound_generation: null
  bound_digest: null
  restrictive_floor: null
  positively_established: null
# ENVIRONMENT_SCOPE: Phase 5 W3-b landed a real dimension_readers entry
# (tos_runtime.compose._currentness_wiring._environment_scope_dimension_reader_for,
# over tos.brokercap.predicates.environment_binding_ok) — this block is
# REMOVED, never left null; a config that still carries an ENVIRONMENT_SCOPE
# key here now refuses to load (tos_runtime.compose._pending_dimensions
# _READER_OWNED_DIMENSION_KEYS).
# INCIDENT: Phase 5 W3-a2 landed a real dimension_readers entry
# (tos_runtime.safety.incident.IncidentService, via
# tos_runtime.compose._safety_wiring.build_safety_mesh) — this block is
# REMOVED, never left null; a config that still carries an INCIDENT key
# here now refuses to load (tos_runtime.compose._pending_dimensions
# _READER_OWNED_DIMENSION_KEYS).
# MONITORING: Phase 5 W3-a2 landed a real dimension_readers entry
# (tos_runtime.safety.monitoring.MonitoringService, via
# tos_runtime.compose._safety_wiring.build_safety_mesh) — this block is
# REMOVED, never left null; a config that still carries a MONITORING key
# here now refuses to load (tos_runtime.compose._pending_dimensions
# _READER_OWNED_DIMENSION_KEYS).
# owner note: Phase 5 (a real post-trade owner) replaces this attestation
POST_TRADE:
  bound_generation: null
  bound_digest: null
  restrictive_floor: null
  positively_established: null
# RECOVERY: Phase 5 W3-b landed a real dimension_readers entry
# (tos_runtime.compose._currentness_wiring._recovery_dimension_reader_for,
# over the TOS Phase 5 W1 recovery barrier's own RecoveryVerdict) — this
# block is REMOVED, never left null; a config that still carries a RECOVERY
# key here now refuses to load (tos_runtime.compose._pending_dimensions
# _READER_OWNED_DIMENSION_KEYS).
# owner note: Phase 5 (a real release-admission currentness owner) replaces this attestation
RELEASE:
  bound_generation: null
  bound_digest: null
  restrictive_floor: null
  positively_established: null
# SAFETY_ENVELOPE_PROFILE: Phase 5 W3-a1 landed a real dimension_readers entry
# (tos_runtime.safety.profile.SafetyProfileService, via
# tos_runtime.compose._safety_wiring.build_safety_mesh) — this block is
# REMOVED, never left null; a config that still carries a
# SAFETY_ENVELOPE_PROFILE key here now refuses to load
# (tos_runtime.compose._pending_dimensions _READER_OWNED_DIMENSION_KEYS).
# TRADING_APPROVAL: Phase 5 W3-b landed a real dimension_readers entry
# (tos_runtime.compose._currentness_wiring._trading_approval_dimension_reader_for,
# over step 4's own recorded IndependentApprovalStage verdict) — this block
# is REMOVED, never left null; a config that still carries a
# TRADING_APPROVAL key here now refuses to load
# (tos_runtime.compose._pending_dimensions _READER_OWNED_DIMENSION_KEYS).
