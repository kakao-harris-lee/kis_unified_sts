# Grafana Dashboard Provisioning
# 대시보드 자동 프로비저닝 설정

apiVersion: 1

providers:
  - name: 'KIS Trading Dashboards'
    orgId: 1
    folder: 'Trading'
    folderUid: 'trading'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
