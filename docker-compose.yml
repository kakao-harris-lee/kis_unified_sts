# KIS Unified Trading Platform - Docker Compose
# 개발/운영 환경 통합 구성

# NOTE: REDIS_PASSWORD uses ${VAR-default} (no colon) on purpose — an explicitly
# empty REDIS_PASSWORD must pass through as "no auth" (e.g. the paper stack points
# at a password-less host redis via host.docker.internal). With ${VAR:-default} an
# empty value would become "changeme" and AUTH would fail against that server.
# Unset still defaults to changeme. Do NOT re-add the colon.
x-redis-runtime-env: &redis-runtime-env
  REDIS_HOST: "${REDIS_HOST:-redis}"
  REDIS_PORT: "${REDIS_PORT:-6379}"
  REDIS_DB: "${REDIS_DB:-1}"
  REDIS_STOCK_DB: "${REDIS_STOCK_DB:-1}"
  REDIS_FUTURES_DB: "${REDIS_FUTURES_DB:-1}"
  REDIS_SYSTEM_DB: "${REDIS_SYSTEM_DB:-1}"
  REDIS_PASSWORD: "${REDIS_PASSWORD-changeme}"
  REDIS_URL: "${REDIS_URL:-redis://:${REDIS_PASSWORD-changeme}@redis:6379/1}"
  REDIS_TLS_ENABLED: "${REDIS_TLS_ENABLED:-false}"
  REDIS_TLS_CERT_REQS: "${REDIS_TLS_CERT_REQS:-required}"
  REDIS_TLS_CA_CERTS: "${REDIS_TLS_CA_CERTS:-}"

x-runtime-storage-env: &runtime-storage-env
  ENVIRONMENT: "${ENVIRONMENT:-dev}"
  RUNTIME_STORAGE_BACKEND: "${RUNTIME_STORAGE_BACKEND:-sqlite}"
  RUNTIME_STORAGE_SQLITE_PATH: "${RUNTIME_STORAGE_SQLITE_PATH:-data/runtime/dev/runtime.db}"
  RUNTIME_STORAGE_SQLITE_WAL: "${RUNTIME_STORAGE_SQLITE_WAL:-true}"
  RUNTIME_STORAGE_SQLITE_BUSY_TIMEOUT_MS: "${RUNTIME_STORAGE_SQLITE_BUSY_TIMEOUT_MS:-5000}"
  RUNTIME_STORAGE_SQLITE_SYNCHRONOUS: "${RUNTIME_STORAGE_SQLITE_SYNCHRONOUS:-NORMAL}"
  MARKET_DATA_SOURCE: "${MARKET_DATA_SOURCE:-parquet}"
  MARKET_DATA_PARQUET_ROOT: "${MARKET_DATA_PARQUET_ROOT:-data/market}"
  DASHBOARD_TRADE_STATS_SOURCE: "${DASHBOARD_TRADE_STATS_SOURCE:-runtime_ledger}"

x-kis-runtime-env: &kis-runtime-env
  KIS_APP_KEY: "${KIS_APP_KEY:-}"
  KIS_APP_SECRET: "${KIS_APP_SECRET:-}"
  KIS_STOCK_APP_KEY: "${KIS_STOCK_APP_KEY:-}"
  KIS_STOCK_APP_SECRET: "${KIS_STOCK_APP_SECRET:-}"
  KIS_FUTURES_APP_KEY: "${KIS_FUTURES_APP_KEY:-}"
  KIS_FUTURES_APP_SECRET: "${KIS_FUTURES_APP_SECRET:-}"
  # Account numbers pair with the app keys — KISClient._resolve_account_no reads
  # KIS_{STOCK,FUTURES}_ACCOUNT_NO (fallback KIS_ACCOUNT_NO) and warns if absent.
  KIS_STOCK_ACCOUNT_NO: "${KIS_STOCK_ACCOUNT_NO:-}"
  KIS_FUTURES_ACCOUNT_NO: "${KIS_FUTURES_ACCOUNT_NO:-}"
  KIS_ACCOUNT_NO: "${KIS_ACCOUNT_NO:-}"
  KIS_ACCOUNT_PRODUCT_CODE: "${KIS_ACCOUNT_PRODUCT_CODE:-01}"
  KIS_IS_REAL: "${KIS_IS_REAL:-false}"
  KIS_REAL_TRADING: "${KIS_REAL_TRADING:-false}"
  KIS_MARKET: "${KIS_MARKET:-mock}"
  KIS_STOCK_MARKET: "${KIS_STOCK_MARKET:-mock}"
  KIS_FUTURES_MARKET: "${KIS_FUTURES_MARKET:-real}"
  KIS_TOKEN_CACHE_DIR: "${KIS_TOKEN_CACHE_DIR:-/app/.cache}"

x-alert-runtime-env: &alert-runtime-env
  TELEGRAM_BOT_TOKEN: "${TELEGRAM_BOT_TOKEN:-}"
  TELEGRAM_CHAT_ID: "${TELEGRAM_CHAT_ID:-}"
  TELEGRAM_STOCK_BOT_TOKEN: "${TELEGRAM_STOCK_BOT_TOKEN:-}"
  TELEGRAM_STOCK_CHAT_ID: "${TELEGRAM_STOCK_CHAT_ID:-}"
  TELEGRAM_FUTURES_BOT_TOKEN: "${TELEGRAM_FUTURES_BOT_TOKEN:-}"
  TELEGRAM_FUTURES_CHAT_ID: "${TELEGRAM_FUTURES_CHAT_ID:-}"
  TELEGRAM_BRIEFING_BOT_TOKEN: "${TELEGRAM_BRIEFING_BOT_TOKEN:-}"
  TELEGRAM_BRIEFING_CHAT_ID: "${TELEGRAM_BRIEFING_CHAT_ID:-}"

x-trading-runtime-volumes: &trading-runtime-volumes
  - ./config:/app/config:ro
  - ./logs:/app/logs
  # KIS token cache. The host ./.cache dir must be owned by uid 1000 (the
  # container's appuser) so tokens persist across restarts. It is tracked via
  # .cache/.gitkeep so a fresh checkout has it deploy-owned; if it is ever
  # missing, `docker compose up` auto-creates it as root and token saves fail.
  - ./.cache:/app/.cache
  - ./data/runtime:/app/data/runtime
  - ./data/market:/app/data/market:ro

x-pipeline-service: &pipeline-service
  build:
    context: .
    dockerfile: Dockerfile
  restart: unless-stopped
  extra_hosts:
    - "host.docker.internal:host-gateway"
  volumes: *trading-runtime-volumes
  depends_on:
    redis:
      condition: service_healthy
  networks:
    - trading-network
  logging:
    driver: "json-file"
    options:
      max-size: "10m"
      max-file: "3"

services:
  # =============================================================================
  # Trading loop daemon (optional profile)
  # =============================================================================
  trader:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-trader
    restart: unless-stopped
    profiles: ["trading"]
    command: ["bash", "scripts/docker/trading_loop_entrypoint.sh"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env, *kis-runtime-env, *alert-runtime-env]
      OPENAI_API_KEY: "${OPENAI_API_KEY:-}"
      # `sts` is a console script (from cli.main import main); pip install . does not
      # make /app importable for it, so put the repo root on the path. (stock daemons
      # use `python -m services.*` which gets /app via cwd, so they don't need this.)
      PYTHONPATH: "/app"
      # Korea-only market: run the orchestrator in KST so the market-open/session
      # window is computed correctly. In UTC the 09:00 open resolves to 18:00 KST
      # and the loop "waits for market open" all session (CLAUDE.md §5).
      TZ: "Asia/Seoul"
      TRADING_MODE: "${TRADING_MODE:-paper}"
      TRADING_ASSET_CLASS: "${TRADING_ASSET_CLASS:-stock}"
      TRADING_STRATEGY: "${TRADING_STRATEGY:-}"
      TRADING_INITIAL_CAPITAL: "${TRADING_INITIAL_CAPITAL:-10000000}"
      TRADING_RUN_MODE: "${TRADING_RUN_MODE:-daemon}"
      TRADING_LIVE_CONFIRM: "${TRADING_LIVE_CONFIRM:-}"
    volumes: *trading-runtime-volumes
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - trading-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =============================================================================
  # Futures trading loop daemon (orchestrator path; asset-pinned).
  # Mirror of `trader` but TRADING_ASSET_CLASS=futures. paper/live is the STACK
  # distinction (TRADING_MODE / KIS_REAL_TRADING), NOT hardcoded — the same
  # service runs in the paper stack and the live stack. Run both assets:
  #   docker compose --profile trading up -d trader trader-futures
  # =============================================================================
  trader-futures:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-trader-futures
    restart: unless-stopped
    profiles: ["trading"]
    command: ["bash", "scripts/docker/trading_loop_entrypoint.sh"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env, *kis-runtime-env, *alert-runtime-env]
      OPENAI_API_KEY: "${OPENAI_API_KEY:-}"
      # `sts` is a console script (from cli.main import main); pip install . does not
      # make /app importable for it, so put the repo root on the path. (stock daemons
      # use `python -m services.*` which gets /app via cwd, so they don't need this.)
      PYTHONPATH: "/app"
      # Korea-only market: run the orchestrator in KST so the market-open/session
      # window is computed correctly. In UTC the 09:00 open resolves to 18:00 KST
      # and the loop "waits for market open" all session (CLAUDE.md §5).
      TZ: "Asia/Seoul"
      TRADING_MODE: "${TRADING_MODE:-paper}"
      TRADING_ASSET_CLASS: "futures"
      TRADING_STRATEGY: "${FUTURES_TRADING_STRATEGY:-}"
      TRADING_INITIAL_CAPITAL: "${TRADING_INITIAL_CAPITAL:-10000000}"
      TRADING_RUN_MODE: "${TRADING_RUN_MODE:-daemon}"
      TRADING_LIVE_CONFIRM: "${TRADING_LIVE_CONFIRM:-}"
      # Futures product: "mini" (default, live A05…) or "kospi200" (full F200 A01…,
      # tighter spread for paper signal validation). FUTURES_SLIPPAGE_TICK_SIZE
      # MUST match the product (mini 0.02 / F200 0.05) or the spread guard
      # (spread_points / tick_size) blocks every entry. MAX_SPREAD_TICKS widens the
      # paper spread cap (F200 ~10 ticks vs mini ~13; default 6 blocks both).
      FUTURES_TRADING_PRODUCT: "${FUTURES_TRADING_PRODUCT:-mini}"
      FUTURES_SLIPPAGE_TICK_SIZE: "${FUTURES_SLIPPAGE_TICK_SIZE:-0.02}"
      FUTURES_PAPER_MAX_SPREAD_TICKS: "${FUTURES_PAPER_MAX_SPREAD_TICKS:-6}"
    volumes: *trading-runtime-volumes
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - trading-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =============================================================================
  # Stock decoupled pipeline daemons (Compose-managed M4/M5)
  # =============================================================================
  stock-market-ingest:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-stock-market-ingest
    profiles: ["stock-ingest"]
    command: ["python", "-m", "services.market_ingest.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env, *kis-runtime-env]
      # KRX market-hours logging: container TZ was UTC causing 9h log timestamp
      # offset (misdiagnosis risk). Code uses now_kst()/datetime.now(UTC) so this
      # is logging-only; no functional change.
      TZ: "Asia/Seoul"
      INGEST_ASSET: "stock"
      INGEST_MAX_SYMBOLS: "${INGEST_MAX_SYMBOLS:-40}"
      INGEST_REFRESH_SECONDS: "${INGEST_REFRESH_SECONDS:-30}"
      # Cold-start feed-start retry backoff (exponential, capped). A KIS
      # /oauth2/Approval outage at start() is non-fatal: the daemon stays up,
      # REST fallback covers the session, and start() retries on this schedule
      # instead of crash-looping the container.
      INGEST_FEED_START_RETRY_INITIAL_SECONDS: "${INGEST_FEED_START_RETRY_INITIAL_SECONDS:-5}"
      INGEST_FEED_START_RETRY_MAX_SECONDS: "${INGEST_FEED_START_RETRY_MAX_SECONDS:-60}"
      TRADE_TARGETS_LATEST_KEY: "${TRADE_TARGETS_LATEST_KEY:-system:trade_targets:latest}"

  stock-strategy:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-stock-strategy
    profiles: ["stock-pipeline"]
    command: ["python", "-m", "services.stock_strategy.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env, *kis-runtime-env]
      TZ: "Asia/Seoul"
      STOCK_STRATEGY_DAEMON: "${STOCK_PIPELINE_MODE:-shadow}"
      STOCK_TICK_STREAM: "${STOCK_TICK_STREAM:-market:ticks}"
      STOCK_WATCHLIST_KEY: "${STOCK_WATCHLIST_KEY:-system:daily_watchlist:latest}"
      STOCK_MAX_SYMBOLS: "${STOCK_MAX_SYMBOLS:-40}"
      # Emit buy signals directly from LLM-discovered trade targets (fast path).
      # Default on here so LLM picks trade in paper/shadow; override per env.
      STOCK_LLM_DISCOVERY_SIGNALS_ENABLED: "${STOCK_LLM_DISCOVERY_SIGNALS_ENABLED:-true}"

  stock-risk-filter:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-stock-risk-filter
    profiles: ["stock-pipeline"]
    command: ["python", "-m", "services.stock_risk_filter.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env]
      TZ: "Asia/Seoul"
      STOCK_RISK_FILTER: "${STOCK_PIPELINE_MODE:-shadow}"
      STOCK_POSITIONS_KEY: "${STOCK_POSITIONS_KEY:-stock:daemon:positions}"

  stock-order-router:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-stock-order-router
    profiles: ["stock-pipeline"]
    command: ["python", "-m", "services.stock_order_router.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env]
      TZ: "Asia/Seoul"
      STOCK_ORDER_ROUTER: "${STOCK_PIPELINE_MODE:-shadow}"
      STOCK_POSITIONS_KEY: "${STOCK_POSITIONS_KEY:-stock:daemon:positions}"
      STOCK_PAPER_SLIPPAGE_RATE: "${STOCK_PAPER_SLIPPAGE_RATE:-0.0001}"

  stock-exit:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-stock-exit
    profiles: ["stock-pipeline"]
    command: ["python", "-m", "services.stock_exit.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env]
      TZ: "Asia/Seoul"
      STOCK_EXIT_DAEMON: "${STOCK_PIPELINE_MODE:-shadow}"
      STOCK_POSITIONS_KEY: "${STOCK_POSITIONS_KEY:-stock:daemon:positions}"
      STOCK_TICK_STREAM: "${STOCK_TICK_STREAM:-market:ticks}"
      STOCK_EXIT_INTERVAL: "${STOCK_EXIT_INTERVAL:-5}"
      STOCK_PAPER_SLIPPAGE_RATE: "${STOCK_PAPER_SLIPPAGE_RATE:-0.0001}"

  stock-monitor:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-stock-monitor
    profiles: ["stock-pipeline"]
    command: ["python", "-m", "services.stock_monitor.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env, *alert-runtime-env]
      TZ: "Asia/Seoul"
      STOCK_MONITOR_DAEMON: "${STOCK_PIPELINE_MODE:-shadow}"
      STOCK_POSITIONS_KEY: "${STOCK_POSITIONS_KEY:-stock:daemon:positions}"
      STOCK_TICK_STREAM: "${STOCK_TICK_STREAM:-market:ticks}"
      STOCK_MONITOR_STATUS_INTERVAL: "${STOCK_MONITOR_STATUS_INTERVAL:-5}"
      STOCK_MONITOR_SIGNAL_META_MAX: "${STOCK_MONITOR_SIGNAL_META_MAX:-1000}"
      TRADING_STATE_KEY_SUFFIX: "${STOCK_TRADING_STATE_KEY_SUFFIX:-}"

  # =============================================================================
  # Futures decoupled pipeline daemons (Compose-managed, DORMANT by default).
  # Profile-gated: nothing starts unless a futures-* profile is passed to
  # `docker compose up`. Modes default to the SAFE end (shadow / paper). Cutover
  # is operator-gated (Phase-5 Gate 1-3 + written approval) — see
  # docs/runbooks/futures-pipeline-cutover-f9.md. Double-trade guard: set
  # FUTURES_ORCHESTRATOR_ENABLED=false on trader-futures at cutover.
  #
  #   Shadow run (reuses trader-futures' raw_data; NO ingest):
  #     docker compose --profile futures-pipeline up -d \
  #       futures-decision-engine futures-risk-filter futures-order-router futures-monitor
  #   Cutover (live): also bring up futures-ingest + futures-killswitch and set
  #     FUTURES_PIPELINE_MODE=live FUTURES_ORDER_ROUTER_MODE=live.
  # =============================================================================
  futures-market-ingest:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-futures-market-ingest
    profiles: ["futures-ingest"]
    command: ["python", "-m", "services.market_ingest.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env, *kis-runtime-env]
      INGEST_ASSET: "futures"
      INGEST_REFRESH_SECONDS: "${FUTURES_INGEST_REFRESH_SECONDS:-3600}"
      # Cold-start feed-start retry backoff (see stock-market-ingest above):
      # a KIS approval/connect failure at start() retries instead of crashing.
      INGEST_FEED_START_RETRY_INITIAL_SECONDS: "${INGEST_FEED_START_RETRY_INITIAL_SECONDS:-5}"
      INGEST_FEED_START_RETRY_MAX_SECONDS: "${INGEST_FEED_START_RETRY_MAX_SECONDS:-60}"

  futures-decision-engine:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-futures-decision-engine
    profiles: ["futures-pipeline"]
    command: ["python", "-m", "services.decision_engine.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env]
      FUTURES_STRATEGY_DAEMON: "${FUTURES_PIPELINE_MODE:-shadow}"
      FUTURES_STRATEGY_SYMBOL: "${FUTURES_STRATEGY_SYMBOL:-}"
      FUTURES_TICK_STREAM: "${FUTURES_TICK_STREAM:-raw_data}"

  futures-risk-filter:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-futures-risk-filter
    profiles: ["futures-pipeline"]
    command: ["python", "-m", "services.risk_filter.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env]
      FUTURES_RISK_FILTER: "${FUTURES_PIPELINE_MODE:-shadow}"

  futures-order-router:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-futures-order-router
    profiles: ["futures-pipeline"]
    command: ["python", "-m", "services.order_router.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env, *kis-runtime-env]
      FUTURES_ORDER_ROUTER: "${FUTURES_ORDER_ROUTER_MODE:-paper}"
      # Publishes the kill-switch api_error_rate metric for the decoupled futures
      # pipeline under its own source key (…:order_router), fed by OrderExecutor
      # REST outcomes. Opt out via ORDER_ROUTER_PUBLISH_KIS_ERROR_RATE=false.
      KIS_ERROR_RATE_SOURCE: "order_router"
      # Executor real/paper gate (config/execution.yaml::execution.trading_mode
      # reads ${TRADING_MODE}). Dedicated knob, safe default PAPER — distinct from
      # the orchestrator's paper/live TRADING_MODE space (executor enum is
      # PAPER/MOCK/REAL). LIVE order placement requires FUTURES_EXECUTOR_TRADING_MODE=REAL.
      TRADING_MODE: "${FUTURES_EXECUTOR_TRADING_MODE:-PAPER}"

  futures-monitor:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-futures-monitor
    profiles: ["futures-pipeline"]
    command: ["python", "-m", "services.futures_monitor.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env, *alert-runtime-env]
      FUTURES_MONITOR_DAEMON: "${FUTURES_PIPELINE_MODE:-shadow}"
      FUTURES_MONITOR_STATUS_INTERVAL: "${FUTURES_MONITOR_STATUS_INTERVAL:-5}"
      FUTURES_TICK_STREAM: "${FUTURES_TICK_STREAM:-raw_data}"

  futures-kill-switch:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-futures-kill-switch
    profiles: ["futures-killswitch"]
    command: ["python", "-m", "services.kill_switch.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env, *alert-runtime-env]
      KIS_FUTURES_EQUITY_KRW: "${KIS_FUTURES_EQUITY_KRW:-100000000}"

  # =============================================================================
  # Scheduler — supercronic runs the one-shot KIS jobs that used to live in the
  # host crontab (scanners, backfills, briefings, macro, forecasting refit,
  # verifications). Profile-gated; DO NOT start it while the host crontab still
  # runs the same jobs (double-execution). Cutover is off-hours after disabling
  # the host KIS crons — docs/plans/archive/2026-06-09-cron-to-compose-scheduler.md.
  # Market-hours daemons (screener/fusion) are separate services (PR-C).
  # =============================================================================
  scheduler:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-scheduler
    profiles: ["scheduler"]
    # tini as PID 1 — supercronic's process reaper fatals ("Failed to fork
    # exec") when it is PID 1; as a child it reads the crontab fine.
    init: true
    command: ["supercronic", "/app/deploy/scheduler.crontab"]
    # The scheduler runs the EOD backfills (stock-backfill / backfill) + the
    # daily_indicator_scanner candidate backfill, which WRITE parquet to
    # data/market, and the verification/counterfactual/rotate jobs, which WRITE
    # under reports/. The shared *pipeline-service mount is data/market:ro (for
    # read-only consumers) and does not mount reports at all — override volumes
    # here so both are writable; otherwise those jobs hit a read-only FS
    # (data/market) or write to ephemeral container-local paths (reports/).
    volumes:
      - ./config:/app/config:ro
      - ./logs:/app/logs
      - ./.cache:/app/.cache
      - ./data/runtime:/app/data/runtime
      - ./data/market:/app/data/market
      - ./reports:/app/reports
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env, *kis-runtime-env, *alert-runtime-env]
      OPENAI_API_KEY: "${OPENAI_API_KEY:-}"
      KRX_API_KEY: "${KRX_API_KEY:-}"
      DART_API_KEY: "${DART_API_KEY:-}"
      ECOS_API_KEY: "${ECOS_API_KEY:-}"
      PYTHONPATH: "/app"
      TZ: "Asia/Seoul"

  # =============================================================================
  # Producers — market-hours daemons that feed the trading pipeline
  # (system:universe / dip_candidates / trade_targets). Self-gate to the KRX
  # regular session (idle off-hours), so they can run as always-on services.
  # Profile-gated; cutover off-hours alongside the scheduler (host crons run the
  # same daemons until then). cron→compose migration PR-C.
  # =============================================================================
  screener:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-screener
    profiles: ["producers"]
    command: ["python", "-m", "services.screener"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env, *kis-runtime-env, *alert-runtime-env]
      TZ: "Asia/Seoul"
      # Publishes the kill-switch api_error_rate metric under its own source key
      # (kill_switch:metrics:api_error_rate_5min:screener). Opt out via
      # SCREENER_PUBLISH_KIS_ERROR_RATE=false.
      KIS_ERROR_RATE_SOURCE: "screener"

  theme-discovery:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-theme-discovery
    profiles: ["producers"]
    command: ["python", "-m", "services.theme_discovery"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env, *kis-runtime-env, *alert-runtime-env]
      TZ: "Asia/Seoul"

  fusion-ranker:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-fusion-ranker
    profiles: ["producers"]
    command: ["python", "-m", "services.fusion_ranker"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env, *kis-runtime-env, *alert-runtime-env]
      TZ: "Asia/Seoul"

  # =============================================================================
  # News / LLM stream pipeline — long-running daemons that used to be systemd
  # units. Profile-gated to avoid accidental external API / LLM spend; run the
  # same service definitions in paper and live stacks with stack-specific env:
  #   docker compose --env-file .env.paper --profile news up -d news-collector news-scorer
  #   docker compose --env-file .env.live  --profile news up -d news-collector news-scorer
  # =============================================================================
  news-collector:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-news-collector
    profiles: ["news"]
    command: ["python", "-m", "services.news_collector.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env]
      PYTHONPATH: "/app"
      TZ: "Asia/Seoul"
      DART_API_KEY: "${DART_API_KEY:-}"
      MARKETAUX_API_TOKEN: "${MARKETAUX_API_TOKEN:-}"
      NAVER_SEARCH_CLIENT_ID: "${NAVER_SEARCH_CLIENT_ID:-}"
      NAVER_SEARCH_CLIENT_SECRET: "${NAVER_SEARCH_CLIENT_SECRET:-}"

  news-scorer:
    <<: *pipeline-service
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-news-scorer
    profiles: ["news"]
    command: ["python", "-m", "services.news_scorer.main"]
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env]
      PYTHONPATH: "/app"
      TZ: "Asia/Seoul"
      OPENAI_API_KEY: "${OPENAI_API_KEY:-}"
      NEWS_SCORER_CONSUMER_GROUP: "${NEWS_SCORER_CONSUMER_GROUP:-news_scorer-v1}"

  # =============================================================================
  # 대시보드 API
  # =============================================================================
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile.dashboard
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-dashboard
    restart: unless-stopped
    expose:
      - "8001"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env]
      API_KEY: "${API_KEY:-}"
      DASHBOARD_REQUIRE_AUTH: "${DASHBOARD_REQUIRE_AUTH:-true}"
      DASHBOARD_API_KEY: "${DASHBOARD_API_KEY:-}"
      DASHBOARD_DEV_MODE: "${DASHBOARD_DEV_MODE:-false}"
      DASHBOARD_KRX_NAME_FALLBACK: "${DASHBOARD_KRX_NAME_FALLBACK:-true}"
      KRX_API_KEY: "${KRX_API_KEY:-}"
      METRICS_REQUIRE_AUTH: "${METRICS_REQUIRE_AUTH:-false}"
      TRADING_ASSET_CLASS: "${TRADING_ASSET_CLASS:-stock}"
    volumes:
      - ./config:/app/config:ro
      # Builder→paper registration drop point: the only path the dashboard
      # *writes* under /app/config. This nested rw bind overrides the :ro parent
      # for that subtree so POST /api/kis-builder/register-paper (and the
      # enable/unregister endpoints) can materialize built/<id>.yaml. Trading
      # services read it back through their own ./config:ro mount (same host dir).
      - ./config/strategies/built:/app/config/strategies/built
      - ./logs:/app/logs
      - ./reports:/app/reports:ro
      - ./data/runtime:/app/data/runtime
      - ./data/market:/app/data/market:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - trading-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =============================================================================
  # Strategy Builder UI (visual strategy builder, Next.js)
  # =============================================================================
  strategy-builder-ui:
    build:
      context: .
      dockerfile: Dockerfile.strategy_builder_ui
      args:
        NEXT_PUBLIC_API_KEY: "${NEXT_PUBLIC_API_KEY:-${DASHBOARD_API_KEY:-}}"
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-strategy-builder-ui
    restart: unless-stopped
    # External access goes through caddy on DASHBOARD_HOST_PORT. Port 3100
    # is container-internal only and must not be published on the host.
    expose:
      - "3100"
    environment:
      - KIS_BUILDER_API_BASE=http://dashboard:8001
      - KIS_BUILDER_API_KEY=${DASHBOARD_API_KEY:-}
      - NEXT_PUBLIC_API_KEY=${NEXT_PUBLIC_API_KEY:-${DASHBOARD_API_KEY:-}}
    depends_on:
      dashboard:
        condition: service_healthy
    # Healthcheck mirrors the Dockerfile so docker-compose can express
    # dependency ordering on it (caddy depends_on service_healthy).
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:3100/').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks:
      - trading-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =============================================================================
  # Caddy reverse proxy — single external entry on DASHBOARD_HOST_PORT.
  # STS API → dashboard:8001 (FastAPI, API-only): /api/kis-builder/*,
  #   /api/{trading,signals,trades,strategies,strategy-lab,strategy-builder,
  #   metrics,health}*, plus /health, /docs, /openapi.json, /redoc, /metrics.
  # Everything else → strategy-builder-ui:3100 (Next.js): UI pages, /_next/*,
  #   and the upstream-compat
  #   /api/{auth,account,orders,market,symbols,files,experiments} proxy handled
  #   by the Next.js catch-all.
  # HTTP-only; expose one host web port mapped to Caddy's internal :5080.
  # =============================================================================
  caddy:
    image: caddy:2
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-caddy
    restart: unless-stopped
    ports:
      - "${DASHBOARD_HOST_PORT:-5081}:5080"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      dashboard:
        condition: service_healthy
      strategy-builder-ui:
        condition: service_healthy
    networks:
      - trading-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =============================================================================
  # Forecasting daemon (HAR-RV vol + event impact scoring)
  # =============================================================================
  forecasting:
    build:
      context: .
      dockerfile: Dockerfile.forecasting
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-forecasting
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      <<: [*redis-runtime-env, *runtime-storage-env]
      OPENAI_API_KEY: "${OPENAI_API_KEY:-}"
    volumes:
      - ./config:/app/config:ro
      - ./logs:/app/logs
      - ./data/runtime:/app/data/runtime
      - ./data/market:/app/data/market:ro
    networks:
      - trading-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =============================================================================
  # Redis (상태 캐시, 세션)
  # =============================================================================
  redis:
    image: redis:7-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-redis
    restart: unless-stopped
    ports:
      - "127.0.0.1:${REDIS_HOST_PORT:-6379}:6379"
    environment:
      REDIS_PASSWORD: "${REDIS_PASSWORD-changeme}"
    volumes:
      - redis-data:/data
    command:
      - redis-server
      - --appendonly
      - "yes"
      - --maxmemory
      - 256mb
      - --maxmemory-policy
      - allkeys-lru
      - --requirepass
      - "${REDIS_PASSWORD-changeme}"
    healthcheck:
      test: ["CMD", "sh", "-c", "REDISCLI_AUTH=$${REDIS_PASSWORD-changeme} redis-cli ping || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - trading-network

  # =============================================================================
  # Prometheus (메트릭 수집)
  # =============================================================================
  prometheus:
    image: prom/prometheus:v2.48.0
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-prometheus
    restart: unless-stopped
    ports:
      - "127.0.0.1:${PROMETHEUS_HOST_PORT:-9090}:9090"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--web.enable-lifecycle'
    depends_on:
      dashboard:
        condition: service_healthy
    networks:
      - trading-network

  # =============================================================================
  # Redis Stream Exporter (Redis Stream -> Prometheus)
  # =============================================================================
  stream-exporter:
    build:
      context: .
      dockerfile: Dockerfile.stream_exporter
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-stream-exporter
    restart: unless-stopped
    # Needs host-gateway so REDIS_HOST=host.docker.internal resolves when the
    # stack points at a host redis (paper cutover) instead of the compose redis.
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: ["python", "/app/services/monitoring/stream_exporter.py"]
    ports:
      - "127.0.0.1:${STREAM_EXPORTER_HOST_PORT:-9093}:9093"
    environment:
      <<: *redis-runtime-env
      STREAM_EXPORTER_PORT: "9093"
      STREAM_EXPORTER_STREAMS: "market:ticks,raw_data"
      STREAM_EXPORTER_READ_COUNT: "200"
      STREAM_EXPORTER_BLOCK_MS: "1000"
      STREAM_EXPORTER_SYMBOL_TTL_SECONDS: "1800"
      STREAM_EXPORTER_MAX_SYMBOLS_PER_ASSET: "60"
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - trading-network

  # =============================================================================
  # MLflow (실험 추적 UI 서버 — sqlite backend + 로컬 아티팩트)
  # =============================================================================
  # UI-only over the shared store: the app clients (shared/backtest/mlflow_tracker.py,
  # cli `sts mlflow`, scripts/optimize_strategies.py) WRITE directly to
  # ./mlflow.db via `sqlite:///mlflow.db` — they do not route writes through this
  # server. This container serves the browse UI over that same store. Routing
  # client writes through the server (set MLFLOW_TRACKING_URI + `--serve-artifacts`
  # proxy + migrate existing absolute artifact URIs) is a separate follow-up.
  mlflow:
    build:
      context: .
      dockerfile: Dockerfile.mlflow
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-mlflow
    restart: unless-stopped
    profiles: ["research"]
    ports:
      # localhost-bound (dev/ops tool); browse at http://localhost:5000
      - "127.0.0.1:${MLFLOW_HOST_PORT:-5000}:5000"
    # ${PWD} = repo root (run `docker compose` from here). Existing runs store
    # ABSOLUTE artifact paths in mlflow.db (e.g.
    # /home/.../kis_unified_sts/mlruns/<id>/...), so mlruns must be mounted at
    # that same absolute path — and the artifact root must match it — or the UI
    # cannot resolve artifacts (figures, trades.csv, models). A plain `:/mlruns`
    # mount silently breaks artifact display.
    command:
      - mlflow
      - server
      - --backend-store-uri
      - sqlite:////mlflow.db
      - --default-artifact-root
      - file://${PWD}/mlruns
      - --host
      - 0.0.0.0
      - --port
      - "5000"
    volumes:
      # mlflow.db / mlruns are gitignored runtime data and must pre-exist on the
      # host (a bind mount over a missing file/dir is created as an empty dir,
      # which breaks the sqlite backend). `make` target / runbook ensures this.
      - ./mlflow.db:/mlflow.db
      - ./mlruns:${PWD}/mlruns
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:5000/health', timeout=5).status==200 else 1)"]
      interval: 30s
      timeout: 6s
      retries: 5
      start_period: 20s
    networks:
      - trading-network

# =============================================================================
# Test runner (profile: test)
#
# Runs the full pytest suite in Docker, mirroring .github/workflows/test.yml
# (parallel non-serial pass + serial pass, tests/performance excluded). Uses an
# isolated, password-less, ephemeral `redis-test` — it never touches the real
# `redis` service or its paper-trading DB 1.
#
#   # Run the whole suite (exit code = pytest result):
#   docker compose --profile test run --rm tests
#
#   # Ad-hoc subset:
#   docker compose --profile test run --rm tests pytest tests/unit/strategy -q
#
#   # Tear down the lingering redis-test afterwards:
#   docker compose --profile test down
#
# `run` starts only `tests` + its dependency `redis-test` (not the default
# stack). KIS_RUN_LIVE_INFRA_TESTS=1 opts into the live-infra tests — safe here
# because that target is the isolated redis-test, not DB 1 on the real server.
# =============================================================================
  redis-test:
    image: redis:7-alpine
    profiles: ["test"]
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-redis-test
    # No auth + ephemeral (no volume): matches CI's bare redis:7-alpine and
    # keeps the suite isolated from the real `redis` service's paper DB 1.
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - trading-network

  tests:
    profiles: ["test"]
    build:
      context: .
      dockerfile: Dockerfile.test
    image: ${COMPOSE_PROJECT_NAME:-kis}-tests
    container_name: ${COMPOSE_PROJECT_NAME:-kis}-tests
    environment:
      ENVIRONMENT: test
      REDIS_URL: "redis://localhost:6379/1"
      # Default off (CI parity). The live-infra tests write real Redis keys, but
      # here that target is the isolated redis-test, so 1 is safe if desired.
      KIS_RUN_LIVE_INFRA_TESTS: "${KIS_RUN_LIVE_INFRA_TESTS:-0}"
    # Share redis-test's network namespace so Redis is reachable at
    # localhost:6379 exactly like CI's runner. NOTE: network_mode forbids a
    # sibling `networks:` key — connectivity is inherited from redis-test.
    network_mode: "service:redis-test"
    depends_on:
      redis-test:
        condition: service_healthy

# =============================================================================
# 네트워크
# =============================================================================
networks:
  trading-network:
    driver: bridge

# =============================================================================
# 볼륨
# =============================================================================
volumes:
  redis-data:
  prometheus-data:
  caddy-data:
  caddy-config:
