# =============================================================================
# engine_driver.yaml — TOS paper 배포 승인값 (W-A / A-2)
# =============================================================================
# 승인 출처: operator 2026-09-18 설정값 승인 · 2026-09-23 제안표 채택 확정 ·
#            docs/plans/2026-09-18-tos-config-value-proposal.md §4.2
#
# 제안표 §0 — 이 파일이 무엇이고 무엇이 아닌가:
#   **이것은 첫 부팅 프로파일이지 운영 안전 태세가 아니다.** 여기 적힌 값들은
#   `run` 이 로컬 헤르메틱 환경에서 구동하도록 만드는 값이다. 구조적으로
#   유효하고 서로 정합하지만, **실 자금·실 주문을 전제한 안전 한도가 아니다.**
#   실 배포 태세는 별도 승인 사안이며, 이 표를 근거로 그 승인을 대신할 수 없다.
#
# 원본 템플릿: tos/runtime/config/engine_driver.example.yaml
# 로더: tos_runtime.compose._engine_wiring.load_engine_driver_config
#       (compose/_engine_wiring.py:159)
# 호출부: compose/_finalize_wiring.py:109 · compose/_recovery_wiring.py:127 (둘 다 무조건)
# =============================================================================

# [O] ⚠ 제안표 §4.2 — example 이 **명시적으로 경고**한다: 창이 전체 인박스보다
# 작으면 **거짓 divergence** 를 보고할 수 있다(원장 컨텍스트 소실). 「전체를
# 덮으라」가 문서의 권고다. 로더가 **양의 정수만** 받으므로
# (compose/_engine_wiring.py:142-156 — 0·음수는 「조용히 꺼진 창」이라며 별도
# 거부) 「0 = 전체」를 쓸 수 없고, 첫 부팅 인박스를 충분히 덮는 큰 정수를 둔다.
replay_window_events: 1000000
