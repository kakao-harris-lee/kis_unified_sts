# Weekly Edge Review — alert thresholds + windows.
# Spec: docs/plans/2026-04-20-futures-paradigm-phase4-execution.md §5.3
# Loaded by jobs.weekly_edge_review.WeeklyEdgeReviewConfig.from_yaml().

weekly_edge_review:
  current_window_days: 7              # rolling lookback for current-week stats
  prev_window_days: 14                # rolling lookback for prev-week comparison (always 7-day shift)
  slippage_alert_ticks: 0.5           # avg_slip > X → alert (spec §5.3)
  known_setups:
    - "A_gap_reversion"
    - "C_event_reaction"
